[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    slow: marks tests that perform file I/O or report generation
    flask: marks tests that use the Flask test client
addopts = -v --tb=short
