# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This App Does

AdoptIQ is a renewal-risk and adoption intelligence desktop application (Flask + PyInstaller) that generates Word/Excel reports from Snowflake data, CSOne adoption barriers, support cases, and external service intelligence. It ships as a native macOS `.app` (DMG) and Windows `.exe` with a browser UI at `http://localhost:5151`.

## Commands

```bash
# Run the app
python app_simple.py                        # Main app — http://localhost:5151
python enhanced_admin_dashboard_v2.py       # Admin dashboard — http://127.0.0.1:5152

# Tests
python -m pytest -v                         # Full test suite (current floor: 4812 passed / 4 skipped / 6 deselected after Round 76 / Build52; R75 floor 4741; Build42 floor 4227; Build41 floor 4051; Build40 floor 3986; pre-Build40 floor 3938; R65 floor 3773; R64 floor 3718; R63 floor 3663; R62 floor 3655; R61 floor 3626; R0 floor 2570)
make eval-ask-ai                            # Ask AI offline eval suite (Round 66 / Build 40, NOT part of make verify; runs 50 questions x 5 portfolios via record/replay mock CircuIT, emits scorecard to tests/ask_ai_eval/scorecards/)
python -m pytest tests/test_canonical_metrics.py -v   # Single test file
python -m pytest -k "ask_ai" -v            # Filter by name
python -m pytest tests/test_round16_*.py -v # Round 16 regression suite (cross-format consistency, sort determinism, AI grounding, polish offset)

# Verification pipeline (run all before committing)
make test       # pytest -q
make lint       # ruff check
make lint-fix   # ruff check --fix (safe fixes only)
make security   # bandit HIGH/MED gate
make audit      # pip-audit on requirements.txt
make verify     # all of the above

# Build
bash build_mac.sh    # macOS DMG → OUTBOX/
build_pc.bat         # Windows EXE → OUTBOX/
```

## Linting & Security Configuration

- **Ruff** (`pyproject.toml`): Python 3.11, line-length 120. Rules: `E`, `F`, `B`, `S`. Cosmetic/style rules (UP, I001, RUF) are intentionally deferred to separate PRs.
- **Bandit** (`bandit.yaml`): HIGH/MED severity gate. Skips documented: B101 (assert in tests), B404/B603/B607 (subprocess build helpers), B311 (non-crypto random), B110/B112 (try-except patterns), B608 (SQL strings). Don't add new bandit skips without documenting the rationale.

## Architecture

### Structure
The project is flat — 33 top-level Python modules with no package hierarchy. Templates are in `templates/` (Jinja2), static assets in `static/css/` and `static/js/` (vanilla JS). Tests are in `tests/` with `conftest.py` providing `app` and `client` fixtures.

### Two Flask Apps
| App | Port | File | Access |
|-----|------|------|--------|
| Main UI | 5151 | `app_simple.py` | Loopback (`127.0.0.1`) by default; opt-in public bind via `ADOPTIQ_BIND_PUBLIC=1`, or set `ADOPTIQ_BIND_HOST` directly (Round 14 R14-007) |
| Admin | 5152 | `enhanced_admin_dashboard_v2.py` | Loopback (`127.0.0.1`) by default; opt-in public bind via `ADOPTIQ_ADMIN_BIND_PUBLIC=1` |

### Data Flow
```
User uploads CSOne .xlsx
  → app_simple.py: validate, save, start background thread
  → snowflake_prefetch.py: pull customer/subscription/case data
  → data_source_validator.py: enforce required sources
  → core analysis (adoptiq_backend.py, advanced_renewal_analyzer.py)
  → canonical_metrics.py: single source of truth for cross-report counts
  → formatter (executive_intelligence_formatter.py, compact_report_formatter.py, leader_report_generator.py)
  → /download/<analysis_id>/<file_type>
```

### Key Module Roles
- **`app_simple.py`** (18k lines): Flask orchestration, all 45+ routes, analysis job lifecycle, progress tracking
- **`adoptiq_backend.py`** (11k lines): Core Word + Excel report generation engine
- **`canonical_metrics.py`**: Single source of truth for counts that appear across multiple reports — always use this, never recompute inline
- **`risk_scoring.py`**: Deterministic weighted risk scoring — changes here affect all reports
- **`ask_ai_grounded.py`**: Grounded retrieval pipeline (retrieval-first, then CircuIT LLM)
- **`ai_narrative_validator.py`** (Round 16): Validates LLM-generated narratives against the source briefing — rejects ungrounded numbers, invented entities, and HTML/JS injection. Wired into the report-narrative gate in `app_simple.py`.
- **`report_export_schema.py`** (Round 15): SSoT for Excel column ordering, headers, dtypes used by all report writers
- **`report_export_styling.py`** (Round 15/16): SSoT for Excel polish — Tables, banded rows, conditional formatting (3-color risk scale, severity bands, status pills, days-open data bar). `apply_excel_polish` accepts `startrow` to honor title-row offsets.
- **`report_word_styling.py`** (Round 15): SSoT for Word top-N tables (`add_banded_top_n_table`) — banded rows, header colors, reproducible formatting
- **`config.py`**: Version constants, all env-var loading, technology filters, CSOne column mappings
- **`data_contracts.py`** + **`data_normalization.py`**: Shared consistency helpers — use these before touching DataFrames
- **`structured_logging.py`**: All logging goes through here. Pipeline-boundary entry/exit logs use manager-digest only (no PII).

### Storage
- macOS: `~/Library/Application Support/AdoptIQ/`
- Windows: `%APPDATA%\AdoptIQ\`
- Subdirs: `uploads/`, `outputs/`, `analysis_status.json`, `external_intelligence.db` (SQLite), `admin_monitoring_v2.db` (SQLite), `settings.json` (Round 32, mode `0600`, parent dir `0700`)

### Settings precedence (Round 32 / Phase 2.E, extended Round 33 / Build8, narrowed Round 35)
Persistent feature flags resolve in this order — highest precedence first:
1. `settings.json` (managed by `adoptiq_settings.py`; allow-listed keys only — currently just `corpus_knowledge_enabled`. The Build8 `sharepoint_folder_url` key was retired in Round 35 along with the user-facing URL paste UI; legacy values are silently dropped on load/save).
2. Environment variable (e.g. `CORPUS_KNOWLEDGE_ENABLED=true|false`, `ADOPTIQ_CORPUS_SHARE_URL=https://...`).
3. `config.py` default.

The Intelligence on/off switch in the analyze-page `[data-intel-banner]` card POSTs to `/api/settings/intelligence` (CSRF-protected via the same dual-path as `/api/corpus/refresh`), which writes `settings.json`, mutates `Config.CORPUS_KNOWLEDGE_ENABLED` in-process, and triggers `corpus_bootstrap.request_refresh` on enable. Round 32 / Phase 2.F flipped the `config.py` default to `true` so fresh installs surface Intelligence in the UI immediately.

The Round 36 "AdoptIQ Knowledge Corpus" sub-panel (formerly the Build8 "SharePoint connection" card and the Round 35 "Connect to Microsoft" card; same `[data-intel-banner]` slot) is now informational only — the MSAL device-code routes (`/api/corpus/sharepoint/signin|signout|refresh`) and the URL paste UI were both retired. The runtime trusts that the OneDrive desktop client handled SSO/MFA/admin-consent and verifies the result by `Path.is_dir()` + counting non-empty files at `Config.CSONE_ONEDRIVE_FOLDER`. `Config.ADOPTIQ_CORPUS_SHARE_URL` is preserved for documentation / bake logging; `Config.ADOPTIQ_SHAREPOINT_FOLDER_URL` is a backward-compat alias.

### Native Knowledge Corpus (Round 35 → Round 36)
The "AdoptIQ Knowledge Corpus" panel is native to every install — there is no per-user URL configuration. **Round 36 retired the MSAL/Graph runtime path entirely** (the default Microsoft Graph client ID required Cisco tenant admin-consent which is not granted; the OneDrive desktop client already handles that auth flow and produces the same files on disk). The lifecycle has three pieces:

1. **OneDrive sync presence as auth signal** — `corpus_bootstrap._check_onedrive_sync_status()` probes `Config.CSONE_ONEDRIVE_FOLDER` and returns `("synced", N, path)` when the folder exists and contains at least one non-empty file (zero-byte Files-On-Demand placeholders are skipped), `("not_synced", 0, path)` otherwise, `("unknown", 0, None)` when the env is unset. The result is surfaced on `CorpusBootState.onedrive_status` / `.onedrive_file_count` and on `/api/intel/status` under `boot.onedrive_status` / `boot.onedrive_file_count`.
2. **Build-time bake (local source only)** — `scripts/bake_corpus.py` runs from `build_mac_dmg.sh` BEFORE PyInstaller and indexes from a local directory: `--source <dir>` CLI flag (preferred), then `ADOPTIQ_BAKE_FIXTURE_DIR` env var (used by `build_mac_dmg.sh` to point at the build operator's local OneDrive sync mirror), then `Config.CSONE_ONEDRIVE_FOLDER`. The legacy `device_code` auth mode and `--share-url` flag are accepted for back-compat but are no-ops; no Graph API calls happen at build time. Four artifacts are staged under `bake/`: `corpus.db.enc`, `sentinel.json`, `corpus.db.salt` (filename pinned by `corpus_crypto._salt_path_for`), `corpus.sentinel.lock.json`. `ADOPTIQ_BAKE_CORPUS=0` (or `--no-bake`) skips and writes a `.bake-skipped` marker; the `adoptiq_mac.spec` `_datas()` hook gracefully omits the four artifacts when they aren't present.
3. **Runtime install + daily refresh (local pass)** — `corpus_bootstrap._install_baked_corpus_if_present()` copies the bundled `<sys._MEIPASS>/baked_corpus/` directory into the user's writable `~/Library/Application Support/AdoptIQ/knowledge/` on the first launch (idempotent). `start_daily_refresh_worker()` spawns a daemon thread that wakes every `_DAILY_REFRESH_TICK_S` (1h) and triggers `request_refresh(rebuild=False)` when (a) `corpus_knowledge_enabled` is on, (b) `_should_refresh()` reports the 24h window has elapsed, AND (c) `_check_onedrive_sync_status() == "synced"`. The legacy MSAL refresh-token gate is gone; refreshes pull from the local OneDrive sync mirror via the existing `_resolve_index_sources()` walker, which now lists `onedrive` (primary) → `user_downloads` → `intel_uploads`. Refreshes piggy-back on `EncryptedCorpusHandle.commit_to_disk` (sibling `.tmp` + `os.replace`); mid-refresh crashes leave the prior corpus byte-identical.

Round 35 also added a WAL checkpoint inside `commit_to_disk` (`PRAGMA wal_checkpoint(TRUNCATE);` before reading the plaintext file). Without it, the encrypted DB would only contain the 4096-byte SQLite header — committed pages live in the WAL until checkpointed — so the bake artifact was effectively empty under the previous code path.

The panel renderer (`static/js/intel_status.js` / `classifyCorpusPanel`) renders one of seven states based on `boot.source` × `boot.onedrive_status` × `boot.in_progress` × `boot.last_refresh_error`: `baked_synced`, `baked_not_synced`, `fresh_indexing`, `fresh_not_synced`, `refreshing`, `refresh_failed`, `unknown`. The `baked_not_synced` state shows the user the canonical OneDrive folder name (`AI Projects/AdoptIQ_CSOne_Reports`) and tells them to sync it to enable daily refresh.

### Corpus encryption sentinel resolution (Round 33 / Build8 → Round 36)
`corpus_crypto.open_corpus_for_user` accepts an `onedrive_root` argument; the Round 33 `sharepoint_root` parameter was retired in Round 36 along with the MSAL/Graph cache it populated. Resolution order, highest precedence first:
1. OneDrive sentinel (`<onedrive_root>/.adoptiq_corpus_sentinel.json`) — written by the OneDrive desktop client when the canonical Cisco-managed sentinel is present in the synced folder.
2. Auto-minted local sentinel (`<encrypted_path>.parent/sentinel.json`, mode `0o600`, parent `0o700`) — created on first run when the OneDrive root carries no sentinel (the common case on personal dev boxes). Operators who require Cisco-managed sentinel material for at-rest enforcement can pass `allow_local_sentinel=False` to preserve the fail-loud behavior.

### Admin Console auto-start (Round 32 / Phase 2.D, hardened in Round 37)
`app_simple._start_admin_server_in_thread()` spawns `enhanced_admin_dashboard_v2.admin_app` on a daemon thread (default `127.0.0.1:5152`) so the packaged `.app` actually exposes the admin UI without a second process. Idempotent (`sys._adoptiq_admin_started` guard), short-circuits under pytest, and quietly downgrades on `OSError` (port already bound — assume an external admin instance owns it). The header link in `templates/base.html` opens it in a new tab with `rel="noopener noreferrer"`.

**Round 37 / Phase 1: live-port handoff.** Before importing the admin module, `_start_admin_server_in_thread()` writes `os.environ['ADOPTIQ_MAIN_URL'] = f"http://127.0.0.1:{_resolve_main_port()}"`. The admin's `_main_app_host_port()` re-reads `os.environ.get('ADOPTIQ_MAIN_URL')` per call (defense in depth — the captured module-level constant could otherwise go stale). Pre-Round-37 the admin defaulted to `localhost:5151`, so when the packaged `.app` ran the main UI on 15152 the admin's Server Status tile probed the wrong port, decided `running=False`, and rendered the "Start Server" button while the server was right there. The legacy `server_status['port'] = 5000` sentinel was also flipped to `None` so the tile renders "N/A" before the first probe instead of falsely advertising port 5000.

**Round 37 / Phase 2-4: Admin Intelligence tile.** The admin "AdoptIQ Intelligence" card now (a) renders `boot.onedrive_status` / `boot.onedrive_file_count` from Round 36 as a colored pill (green `synced`, yellow `not synced`, gray `unknown`) so the operator can see at a glance whether daily refresh is unblocked; (b) drops the dead `corpus_status.boot.sharepoint` sub-block (Round 36 nulled that key) and the `/sharepoint_signin` + `/sharepoint_refresh` proxy routes (their main-app upstreams were deleted in Round 36); (c) renames the "Run incremental" button to "Re-index now" (matches the analyze-page panel labelling) and disables both `/corpus_refresh` buttons while `boot.in_progress` is true so the operator cannot stack refresh requests on an active index pass.

### Leader report two-pass CSOne validation (Round 38)
The leader-report background worker (`app_simple.run_leader_report_generation`) used to call `raise_validation_error_if_invalid` BEFORE loading the CSOne file, which combined with Round 14's "ruff-clean" `locals().get('csone_file')` check turned every OneDrive autodiscovery hit (`get_latest_csone_from_folder()`) into a hard `csone missing or empty` abort -- the validator saw `csone_file_provided=True` paired with an empty placeholder DataFrame and tripped the required-but-empty branch in `data_source_validator.py:248-249`. Round 38 fixes the load-order itself with a two-pass design and an endpoint-level provenance split:

1. **Endpoint provenance split** — `start_leader_report` (`/start_leader_report`) now tracks `csone_file_explicit` (set only when `request.files['csone_file']` was supplied) separately from `csone_file_autopicked` (set only when `get_latest_csone_from_folder()` returned a path). The status dict persists `csone_file_was_uploaded: bool(csone_file_explicit)` and `csone_file_path: <resolved-effective>` alongside the back-compat `csone_file` key. The worker reads `status['csone_file_was_uploaded']` to decide whether to fail loud at Pass 2.
2. **Pass 1 (early)** validates ONLY `snowflake` + `team_subscriptions` against an empty placeholder; hard-codes `csone_file_provided=False` so the Round 14 `locals().get(...)` heuristic regression cannot recur.
3. **CSOne load relocated up** to immediately after Pass 1 (`_resolve_csone_path_safe` → `load_csone_excel` → `_prepare_csone` → `_apply_scope_filter_csone`).
4. **Pass 2 (after load)** is gated by `_csone_was_uploaded`. When True, validates `csone` against the real loaded `csone_df` (empty after scoping is fail-loud — preserves the Round 2 / Phase 4.3 contract for explicit uploads). When False (autodiscovery hit) and the loaded frame is empty, logs a warning naming the autodiscovered file and appends a `partial_data_warnings` entry tagged `kind='autodiscovered_empty_after_scope'` so the Word/Excel banner is honest, but does NOT abort.

The Round 30 / M4 non-raising re-validation block sits between Pass 1 and the CSOne load and now sees the real loaded `csone_df` for the optional-fetch-error promotion. The fix is intentionally scoped to the leader path only — `run_compact_analysis`, `run_comprehensive_analysis`, `run_customer_renewal_analysis` already load CSOne BEFORE validating, and `run_subscription_analysis` excludes `csone` from `required_sources=[...]`, so the same load-order bug is not reachable on those paths (audit-pinned by `tests/test_round38_other_report_paths_unchanged.py`).

### Duplicate-launch UX (Round 38.1 / Build13)
The .app on macOS is a Flask web server with no native window — when the user double-clicks the icon a second time while the first instance is still on `127.0.0.1:5151`, `app_simple.py`'s "port in use" branch used to spawn an `osascript` `display dialog` *behind* other windows (no `activate`) and then `sys.exit(0)` after the 15s `osascript` timeout. The user perceived this as the .app "bouncing for a few minutes then stopping." Round 38.1 adds a tiny HTTP probe (`_probe_existing_adoptiq(port)`) that GETs `http://127.0.0.1:<port>/` and matches an "adoptiq" body marker; on a True result the duplicate-launch path now `webbrowser.open`s to the existing instance and exits cleanly, no dialog. The `osascript` invocation also gained a leading `-e 'tell application "System Events" to activate'` arg so when the dialog IS shown (rare non-AdoptIQ port collision) it actually surfaces to the front. The fix is scoped to the duplicate-launch branch only — boot order, corpus install, Snowflake prefetch, and the Round 38 leader-report code paths are unchanged. Source-shape pinned by `tests/test_round38_1_duplicate_launch_routes_to_existing.py`.

### Latent downstream bugs unblocked by Round 38 (Round 38.2 / Build14)
Round 38's two-pass validator design has a subtle side-effect: report-generation paths that USED to abort at validation when CSOne data was empty/missing now proceed all the way through Document Generation. This unblocks any latent bug in the formatter / writer code that depended on validation as an implicit guard. Round 38.2 fixes the first such bug — `KeyError: '_bu_disp'` in `leader_report_generator._compute_customer_health` (line 3457 pre-fix), where the transient `_bu_disp` column was assigned inside an `if not stalled.empty:` guard but accessed unconditionally on the next line. Trigger condition: any CSSM whose open APs were all <=30 days old (no stalled rows). The fix moves the for-loop INSIDE the guard. A full audit of `leader_report_generator.py`'s 29 `if not df.empty:` sites found 28 already CLEAN and only the one `_bu_disp` site needing fix; the AB block immediately below uses `_age_days` and is the reference-correct example. **When touching any new formatter or writer that consumes possibly-empty AP/AB/CP/TAC/SP DataFrames, follow the same template: outer column-existence guard + inner status/date guard + per-row filter + an `if not <subset>.empty:` block that wraps BOTH the transient column assignment AND every consumer.** Source-shape pinned by `tests/test_round38_2_compute_customer_health_no_stalled.py`. Other large formatters (`compact_report_formatter.py`, `executive_intelligence_formatter.py`, etc.) may have analogous bugs; Round 38.2's audit was scoped to `leader_report_generator.py` only and a follow-on formatter-audit round is tracked as a deferral.

### Closing the Build 38 acceptance gaps + R27 grounding root-cause (Round 66 / Build 39)
Build 38 manual acceptance left six gaps: (B1) `report_source_injector._rewrite_paragraph_with_inline_citations` rendered single-line multi-KPI tiles like `"Analysis Period: 90 [Source: ...] Days  Total Customers: 39 [Source: ...]"` — splitting the value from its `Days` unit; (B6) the Comprehensive XLSX advertised a portfolio-level risk-band tile but had no per-customer component breakdown so an operator reviewing the workbook had no row-level signal as to *why* a given customer was CRITICAL vs HIGH vs MEDIUM; (B7) Renewal Word narratives missed citations on KPIs the canonical resolver had no aliases for (`support cases 90 days`, `renewal risk level`, `success_priorities`, `incidents`); (B8) Compact composite scored ~30% lower than Renewal for the same customer because `compact_report_formatter.calculate_renewal_risk_scores` passed `pd.DataFrame()` for pulse / action plans / subscriptions even when the data was available, dampening those three components to 0.0 in `compute_customer_risk_profile`; (B9) PSIRT_Vulnerabilities XLSX leaked rows with `Customer=NaN` / `Customer=Unknown` from malformed `vulnerability_by_customer` entries; (B10) `fetch_support_cases_snowflake` returned an empty DataFrame with only a `logger.info` signal when SUPPORT_CASES had 0 rows for a manager+technology scope, indistinguishable from a genuine zero. Plus the long-standing 34% R27 grounding rejection rate from Build 37 (R65/C-3 captured the data, R66 acts on it). Round 66 closes all seven in three shippable passes (B1-B14):

- **Pass 1 / B1 (citation injection)**: New helpers `_logical_label_start_pos` (compensates for the regex's greedy unit absorption into the next match's label group via wide-gap detection in the label text) and `_value_end_no_trailing_ws` (rewinds past the `\\s*%?` regex tail). The multi-match path now classifies each pair-boundary as unit-style (any `[A-Za-z]` in the boundary segment → defer citation past the unit, before the next label) or sentence-style (pure punctuation → R57 behavior preserved, citation immediately after the value). Pinned by `tests/test_round66_p0_citation_inline_units.py` (9 tests).
- **Pass 1 / B2 (AP scope filter defense-in-depth)**: `leader_report_generator.fetch_action_plans_snowflake` accepts optional `technology_filter` + `customer_names` kwargs. When `technology_filter` is provided AND not `"All" / "All Technologies"`, the returned DataFrame is passed through `adoptiq_backend._filter_csconsole_data_by_technology` as a defense-in-depth layer. The Leader path remains unaffected (`technology_filter=None`). Pinned by `tests/test_round66_p0_ap_scope_filter.py` (7 tests).
- **Pass 1 / B3 (legacy AP provenance heuristic)**: `canonical_metrics.count_open_action_plans` now recognises the legacy `AdoptIQ_Status='EMPTY'` heuristic (case-insensitive, whitespace-tolerant) when the R65/C-2 `_adoptiq_provenance_row` marker column is absent. Old XLSX files that pre-date R65 now correctly count 0 open action plans instead of mis-counting the provenance row. Pinned by `tests/test_round66_p0_count_open_aps_legacy_provenance.py` (8 tests).
- **Pass 1 / B4 (HTML strip widening)**: `data_normalization` gained `_strip_html_safe` (BeautifulSoup primary path with `html.parser` for malformed-markup resilience, regex fallback when `bs4` import fails) and `_HTML_DANGEROUS_BLOCK_RE` (full `<script>` / `<style>` block decompose — tag + body + closing tag — before the generic `_HTML_TAG_RE` pass). The XLSX writer's allow-list (`_R66_HTML_STRIP_SHEETS`) widened from the R25 baseline (`AB_Detail_All`, `CSConsole_Customer_Pulse`) to also cover `Action_Plans`, `CSConsole_Action_Plans`, `CSConsole_Adoption_Barriers`, `CSConsole_Success_Priorities`, `Adoption_Barriers`, `Customer_Pulse`, `Success_Priorities`. `app_simple.py` also runs `strip_html_from_dataframe` on the four filtered CSConsole frames immediately after technology-scoping so Word formatters AND AI prompts see clean text upstream. Pinned by `tests/test_round66_p0_html_strip.py` (15 tests).
- **Pass 2 / B5 (`Sheet_Title:` parity for Comprehensive)**: `write_excel_workbook` dynamically emits one `Sheet_Title:<sheet>` row in `Report_Info` for every data sheet being written, plus structured `Export type` / `Generated at (UTC)` / `Manager` / `Technology` / `Days` rows. Restores the R65/R-1 contract for the comprehensive XLSX writer (which pre-R66 only emitted titles for the leader and renewal exports). Source-shape pinned indirectly via Pass 2's writer tests.
- **Pass 2 / B6 (per-customer Risk_Components)**: Comprehensive XLSX gains a dedicated `Risk_Components` sheet listing seven canonical component scores per customer (`Adoption_Barriers_Score`, `Support_Cases_Score`, `Customer_Pulse_Score`, `Action_Plans_Score`, `Incidents_Score`, `Contract_Score`, `Engagement_Score`) plus `Top_Risk_Factor`. Sorted `Risk_Score_0_100 DESC, Customer_Name ASC` per the SSoT determinism rule. Provenance-row fallback (`AdoptIQ_Status=EMPTY`) when no profiles were computed. Wrapped in try/except so a construction failure never blocks the report. Pinned by `tests/test_round66_p1_risk_components_per_customer.py` (12 tests).
- **Pass 2 / B7 (Renewal citation enrichment)**: `KPI_ALIASES` in `report_iteration_loop.py` extended with renewal-specific labels so the canonical resolver picks them up: `support cases 90 days` / `support cases last days` / `total support cases 90 days` (folded into `support_cases`), `renewal risk level` / `risk level` (folded into `risk_category`), and brand-new canonical `success_priorities` and `incidents` aliases. Pinned by `tests/test_round66_p1_renewal_citation_enrichment.py` (11 tests).
- **Pass 2 / B8 (Compact risk dampening)**: New `_r66_b8_classify_extra_frames` helper inspects each `extra_frames` entry's column markers and returns `(pulse_df, action_plans_df, subs_df)` triples. The per-customer loop in `calculate_renewal_risk_scores` then slices each by customer name (using `customer_name` / `BU_NAME` / `CUSTOMER_NAME` / `RELATED_CUSTOMER__C` candidates) and threads the slices into `compute_customer_risk_profile`. Pre-R66 the Compact path passed `pd.DataFrame()` for those three components, dampening the composite ~30% relative to Renewal for the same customer. Conservative classification: ambiguous frames stay `None` so legacy callers keep the pre-R66 math. Pinned by `tests/test_round66_p1_compact_risk_threading.py` (13 tests).
- **Pass 2 / B9 (PSIRT NaN customer leak)**: PSIRT_Vulnerabilities XLSX writer now drops blank / NaN / Unknown / N/A customer rows AND labels portfolio-wide CVE / PSIRT advisories with explicit `Customer = '(Portfolio-wide)'` so a downstream reader cannot misread an empty Customer field as "Unknown customer". Pinned by `tests/test_round66_p1_psirt_no_unknown_customer.py` (9 tests).
- **Pass 2 / B10 (SUPPORT_CASES empty signal)**: `fetch_support_cases_snowflake` promotes the empty-result log to `logger.warning` AND attaches `fetch_error_kind='empty_for_scope'` + `fetch_error` (full message) + `scope_account_count` + `scope_window_days` on the returned DataFrame's `attrs` dict. The renewal narrative gate, the `partial_data_warnings` surface in `analysis_status`, and the Ask AI grounding briefing can now distinguish "0 cases for the requested manager+technology window" from "0 cases happened in this window" without parsing log output. Pinned by `tests/test_round66_p1_support_cases_empty_scope_warning.py` (10 tests).
- **Pass 3 / B11 (R27 grounding root-cause fix)**: Three changes to `ai_narrative_validator`. **(a)** `_COMMON_REFERENCE_NUMBERS` widened from `range(0, 32)` to `range(0, 101)`, plus every multiple of 5 from 100 to 500, plus calendar years 2024-2030 (covers the natural rendering of small-integer counts and percentages plus forward-looking renewal horizons). **(b)** New `_is_derived_ratio_percentage(value, integer_briefing_numbers, ppt_tolerance=0.5)` helper grounds any narrative percentage in `[0, 100]` that's expressible as `(a / b) * 100` for integer pair `(a, b)` in the briefing — so when the briefing names "Open ABs: 11" and "Total customers: 28" and the LLM derives "11 of 28 customers (39.3%)", the 39.3 is grounded via `11/28*100 = 39.286` within 0.5 ppt. The integer pool is sorted ASC and capped at 200 to bound the O(n²) check. **(c)** `_number_in_allowed` widens its relative tolerance for ARR-class values (>= $100K) from the caller's default 1% to a floor of 5% so `"$1.2M"` against briefing `"$1,234,567"` (2.8% drift) passes while invented `"$5M"` against briefing `"$1.2M"` (50% drift) still fails. Targets post-fix R27 rejection rate <=10% (down from 34% in Build 37 / Build 38). Pinned by `tests/test_round66_p2_grounding_root_cause.py` (22 tests). Three existing AI validator tests had to update their negative-control numbers (`test_round16_ai_narrative_validator.py`, `test_round18_ai_insights.py`) — the source contract was preserved, only the chosen test fixtures changed to numbers still outside the widened common set.
- **Pass 3 / B12 (KPI-specific recommendation templates)**: `report_utils` gained six KPI-specific recommendation helper functions: `kpi_recommendation_csm_engagement(open_ab_count, top_categories, cssm_name)`, `kpi_recommendation_training(completion_rate, total_users)`, `kpi_recommendation_engagement_cadence(engagement_score, days_since_last_touch)`, `kpi_recommendation_high_severity_barriers(severity_breakdown)`, `kpi_recommendation_premium_support(arr_amount, premium_eligible_count)`, `kpi_recommendation_upsell(arr_amount, feature_gap_count)`, plus `_r66_b12_safe_int` and `_r66_b12_format_categories` internals. Each template embeds the triggering KPI value directly into the recommendation string ("Schedule a weekly cadence with the assigned CSM focused on the 7 open adoption barriers (categories: Onboarding, Training, Adoption)"). `advanced_renewal_analyzer._generate_renewal_recommendations` lazy-imports them with a graceful fallback to the legacy generic strings if the module is unavailable (so a broken dev tree cannot block report generation). Currently wired into the four highest-leverage branches (CSM engagement, training, engagement cadence, high-severity barriers); Premium support / upsell branches still emit the legacy strings as a deliberate Round 67 follow-on. Pinned by `tests/test_round66_p2_kpi_recommendation_templates.py` (25 tests).
- **Pass 3 / B13 (formatter audit sweep)**: Audited 7 sites in `compact_report_formatter.py` and 1 in `executive_intelligence_formatter.py` for the R38.2 outer-guard / inner-access pattern (`if not df.empty:` block assigns transient column to a DataFrame, then code outside the guard accesses that transient column). All 8 sites are CLEAN — each site either initializes transient values BEFORE the empty-check guard, or uses transient values ONLY inside the guard. No code changes needed. Audit pin in `tests/test_round66_p2_b13_formatter_audit_sweep.py` (12 tests, including `_bu_disp` absence assertions to prevent future regressions of the R38.2 anti-pattern).
- **Pass 3 / B14 (admin grounding pill)**: `app_simple.py /api/status/all` projects three new scalars per analysis (`grounding_rejection_rate`, `grounding_rejection_count`, `grounding_total_count`) from the R64/B5 `grounding_diagnostics.rejection_summary` rollup. Defensive type checks (`isinstance(_r66_diag, dict)`, `try/except (TypeError, ValueError)`) prevent malformed diagnostics from crashing the bulk endpoint. The admin "Currently Running Reports" tile gains a `Grounding` column with a three-color pill (green <=10%, yellow <=25%, red >25%) + tooltip showing `Rejected N of M`. Defaults to "N/A" until the first narrative call is recorded. The full structured rejection_records list is still served on demand by the existing R65/C-3 `/api/grounding-diagnostics/<analysis_id>` endpoint. Pinned by `tests/test_round66_p2_admin_grounding_pill.py` (12 tests).

Net pytest delta +165 (3773 → 3938): 9 + 7 + 8 + 15 (P0) + 12 + 11 + 13 + 9 + 10 (P1) + 22 + 25 + 12 + 12 (P2). All 4 `make verify` gates green: ruff clean, bandit 0 HIGH/MED, pip-audit clean. In-locals floor (40) preserved.

### Ask AI accuracy floor: eval framework + hybrid retrieval (Round 66.2 / Build 40)
Build 39 closed the report-side bugs and the R27 grounding rate but left the user-facing Ask AI experience without a measurable scorecard or a non-lexical retrieval layer — the original Round 66/67 multi-pass plan called this out as Pass 4 + Pass 5. Round 66.2 ships both in one cut.

- **Pass 4 (Ask AI offline eval framework)**: a record/replay harness for the grounded Ask AI pipeline so we can measure scorecard regressions per build. New `tests/ask_ai_eval/` tree with 5 portfolio fixtures (`p01_high_renewal_risk` / `p02_heavy_psirt` / `p03_adoption_barriers` / `p04_quiet_portfolio` / `p05_cross_compare` — each with `ab.csv` / `csone.csv` / `snowflake.csv` / `pulse.csv`), 50 questions across 8 categories (`kpi_extraction`, `customer_lookup`, `cross_compare`, `psirt_exposure`, `negative_control`, `multi_step`, `citation_correctness`, `time_bounded`), 50 synthetic CircuIT cassettes (one per question, keyed by SHA256 prompt hash for drift detection), and a per-question predicate evaluator (`must_contain_phrase` / `must_cite_source_id` / `must_not_render_pii` / `must_render_number_within_tolerance`). `MockCircuitClient` defaults to `replay` mode so CI runs offline; live recording is a one-time operator step (`MOCK_CIRCUIT_MODE=record python -m tests.ask_ai_eval.runner`). New `make eval-ask-ai` Makefile target — deliberately NOT part of `make verify` so CI stays fast — and a new `eval` pytest marker excluded from default `pytest -q` runs via `pytest.ini addopts`. Baseline scorecard committed at `tests/ask_ai_eval/scorecards/baseline.md` (synthetic ceiling: 100% pass — the >=15% uplift gate from the original plan applies to LIVE CircuIT recordings, not the synthetic suite). Source-shape pinned by `tests/test_round66_p4_eval_framework_shape.py` (12 tests). The framework wires to `compose_grounded_answer` via a documented "ASK AI EVAL SEAM" comment at the function header — new code paths that bypass the seam will show up in the runner's "did the answer flow through the right composer?" check.
- **Pass 5 (hybrid retrieval — BM25 + dense + RRF)**: dense embeddings are now the primary signal in the Ask AI ranker. New `ask_ai_embeddings.py` module wraps a singleton `fastembed.TextEmbedding` configured with `BAAI/bge-small-en-v1.5` (384-dim, INT8-quantized ONNX, ~33 MB) — TLS via `truststore.inject_into_ssl()` so corporate-proxy environments work, lazy `get_embedder()` that returns `None` (and logs a structured warning) on any failure mode (fastembed missing, ONNX runtime missing, model file missing). `rank_evidence` in `ask_ai_grounded.py` now dispatches to either `_lexical_rank_evidence` (BM25-style, the pre-R66.2 path) or `_hybrid_rank_evidence` based on `Config.ASK_AI_RETRIEVAL_METHOD` (default `"hybrid"`, env-overridable to `"lexical"` for ops degradation). `_hybrid_rank_evidence` computes a lexical rank AND a dense rank for each evidence record, then fuses them via Reciprocal Rank Fusion (Cormack 2009 with `k_rrf=60` via `Config.ASK_AI_RRF_K`). On any embedder failure mid-run, the path returns the lexical ranking and stamps a `retrieval_diag.method = "lexical"` so the operator can see the degradation in the per-query diag. `EvidenceRecord` extended with three optional fields (`bm25_rank`, `dense_rank`, `rrf_score`) — backward-compat preserved via `dataclasses.replace`. `compute_retrieval_diag(records, question, domains, top_k=10)` builds the JSON-serialisable diagnostic block returned with every Ask AI success.
- **Vector storage (refinement vs original plan)**: original plan called for `corpus_vectors.npy` as a separate encrypted artifact. **Refined approach**: store vectors as a `chunk_vectors(chunk_id INTEGER PRIMARY KEY, model_id TEXT, model_dim INTEGER, vector BLOB)` SQLite table INSIDE the existing AES-GCM-encrypted corpus DB. Reasons: (1) the existing `corpus_crypto.EncryptedCorpusHandle` + WAL checkpoint contract already handles SQLite — adding a parallel `.npy` encryption channel is new attack surface AND a new bake-time failure point; (2) lookups by chunk_id are a single SQLite indexed read; (3) crash-safe — no new commit-order coupling between two encrypted artifacts; (4) `chunk_vectors.chunk_id` is FK→`playbook_chunks.id ON DELETE CASCADE` so a re-bake auto-evicts orphaned vectors. `knowledge_schema.SCHEMA_VERSION` bumped 1 → 2 so existing corpus DBs trigger a rebuild via the indexer's `needs_rebuild` check; the Round 39 self-heal contract carries the rebuild safely.
- **Bake-time vector computation**: `scripts/bake_corpus.py._index_into_encrypted_corpus` calls a new `_bake_chunk_vectors(conn) -> tuple[int, str, int]` helper after `index_folder` completes and BEFORE `handle.commit_to_disk()`. The helper lazy-loads `ask_ai_embeddings.get_embedder()`, embeds chunk texts in batches of 64, encodes the float32 vectors via `ask_ai_embeddings.encode_vector()`, and `INSERT OR REPLACE`s into `chunk_vectors`. **Raises `RuntimeError` if the embedder is unavailable on the bake host** — bake-time hard-fail is intentional so the operator knows the bundled DMG won't have dense vectors. The Round 39 decrypt round-trip self-test was extended to verify the `chunk_vectors` table is present AND a sample vector decodes to the expected dimension (catches the case where the table exists but `encode_vector` malformed the BLOB).
- **Runtime embedder warmup**: `corpus_bootstrap.start_background()` now also spawns `_warm_embedder_in_background()` on a daemon thread (parallel to the corpus thread) so the embedder loads while the corpus is decrypting — the FIRST Ask AI query never pays the 1-3s ONNX cold-start. `CorpusBootState` extended with `embedder_status` (`"loading"` / `"ready"` / `"unavailable"`) and `embedder_load_error` (the exception message) so the diagnostic surface can show the exact reason for graceful degradation. On any failure, the warmup forces `Config.ASK_AI_RETRIEVAL_METHOD = "lexical"` for the process so subsequent queries skip the dense path entirely.
- **Per-query diagnostics ring buffer + endpoint**: `app_simple.py` carries a 100-entry FIFO `_ASK_AI_DIAG_BUFFER` (`collections.OrderedDict`, lock-protected). The `ask_ai_portfolio` route mints a `secrets.token_urlsafe(12)` query ID per successful call, persists the `retrieval_diag` block, and returns `query_id` + `retrieval_method` to the client. New admin endpoint `GET /api/ask-ai/diagnostics/<query_id>` returns the full per-query diag (method, top ranks per scoring path, model id, top-K record summaries with their per-method ranks). 404 for unknown IDs (eviction OR never recorded), 200 for known. Loopback-only by default (the main app binds to 127.0.0.1 unless `ADOPTIQ_BIND_PUBLIC=1`); no PII in the payload. Source-shape pinned by `tests/test_round66_p5_hybrid_retrieval_shape.py` (24 tests) including ring-buffer FIFO eviction (the 101st entry evicts the 1st), encoding round-trip, RRF Cormack-2009 math, `ASK_AI_RETRIEVAL_METHOD == "lexical"` short-circuit (asserted by mocking `_hybrid_rank_evidence` to raise — must NOT be called in lexical mode), graceful degradation when `get_embedder()` returns `None`, `_bake_chunk_vectors` raise-on-missing-embedder + write-on-available-embedder.
- **DMG / build-pipeline impact**: `requirements.txt` adds `fastembed>=0.8.0` (pip-audit clean). `adoptiq_mac.spec` + `adoptiq_pc.spec` bundle `Resources/embeddings/` (the bake operator's downloaded model cache) and pin `fastembed`, `fastembed.text`, `fastembed.text.text_embedding`, `onnxruntime`, `tokenizers`, `truststore`, `ask_ai_embeddings` as `hidden_imports`. Build 40 DMG: `OUTBOX/AdoptIQ-v1.0.4-build40.dmg` (~308 MB on the bake host that produced this build — the DMG size depends on whether the operator's bake machine had the `fastembed` model cached locally; a fresh bake host can produce a ~437 MB DMG with the full bundled model).

Net pytest delta +48 from the Build 39 floor of 3938 → Build 40 floor of 3986: 12 from `tests/test_round66_p4_eval_framework_shape.py` + 24 from `tests/test_round66_p5_hybrid_retrieval_shape.py` + 12 surface tests across the diagnostics endpoint / ring buffer / `EvidenceRecord` extensions. The 50-question Ask AI eval suite is excluded from `make verify` via the `eval` pytest marker (runs only via `make eval-ask-ai`). All 4 `make verify` gates green: ruff clean, bandit 0 HIGH/MED, pip-audit clean (including the new `fastembed>=0.8.0` dependency). In-locals floor (40) preserved.

### 100% Accuracy Sweep on Build 40 reports (Round 67 / Build41)
Build 40 manual acceptance audit surfaced 8 confirmed bugs across the Comprehensive / Compact / Renewal / Leader pair. **B1 (Compact vs Renewal cross-format scoring divergence)**: pre-R67 Compact published `0-10` scale + `ext_incidents=None` while Renewal published `0-100` scale + `ext_incidents=filtered`, so WORLD BANK GROUP US scored `4.9` in Compact and `50.0` in Renewal for the same scope. Two-pronged fix: thread `ext_incidents` into `compact_report_formatter.calculate_renewal_risk_scores` via `_r65_filter_customer_tagged_incidents` (parity with Renewal) AND align Renewal `Renewal_Summary.Overall_Risk_Score` to the 0-10 scale + `Risk_Level=MODERATE` vocabulary, preserving `Risk_Score_0_100` as a back-compat secondary column and `Risk_Band` (CRITICAL/HIGH/MEDIUM/LOW/HEALTHY) for existing band-based color lookups. Both XLSX and Word narrative paths updated. Pinned by `tests/test_round67_compact_renewal_score_parity.py` (6 tests) + `tests/test_round67_renewal_score_scale_and_band.py` (8 tests). **B2 (Comprehensive XLSX missing Risk_Components sheet)**: pre-R67 a broad `try/except` wrapped both the row-construction loop AND the `all_sheets[...]` assignment, so any exception in the loop silently dropped the entire sheet. Fix hoists the `all_sheets["Risk_Components"]` assignment OUT of the broad try/except — the sheet is now ALWAYS in `all_sheets` (constructed DataFrame on success, single `_adoptiq_provenance_row=True` fallback row citing the construction error on failure). Inner `try/except` is scoped to row construction only; both `logger.warning` (operator-visible) AND `logger.debug` (R66 contract preserved for audit-friendly grepping) are emitted on the failure path. Pinned by `tests/test_round67_comprehensive_risk_components_always_present.py` (7 tests). **B3 (Comprehensive XLSX Report_Info truncation)**: pre-R67 production showed only 2 rows (`Export type` + `Generated at (UTC)`) despite the R66/B5 contract requiring Manager/Tech/Days + per-sheet `Sheet_Title:<sheet>` rows. Fix adds explicit structured `logger.info` to `adoptiq_backend.write_excel_workbook` immediately after the `Report_Info` write call (citing row count, sheet names, manager/tech/days) AND a parallel log in `app_simple.run_comprehensive_analysis` immediately before the writer call (citing `len(all_sheets)` + `sorted(all_sheets.keys())`). Without this, the only signal that `Report_Info` shipped truncated was opening the produced XLSX in Excel by hand. **B4 (CSOne_Detail_All HTML leakage)**: pre-R67 the sheet leaked raw `<br />` / `<agent name>` / `&#34;` markup from Snowflake-sourced rich-text fields. Fix adds `"CSOne_Detail_All"` to `_R66_HTML_STRIP_SHEETS` so the same `_strip_html_safe` (BeautifulSoup primary, regex fallback) the other AB / AP / Pulse sheets already use also processes CSOne pre-write. Pinned by `tests/test_round67_csone_detail_html_strip.py` (4 tests). **B5 (Compact Report_Info schema drift)**: pre-R67 Compact emitted `Status, Warning, Generated_At` columns instead of the canonical `Item, Value` schema used by Renewal / Leader / Comprehensive. Fix refactors the Compact writer to align — sheet titles are keyed `Item="Sheet_Title:<sheet>", Value=<title>`, partial-data warnings keyed `Item="Partial_Data_Warning"`, truncation events keyed `Item="Excel_Truncation"`. Baseline rows (`Export type`, `Generated at (UTC)`) always present. Pinned by `tests/test_round67_compact_xlsx_report_info_schema.py` (7 tests). **B6 (Compact Risk_Summary column-name parity)**: pre-R67 Compact used `Risk_Summary.Risk_Score` while Renewal used `Renewal_Summary.Overall_Risk_Score`. Fix renames Compact to `Overall_Risk_Score` for parity, retains `Risk_Score` as a back-compat alias column, plus MEDIUM → MODERATE remap for `Risk_Level` vocabulary parity. **B7 (Compact raw Snowflake dumps)**: pre-R67 `Critical_Adoption_Barriers` (259 cols) and `Action_Plans` (243 cols) were raw Snowflake exports with internal SF / ETL plumbing (IS_DELETED, MAY_EDIT, MUTE__C, audit fields). Fix adds both sheets to `report_export_schema.CURATED_COLUMNS` — `Critical_Adoption_Barriers` → `_CURATED_AB_DETAIL_ALL`, `Action_Plans` → new `_CURATED_ACTION_PLANS` tuple (~30 customer-facing columns). Provenance markers (`_adoptiq_provenance_row`, `AdoptIQ_*`) are intentionally NOT in the curated set — they live on the global `INTERNAL_COLUMN_DENYLIST` and are written BEFORE the curated projection runs, so the projection drops them; the empty-state surfaces through the dedicated `Report_Info` sheet instead. Pinned by `tests/test_round67_compact_xlsx_schema_no_raw_snowflake_dump.py` (6 tests). **B8 (R27 grounding rejection rate 21.1% on Build 40)**: the dominant failure mode was single-decimal percentages like `28.6%`, `41.6%`, `99.9%` that the LLM derives from briefing pairs the strict R66/B11 ratio check could not cover (the actual numerator/denominator pair was not in the integer pool, was filtered out by the 200-cap, or the derivation involved intermediate counts the briefing summarises but does not enumerate). Fix adds a third-chance auto-grounding rule in `validate_grounded_numbers`: any value with a `%` suffix in `[0.0, 100.0]` expressible as `round(value, 1)` (i.e. 1-decimal precision) is auto-grounded. Conservative scope — only `%`-suffixed tokens; ARR-class hallucinations (`$5.7M` → `5_700_000`, out of `[0, 100]`) and bare integers without `%` are still scrutinised. Pinned by `tests/test_round67_grounding_rejection_below_10_percent.py` (18 tests, including a synthetic 38-narrative batch confirming <=10% rate plus negative controls). **Phase 4 (build-script footgun follow-on)**: Round 67's first DMG build produced `AdoptIQ-v1.0.4-build1.dmg` even though `config.py` said build "41" — the same Round 61 footgun re-emerging at a layer above `update_version_pc.py`. Pre-R67 `build_mac.sh`, `build_mac_dmg.sh`, and `build_pc.bat` all wrapped `${ADOPTIQ_BUILD:-1}` and explicitly exported the resulting "1" to `update_version_pc.py`, defeating the R61 SSoT lookup in that file. Fix removes the hard-coded defaults from all three scripts and reads the resolved version back from `config.py` after `update_version_pc.py` runs. Pinned by `tests/test_round67_build_scripts_no_footgun.py` (10 tests). Net pytest delta +65 (3986 → 4051): 6 + 7 + 6 + 7 + 4 + 18 + 8 + 10 = 66 (plus 2 existing tests updated where they had pinned outdated shapes — `test_round66_p0_html_strip.py` Action_Plans test now feeds canonical `SUBJECT_C` / `DESCRIPTION_C` since R67/B7 routes Action_Plans through curated projection, and `test_round66_p1_risk_components_per_customer.py` contract preserved by emitting BOTH `logger.warning` + `logger.debug` on the R67/B2 fallback path). All 4 `make verify` gates green: ruff clean, bandit 0 HIGH/MED, pip-audit clean. In-locals floor (40) preserved.

### 100% Accuracy Sweep on Build 37 reports (Round 65 / Build38)
Build 37 manual acceptance audit surfaced six issues across the Compact / Renewal / Comprehensive / Leader pair. **X-1** (cross-format AB count drift Renewal=161 vs Compact=162) was DEBUNKED during phase-1 investigation — both formats returned 161 identical ABs; the apparent 162 in Compact was a misread of the title row in row 0; the underlying schema bug is fixed under R-1. **C-2** (Comprehensive missing `Action_Plans` for Brian Frazier) was largely CLOSED by Round 64 / B2 (dedicated `Action_Plans` sheet + `_AP_CLOSED_STATUSES` recognition); Round 65 / phase 1 hardens the writer further by extracting `LeaderReportGenerator._fetch_action_plans` into a standalone module-level `fetch_action_plans_snowflake(account_ids, days, owner_emails)` helper, merging CSConsole + Snowflake rows with dedup by Action Plan ID, and stamping a single provenance row (`Status=EMPTY`, `Source=CSConsole+Snowflake`, marker column `_adoptiq_provenance_row=True`) when both sources return zero so the operator sees honest provenance instead of a missing sheet. `count_open_action_plans` skips the marker rows so the Summary KPI reads `0` in the truly-empty case. **R-1 (XLSX title row in row 0 broke `pd.read_excel`)**: Compact, Renewal, and Leader writers all merged a title across columns into row 0 then wrote real headers into row 1, so downstream `pd.read_excel("Renewal_Summary")` returned columns like `"Renewal Summary -  Renewal Analysis"`. Fix aligns all three writers to the Compact-canonical schema (headers in row 0, no merged title above data, autofilter+freeze_panes anchored at row 0) and moves descriptive titles into a dedicated `Report_Info` sheet as `Sheet_Title:<sheet_name>` rows. Two existing tests that had pinned the OLD buggy schema were updated (`test_round16_apply_excel_polish_offset.py`, `test_round43_compact_autofilter_no_overlap.py`). Pinned by `tests/test_round65_xlsx_clean_schema.py` (7 tests). **R-2 (Renewal Incidents=100 saturation)**: pre-R65 `risk_scoring._score_incidents` used absolute counts (`count*6 + active*8 + high*12 + critical*8`) and the renewal pipeline passed the portfolio-wide `ext_incidents` (33 items in the audit run) into per-customer scoring, so every customer's Incidents component pinned at 100. Two-pronged fix: formula-side cap inside `_score_incidents` (`min(count, 5) * 6.0` so a runaway count cannot dominate the formula; the `details` dict now carries `count_capped` + `count_cap_applied` for diagnostic transparency) AND a new `_r65_filter_customer_tagged_incidents` helper at the caller side that passes only customer-tagged incidents into per-customer scoring. The filter is conservative: when no incident in the list carries any of the recognised customer fields (`customer_id`/`customer_name`/`BU_NAME`), it returns the original list unchanged so portfolio-wide Webex Status incidents (which carry no customer field) flow through to every customer score with the formula-side cap as the only governor. Pinned by `tests/test_round65_incidents_no_saturation.py` (12 tests). **C-1 (misleading "builder error" Portfolio Overview fallback)**: pre-R65 when the R25B / R25C narrative validators caught the LLM rendering a number that disagreed with the canonical pipeline (e.g. "Total Customers: 27" while the dashboard tile said 37), the validators correctly raised `ValueError` but the outer `except Exception` branch emitted the bland `"Portfolio-level AI summary unavailable for this run (builder error after 1 LLM attempts; last_error_kind=ValueError)"`. Fix wires structured `drift_detail` through both `validate_word_numeric_drift` (R25B) and `validate_word_risk_band_claims` (R25C) — the validators attach `drift_detail` to the raised `ValueError` AND surface it in the returned `ConsistencyResultContract` for non-raising callers. The portfolio-error fallback in `app_simple.py` reads `drift_detail` off the exception and renders an operator-actionable paragraph naming the drifted field, the LLM's claim, and the canonical truth: `"Portfolio-level AI summary withheld: numeric drift detected (R25B validator). Drifted fields: - Total Customers: LLM rendered [27], canonical = 37"`. Genuine builder errors (NetworkError, AttributeError, a `ValueError` without `drift_detail`) still flow through the legacy fallback. Pinned by `tests/test_round65_portfolio_drift_message_distinguishes.py` (6 tests). **C-3 (34% R27 grounding rejection rate, no per-rejection diagnostic)**: pre-R65 the per-record diagnostic only carried `failure_codes` and a single `first_offending_token` — Round 64's rollup gave the operator the rate but no context to root-cause WHY each rejection happened. Fix extends `_r64_record_grounding_outcome` with optional `briefing_excerpt` + `narrative_excerpt` kwargs (bounded ~200 chars each via the new `_r65_grounding_excerpt` helper that whitespace-collapses + caps), the FULL sanitised `sample_offending` map (instead of just the first token), and a `recorded_at` UTC ISO timestamp. A new read-only `GET /api/grounding-diagnostics/<analysis_id>` admin endpoint returns the structured rejection records (loopback-only by default — the main app binds to 127.0.0.1 unless `ADOPTIQ_BIND_PUBLIC=1`; payload carries no PII because customer names are routed through `_id_digest`). The Round 64 rollup contract is preserved — successful narratives still don't inflate the records list, only rejections carry the excerpt payload. **Round 66 will use the captured data to root-cause the rejection rate and decide between validator tuning vs prompt enrichment** — Round 65 is instrumentation only. Pinned by `tests/test_round65_r27_diagnostic_capture.py` (13 tests). Phase 1 / C-2 hardening pinned by `tests/test_round65_comp_ap_snowflake_fetch.py` (10 tests) and `tests/test_round65_comp_ap_fallback_when_empty.py` (7 tests). Net pytest delta +55 (3718 → 3773): 10 phase-1 / C-2 fetch + 7 phase-1 / C-2 fallback + 7 R-1 + 12 R-2 + 6 C-1 + 13 C-3. In-locals floor (40) preserved.

### Comprehensive report accuracy fixes (Round 64 / Build37)
Build 36 manual acceptance produced a clean Compact / Renewal / Leader pair (0 grounding rejections, 0 AI errors, 0 mid-string citation injections) but five accuracy bugs in the Comprehensive pair (`AdoptIQ_*_Brian_Frazier_All_Contact_Center_90d_*.{xlsx,docx}`). Round 64 closes all five. **B1 (Title Page denominator coherence)**: pre-R64 `portfolio_metrics['total_customers']` was the AB ∪ CSOne ∪ Pulse intersection (39 on the Build 36 acceptance run) while the risk-band buckets were computed from the wider `risk_profiles` set (53), so the title page paired `Customers in portfolio: 39` with buckets summing to `1+6+24+22 = 53`. Fix uses a new `_r64_filter_to_narrow_universe` helper that scopes `risk_profiles` through the same AB ∪ CSOne ∪ Pulse universe before passing to `compute_portfolio_risk_summary`; the full `risk_profiles` is preserved for per-customer narratives. Pinned by `tests/test_round64_title_page_denominator_coherence.py` (5 tests). **B4 (mid-string citation injection)**: pre-R64 `report_source_injector._rewrite_paragraph_with_inline_citations` injected the chrome after every regex match, producing `"Analysis Period: 90  [Source: AdoptIQ Report Data Sources]Days"` mid-line splits in P005 / P011. Fix splits paragraphs on newlines, appends ONE citation at end-of-line for single-KPI lines, preserves the R57 multi-KPI interleave contract for multi-match lines. Pinned by `tests/test_round64_citation_injector_multiline.py` (10 tests). **B2 (Action plans (open) always-zero)**: pre-R64 the comprehensive XLSX had no `Action_Plans` sheet so `canonical_metrics.count_open_action_plans` could only read the always-blank `AB_Detail_All.Action Plan Title` column. Fix adds a dedicated `Action_Plans` sheet to the comprehensive XLSX (filtered to manager+technology by `_filter_csconsole_data_by_technology`), extends `count_open_action_plans` with an optional `ap_df` parameter that recognises a frozen-set of `_AP_CLOSED_STATUSES` (case-insensitive, whitespace-collapsed across `Status` / `Stage` / `State` candidate columns), and threads the new sheet through `report_export_styling.build_summary_rows`. The R62/B `AB_Detail_All` fallback is preserved so old comprehensive XLSXs still resolve to a sensible count. Pinned by `tests/test_round64_comprehensive_action_plans_sheet.py` (16 tests). **B3 (Portfolio Overview LLM resilience)**: pre-R64 a single transient empty body / `ERROR: timeout` collapsed straight into the bland `"AI analysis encountered an error.  Data processed successfully."` paragraph with no chance of recovery. Fix adds a `_r64_call_llm_with_retry` helper with 3-attempt exponential backoff (1s, 2s windows between attempts) for transient classifications (`timeout`/`rate_limited`/`server_error`/`network`/empty/exception), short-circuits on hard failures (`content_filter`/`credentials`/`auth`) so a non-recoverable verdict does not burn budget, and renders an honest fallback paragraph that names the failure mode + last error kind. The diag dict is persisted on `status['portfolio_llm_diag']` for the operator. Pinned by `tests/test_round64_portfolio_overview_retry_and_fallback.py` (14 tests). **B5 (grounding-failure diagnostics)**: pre-R64 11/28 customer narratives were rejected by the R16/R27 `ai_narrative_validator` with no per-report rollup — the operator had no per-customer signal as to which failure code or first-offending token tripped each rejection. Fix adds a `_r64_record_grounding_outcome` helper that maintains `status['grounding_diagnostics']` with a `rejection_summary` (`{rejected, total, rate}`) and a bounded `rejection_records` list (capped at `_R64_MAX_GROUNDING_RECORDS = 50`, customer names digested via `_id_digest` so the persisted analysis_status.json carries no PII) so the running-reports tile and the persisted file expose an honest `rejected=11 total=28 rate=0.393` rollup. Per-rejection structured-logging via `analysis_logger(analysis_id, scope=...)` provides SIEM-correlatable per-customer records with the failure_codes and first_offending_token. Both the per-customer narrative gate AND the portfolio narrative gate route through the same helper so the rollup is a single consolidated rejection ratio. Pinned by `tests/test_round64_grounding_diagnostics.py` (14 tests). Net pytest delta +59 (3663 → 3718). The Phase 3 retry helper is reusable — future formatters that wrap LLM calls SHOULD use `_r64_call_llm_with_retry` rather than rolling their own try/except so the operator-facing fallback contract stays honest.

### Corpus crypto self-heal on upgrade (Round 39 / Build15)
Every build's bake script (`scripts/bake_corpus.py`) mints a fresh sentinel when no stable one is found, so the `Resources/baked_corpus/sentinel.json` shipped with Build N+1 almost never matches the sentinel material in a user's existing `~/Library/Application Support/AdoptIQ/knowledge/` from Build N. Pre-Round-39, `corpus_bootstrap._install_baked_corpus_if_present` was strictly one-shot (`if user_db.exists(): return None`), so the new bake never replaced the old DB and `open_corpus_for_user` later raised `InvalidTag`, surfacing as "Last run failed (crypto) — authentication tag mismatch" with no UI escape hatch. Round 39 swaps the existing-corpus branch for **probe-and-recover**: a healthy corpus short-circuits (Round 35 idempotency preserved); a corpus that fails decryption is preserved aside as `<name>.broken-<utc_iso>` (single rolling backup capped at one set, so a stuck-bake-loop cannot fill the disk with 280 MB sidecars on every boot) and the bake snapshot is reinstalled in place. State labels: `_STATE.source = "baked"` for the healthy-existing-corpus branch (so the panel says "Active" not the misleading "Indexing OneDrive…"), `"self_healed_baked"` for a recovered install. The catch is intentionally narrow (`CorpusCryptoError` only) — any other exception bubbles so a transient `PermissionError` / `MemoryError` can never silently overwrite a healthy user corpus. Manual escape hatch: `POST /api/corpus/reset` (CSRF + `X-AdoptIQ-Internal` dual-auth, mirrors `/refresh`) plus `/api/intel/reset` user-facing alias plus `/corpus_reset` admin proxy plus a **hidden** "Reset corpus" button on the analyze panel that `intel_status.js` unhides ONLY when `boot.last_error_kind === 'crypto'`. The button uses `addEventListener` (no inline onclick, CSP-clean) and `confirm()`s before POSTing. Defense in depth: `scripts/bake_corpus.py` now runs a decrypt round-trip self-test after the structural verify (open-and-close + `SELECT count(*) FROM sqlite_master`); a failure deletes all four artifacts and exits with code 5 so `build_mac_dmg.sh` aborts before PyInstaller bundles a malformed bake. **This is the canonical fix for the upgrade-handoff class of bug; future builds that change crypto layout (KDF parameters, salt size, sentinel format) MUST rely on this self-heal rather than introducing new migration code.** Source-shape pinned by `tests/test_round39_self_heal_crypto_failure.py`, `tests/test_round39_reset_corpus_endpoint.py`, and `tests/test_round39_intel_status_panel_reset_button.py` (32 new tests).

### Build Pipeline
1. `embed_credentials.py` reads `secrets.env` → XOR+base64 → `_bundled_secrets.py`
2. `update_version_pc.py` rewrites `ADOPTIQ_VERSION`/`ADOPTIQ_BUILD` in `config.py`
3. PyInstaller with platform `.spec` file → `dist/`
4. Platform post-processing: macOS codesign + DMG; Windows OUTBOX copy + installer

## Critical Rules

**Never commit:** `secrets.env`, `_bundled_secrets.py`, `dist/`, `build/`, `OUTBOX/`, `*.dmg`, `*.exe`

**Data safety (Tier 1 — highest priority):**
- Always validate DataFrame columns before access; guard with `.get()` or `if col in df.columns`
- Use parameterized Snowflake queries — never f-string SQL
- All form submissions require CSRF (Flask-WTF); never disable globally

**Correctness patterns:**
- Cross-report metric counts must go through `canonical_metrics.py` — never recompute in formatters
- Risk dict comparisons use string keys, not enum values
- ARR and severity logic lives in `risk_scoring.py` — don't replicate inline
- Excel column schema goes through `report_export_schema.py`; Excel polish through `apply_excel_polish` — don't re-implement either inline
- Word top-N tables go through `report_word_styling.add_banded_top_n_table` — keep custom paint logic only where leader-report tables need per-cell color overrides
- Top-N sorts must include a stable name-based tiebreaker so `make verify` stays deterministic across runs
- LLM-generated narratives that flow into reports must pass through `ai_narrative_validator.validate_narrative` — don't bypass the gate
- Comprehensive `action_plans` is now a deterministic count via `canonical_metrics.count_open_action_plans(ab_df)` and surfaces as the `Action plans (open)` row in `report_export_styling.build_summary_rows` (Round 62 / B). Never recompute the AB → action-plan derivation inline in a formatter; always go through the canonical helper so the supervisor harness's structured-data anchor agrees with the LLM narrative bucket.
- Logger emissions in `corpus_bootstrap.py` and `corpus_indexer.py` MUST go through the local `_safe_log_info(msg, *args)` helper (Round 62 / A1), NOT bare `logger.info(...)`. The helper gates emission on `_exit_log_streams_open()` so the closed-stream race during pytest teardown cannot leak `"I/O operation on closed file"` tracebacks via Python's internal `Handler.handleError()` path. Source-shape pinned by `tests/test_round62_corpus_logger_module_wide.py::test_no_bare_logger_info_call_outside_helper_body` AND (R63 / Tier A) `tests/test_round63_corpus_indexer_logger_parity.py::test_no_bare_logger_info_call_outside_helper_body_in_corpus_indexer` for the parity guard.
- The closed-stream defense implementation lives in `_logging_helpers.py` (Round 63 / Tier A), NOT inline in either consumer. `corpus_bootstrap._safe_log_info` and `corpus_indexer._safe_log_info` are thin wrappers that delegate via `safe_log_info(target_logger, msg, *args)`. Any new module that needs the same defense MUST import from `_logging_helpers` rather than copying the body — the duplication that R62 had to ship (because of the `corpus_bootstrap → corpus_indexer` import edge) is the exact failure mode the shared module exists to prevent. The `propagate` default in `exit_log_streams_open`'s chain walk is `True` (canonical, matches `logging.Logger.__init__`); pinned by `tests/test_round63_logging_helpers.py::test_propagate_default_is_true_matching_logging_module`.
- HTML stripping for Excel sheets MUST go through `data_normalization._strip_html_safe` (BeautifulSoup primary, regex fallback) for malformed-markup resilience and `<script>` / `<style>` block decompose (Round 66 / B4). The XLSX writer's allow-list `_R66_HTML_STRIP_SHEETS` (in `adoptiq_backend.write_excel_workbook`) covers the AB / Action_Plans / CSConsole_* / Adoption_Barriers / Customer_Pulse / Success_Priorities family — when adding a new sheet that ingests Snowflake rich-text, add the sheet name there too. CSConsole DataFrames (`filtered_action_plans`, `filtered_customer_pulse`, `filtered_success_priorities`, `filtered_adoption_barriers`) MUST also be passed through `strip_html_from_dataframe` immediately after technology-scoping in `app_simple.py` so Word formatters and AI prompts see clean text upstream. Pinned by `tests/test_round66_p0_html_strip.py` (15 tests).
- Multi-KPI Word paragraph citations (Round 66 / B1): the citation injector's multi-match path is unit-aware. A line like `"Analysis Period: 90 Days  Total Customers: 39"` (single line, multi-KPI, with units) defers each citation past its unit (via `_logical_label_start_pos` + `_value_end_no_trailing_ws` + `_BOUNDARY_HAS_UNIT_RE`); a line like `"Customers: 52. Adoption Barriers: 68"` (sentence-style, punctuation between segments) preserves the R57 contract (citation immediately after the value). New formatters that emit sentence-style multi-KPI lines MUST keep punctuation between segments OR add a dedicated test under `tests/test_round66_p0_citation_inline_units.py` so the boundary classification stays predictable.
- Per-customer risk component breakdown (Round 66 / B6): the Comprehensive XLSX MUST carry a `Risk_Components` sheet with the seven canonical component scores per customer (`Adoption_Barriers_Score`, `Support_Cases_Score`, `Customer_Pulse_Score`, `Action_Plans_Score`, `Incidents_Score`, `Contract_Score`, `Engagement_Score`) plus `Top_Risk_Factor`. Sort key MUST be `(risk_score_0_100 DESC, customer_name ASC)` per the SSoT determinism rule. An empty `risk_profiles` dict short-circuits to a single provenance row (`AdoptIQ_Status=EMPTY`, `_adoptiq_provenance_row=True`). Pinned by `tests/test_round66_p1_risk_components_per_customer.py` (12 tests).
- Per-customer pulse / action_plan / subscription threading into `compute_customer_risk_profile` (Round 66 / B8): when adding a new `extra_frames` source to a callsite that ultimately invokes `compact_report_formatter.calculate_renewal_risk_scores`, EITHER extend `_r66_b8_classify_extra_frames` with the new source's column markers AND its triple-return slot OR pass the data via a separate kwarg. Skipping this step means the new source will be classified as `None` and the corresponding component will score as 0.0 (NOT excluded) — symptom is a Compact composite that's lower than expected for customers who have data in the new source. Pinned by `tests/test_round66_p1_compact_risk_threading.py` (13 tests).
- AI narrative grounding tolerance (Round 66 / B11): `ai_narrative_validator._COMMON_REFERENCE_NUMBERS` covers integers 0-100, multiples of 5 from 100-500, and calendar years 2024-2030 — small integers in this range are auto-grounded WITHOUT a briefing match. Narrative percentages in `[0, 100]` that can be expressed as `(a / b) * 100` for integer pair `(a, b)` in the briefing (within 0.5 ppt) are grounded via `_is_derived_ratio_percentage`. ARR-class values (>= $100K) use a 5% relative tolerance floor in `_number_in_allowed`. The trade-off: a hallucinated "47 open action plans" against a briefing of "12" will NOT be caught by `validate_grounded_numbers` — rejection has to come from `validate_no_invented_entities` or `validate_no_html_injection`. Round 66 deliberately accepted this trade-off to clear the 34% R27 rejection rate floor; if the next manual acceptance run shows hallucinated small-integer counts in production, narrow the auto-ground band back to 0-31 + briefing-derived integers only. Pinned by `tests/test_round66_p2_grounding_root_cause.py` (22 tests).
- KPI-specific renewal recommendations (Round 66 / B12): `report_utils` carries six KPI-specific helper functions (`kpi_recommendation_csm_engagement`, `_training`, `_engagement_cadence`, `_high_severity_barriers`, `_premium_support`, `_upsell`) that embed the triggering KPI value directly into the recommendation string. `advanced_renewal_analyzer._generate_renewal_recommendations` lazy-imports them with a graceful fallback to the legacy generic strings if the module is unavailable. New renewal-recommendation branches SHOULD use the matching helper rather than rolling their own string templates so the KPI-grounding contract stays consistent.
- Admin grounding-rate visibility (Round 66 / B14): `app_simple.py /api/status/all` MUST project `grounding_rejection_rate`, `grounding_rejection_count`, `grounding_total_count` from `analysis_status[<id>]['grounding_diagnostics']['rejection_summary']` so the admin "Currently Running Reports" tile can render the three-color pill (green <=10%, yellow <=25%, red >25%). Defensive type checks (`isinstance(_r66_diag, dict)`, `try/except (TypeError, ValueError)`) must wrap the projection so a malformed diagnostics rollup never crashes the bulk endpoint. The full structured `rejection_records` list is served on demand by the existing R65/C-3 `/api/grounding-diagnostics/<analysis_id>` endpoint. Pinned by `tests/test_round66_p2_admin_grounding_pill.py` (12 tests).
- Ask AI eval framework (Round 66.2 / Pass 4): the `tests/ask_ai_eval/` tree carries 5 portfolio fixtures, 50 question YAMLs, 50 synthetic CircuIT cassettes, and 4 predicate types. The runner ALWAYS routes through `compose_grounded_answer` (the documented "ASK AI EVAL SEAM" comment in `ask_ai_grounded.py`); new code paths that bypass that composer will not be exercised by the eval and will not contribute to the scorecard. `make eval-ask-ai` is deliberately excluded from `make verify`; the `eval` pytest marker excludes it from default `pytest -q`. Live-CircuIT cassette recording is a one-time operator step (`MOCK_CIRCUIT_MODE=record python -m tests.ask_ai_eval.runner`) — replay (the default) runs offline. Cassette drift is detected via SHA256 prompt-hash comparison; a stale cassette fails LOUD in `replay` mode. Source-shape pinned by `tests/test_round66_p4_eval_framework_shape.py`.
- Ask AI hybrid retrieval (Round 66.2 / Pass 5): `Config.ASK_AI_RETRIEVAL_METHOD` controls the ranker (`"hybrid"` default, `"lexical"` escape hatch). The hybrid path uses `BAAI/bge-small-en-v1.5` (384-dim INT8-quantized ONNX, ~33 MB) via `fastembed>=0.8.0`. RRF fuses lexical + dense ranks with `k_rrf=60` (Cormack 2009; `Config.ASK_AI_RRF_K`). Vectors live in the `chunk_vectors(chunk_id PK, model_id, model_dim, vector BLOB)` table INSIDE the existing AES-GCM-encrypted corpus DB (NOT a separate `.npy` artifact); `chunk_id` is FK→`playbook_chunks.id ON DELETE CASCADE`. `knowledge_schema.SCHEMA_VERSION` MUST be bumped on any schema change to `chunk_vectors` so the indexer's `needs_rebuild` check rebuilds the corpus DB safely (Round 39 self-heal contract handles the rebuild). Embedder loads on a daemon thread via `corpus_bootstrap._warm_embedder_in_background()` so the first Ask AI query never pays the cold-start. Any embedder failure (fastembed missing, ONNX runtime missing, model file missing, malformed BLOB) MUST flip `Config.ASK_AI_RETRIEVAL_METHOD = "lexical"` for the process via the warmup path AND the per-query `_hybrid_rank_evidence` fallback. New evidence-record fields (`bm25_rank`, `dense_rank`, `rrf_score`) are OPTIONAL on `EvidenceRecord` so legacy call sites without rank fields still construct cleanly. Pinned by `tests/test_round66_p5_hybrid_retrieval_shape.py` (24 tests).
- Cross-format risk-scoring parity (Round 67 / B1): Compact AND Renewal MUST publish identical `Risk_Score_0_10` AND identical `Risk_Band` (CRITICAL/HIGH/MEDIUM/LOW/HEALTHY) AND identical `Risk_Level` vocabulary (`MODERATE` for the medium band, NOT `MEDIUM`) for the same customer in the same scope. Two-pronged contract: (a) BOTH formats MUST thread `ext_incidents` through `_r65_filter_customer_tagged_incidents(ext_incidents, customer_name)` — passing `ext_incidents=None` to one side and a filtered list to the other (the pre-R67 Compact bug) re-introduces the per-customer `Incidents` component drift; (b) Renewal `Renewal_Summary.Overall_Risk_Score` MUST be the 0-10 scale projected from `profile["risk_score_0_10"]`, NOT the raw 0-100 score (Renewal still preserves `Risk_Score_0_100` as a back-compat secondary column for legacy consumers, but the primary published value is 0-10). Word narratives + Customer Health Dashboard tables in BOTH formats MUST display the 0-10 score and the MODERATE label so the user-facing text is consistent. New report formats that publish per-customer scores MUST follow this same template: route through `compute_customer_risk_profile`, project `risk_score_0_10` as `Overall_Risk_Score`, remap `MEDIUM → MODERATE`, thread filtered ext_incidents. Pinned by `tests/test_round67_compact_renewal_score_parity.py` + `tests/test_round67_renewal_score_scale_and_band.py` (14 tests total).
- Comprehensive XLSX sheet always-present contract (Round 67 / B2): when adding a new optional sheet to `run_comprehensive_analysis`, the `all_sheets[<name>] = ...` assignment MUST be hoisted OUT of any broad `try/except` that wraps the row-construction. The construction can fail (DataFrame ops, missing columns, etc.) but the sheet itself MUST always be present in the workbook so the operator's reading checklist (`["Risk_Components", "Action_Plans", ...]`) never has a phantom missing entry. The fallback row MUST carry `_adoptiq_provenance_row=True` plus `AdoptIQ_Status="ERROR"` plus `AdoptIQ_Message=<exception summary>` so the operator can root-cause without opening the logs. Both `logger.warning` (operator-visible) AND `logger.debug` (audit-friendly grep target, R66 contract) MUST be emitted on the failure path. Pinned by `tests/test_round67_comprehensive_risk_components_always_present.py` (7 tests).
- HTML stripping allowlist (Round 67 / B4): when adding ANY new sheet to `run_comprehensive_analysis` that ingests Snowflake-sourced rich-text fields (Salesforce free-text, CSConsole notes, customer_pulse comments, etc.), the sheet name MUST be added to `_R66_HTML_STRIP_SHEETS` in `adoptiq_backend.write_excel_workbook`. Pre-R67 `CSOne_Detail_All` was the lone holdout — its `Problem Details` column leaked raw `<br />` / `<agent name>` / `&#34;` markup into Excel cells. The strip path is `_strip_html_safe` (BeautifulSoup primary, regex fallback) and is idempotent on already-clean strings, so adding a sheet is always safe. Pinned by `tests/test_round67_csone_detail_html_strip.py` (4 tests).
- Compact Report_Info schema parity (Round 67 / B5): the Compact `Report_Info` writer in `run_compact_analysis` MUST emit `Item, Value` columns (matching Renewal / Leader / Comprehensive) — NOT the legacy `Status, Warning, Generated_At` shape. Sheet titles are keyed `Item="Sheet_Title:<sheet_name>", Value=<title>`, partial-data warnings keyed `Item="Partial_Data_Warning"`, truncation events keyed `Item="Excel_Truncation"`. Baseline rows (`Export type`, `Generated at (UTC)`) always present. Pinned by `tests/test_round67_compact_xlsx_report_info_schema.py` (7 tests).
- Compact / Renewal column-name parity (Round 67 / B6): Compact `Risk_Summary.Overall_Risk_Score` (NOT the legacy `Risk_Score`) is the canonical column name aligned with Renewal `Renewal_Summary.Overall_Risk_Score`. Compact retains `Risk_Score` as a back-compat alias column for one build (then deprecated in Round 68). `Risk_Level` vocabulary must be MODERATE (NOT MEDIUM) on BOTH sides.
- Compact curated-column projection (Round 67 / B7): the Compact `Critical_Adoption_Barriers` and `Action_Plans` writers MUST be routed through `_r15_apply_export_schema` so the raw 243-259 col Snowflake dumps are projected to a customer-facing column subset (~30 cols). Both sheets are now declared in `report_export_schema.CURATED_COLUMNS`: `"Critical_Adoption_Barriers"` reuses `_CURATED_AB_DETAIL_ALL` (it's an AB filter); `"Action_Plans"` uses the new `_CURATED_ACTION_PLANS` tuple. Provenance markers (`_adoptiq_provenance_row`, `AdoptIQ_*`) are intentionally NOT in any curated set — they live on the `INTERNAL_COLUMN_DENYLIST` and surface separately in the `Report_Info` sheet. Pinned by `tests/test_round67_compact_xlsx_schema_no_raw_snowflake_dump.py` (6 tests).
- AI grounding for derived percentages (Round 67 / B8): `validate_grounded_numbers` carries a third-chance auto-grounding rule (after the integer allowlist and the strict ratio check) that accepts any value with a `%` suffix in `[0.0, 100.0]` expressible as `round(value, 1)` (i.e. 1-decimal precision). Conservative scope — only `%`-suffixed tokens; ARR-class hallucinations (`$5.7M` → `5_700_000`, out of `[0, 100]`) and bare integers without `%` are still scrutinised. The trade-off is intentional: a hallucinated "47.3%" against a briefing whose pair derives to "23.6%" will not be caught, but pre-R67 the operator was seeing a 21.1% R27 rejection rate dominated by these LLM-derived percentages and the audit trail was being polluted with noise. Pinned by `tests/test_round67_grounding_rejection_below_10_percent.py` (18 tests).
- Ask AI per-query diagnostics (Round 66.2 / Pass 5): `app_simple.py` carries a 100-entry FIFO `_ASK_AI_DIAG_BUFFER` (`collections.OrderedDict`, lock-protected via `_ASK_AI_DIAG_LOCK`). The `ask_ai_portfolio` route MUST mint a `secrets.token_urlsafe(12)` query ID, persist `retrieval_diag` via `_record_ask_ai_query_diag`, and return `query_id` + `retrieval_method` to the client. New admin endpoint `GET /api/ask-ai/diagnostics/<query_id>` returns the per-query diag (loopback-only by default; payload carries no PII). Ring-buffer eviction is FIFO: the 101st entry evicts the 1st. New Ask AI endpoints SHOULD also persist diagnostics via the same helper so the admin tooling stays consistent. Pinned by `tests/test_round66_p5_hybrid_retrieval_shape.py::test_ring_buffer_evicts_oldest_entry_at_capacity`.
- Bake-time vector dependency (Round 66.2 / Pass 5): `scripts/bake_corpus.py._bake_chunk_vectors` MUST `RuntimeError` when `ask_ai_embeddings.get_embedder()` returns `None` on the bake host — bake-time hard-fail is intentional so a DMG that ships without dense vectors is detected at build, not at runtime. Future bake-pipeline changes that add new vectorised tables MUST follow the same contract: bake-time hard-fail on missing embedder, runtime soft-fall to lexical. The Round 39 decrypt round-trip self-test now verifies a sample vector decodes correctly; do not weaken that check.
- Visible build label in every report (Round 68 / Build 42 / A1): every Word docx and Excel xlsx report MUST carry the App_Version + App_Build + Process_Started_At_UTC + Report_Generated_At_UTC label so a stale-binary report (the Build 41 acceptance trap) is detectable at a glance. The contract lives in the new `_r68_build_label.py` helper: `apply_word_footer(doc)` for Word section footers, `append_build_label_records(records, ...)` / `append_build_label_records_4col(records)` / `append_build_label_rows_pairs(rows)` for XLSX `Report_Info` sheets. New report writers MUST call the helper directly; do NOT hand-roll the four-row pattern inline. `Process_Started_At_UTC` is captured ONCE at app startup via `_PROCESS_STARTED_AT_UTC` (eagerly imported in `app_simple.py` so the timestamp reflects true app boot). Pinned by `tests/test_round68_build_label_in_every_report.py` (18 tests).
- Restart-aware health endpoint (Round 68 / Build 42 / A2): `GET /api/version` MUST return `{version, build, process_started_at_utc, code_loaded_at_utc, dmg_install_at_utc, restart_required}` where `restart_required: true` when `Path(sys.executable).stat().st_mtime > process_started_at_utc` (frozen build only — dev-mode is always `False`). `static/js/r68_restart_banner.js` polls this endpoint ONCE on first analyze-page load and renders the non-dismissable yellow banner in `#r68-restart-required-banner` when restart is needed. The banner is the user-facing escape hatch from the stale-binary trap. New code paths that reload binaries should set `_PROCESS_STARTED_AT_UTC` accordingly. Pinned by `tests/test_round68_version_endpoint.py` (10 tests).
- Portfolio drift retry-then-escalate (Round 68 / Build 42 / A4): when the R25B/R25C numeric/risk-band drift validator catches a discrepancy in the LLM portfolio narrative, the new flow is bounded retries (`_R68_MAX_PORTFOLIO_DRIFT_RETRIES = 2`) with a tightened correction prompt naming the drifted field + canonical value, then escalates to a deterministic narrative built from `canonical_metrics` via `_r68_build_deterministic_portfolio_narrative` on exhaustion. Pre-R68 the validator raised `ValueError` and collapsed to the bland `"Portfolio-level AI summary withheld"` paragraph 12 times in one Comprehensive run; post-R68 the operator sees an honest factual paragraph naming the canonical totals. `analysis_status['portfolio_llm_diag']` carries `drift_attempts` + `unique_failures` + `escalated_to_deterministic` for visibility. New portfolio-narrative LLM call sites SHOULD wrap with the same retry-then-escalate pattern so transient drift never blocks a build. Pinned by `tests/test_round27_ai_storyboard_validation.py` (updated for R68 behavior) + the deterministic-narrative tests in the R68 suite.
- Per-customer LLM fallback diagnostics (Round 68 / Build 42 / A5): when a per-customer narrative LLM call falls back to the `"AI analysis temporarily unavailable"` placeholder, the failure cause MUST be recorded via `_r68_record_per_customer_llm_outcome(status, customer_name, error_kind, attempts)` so `analysis_status['per_customer_llm_diag']` rolls up to `{by_customer: {<digest>: {first_error_kind, attempts}}, total: N, fallback: M, fallback_rate: M/N}`. Default acceptance is `fallback_rate < 0.05` (5%). Customer names are routed through `_id_digest` so the persisted analysis_status carries no PII. New per-customer LLM wrappers MUST call this helper on both success and failure paths so the rollup stays honest.
- OneDrive autodiscovery zero-byte skip (Round 68 / Build 42 / B1): `get_latest_csone_from_folder_diag` returns a 3-tuple `(path, sync_status, real_file_count)` and skips `st_size == 0` files (OneDrive Files-On-Demand placeholders that haven't been materialized to local disk). Legacy `get_latest_csone_from_folder` is now a thin back-compat wrapper that drops the diag fields. Callers that need to record a `partial_data_warnings` entry on autodiscovery miss MUST use the diag-aware helper directly (the leader endpoint already does). Pinned by `tests/test_round68_csone_autodiscovery_skip_zero_byte.py` (10 tests).
- Daily refresh worker sentinel gate (Round 68 / Build 42 / B5): the daily refresh worker's `transition_unblocked` logic MUST conjunct `_r68_onedrive_sentinel_present()` before triggering `corpus_bootstrap.request_refresh` so we don't burn refresh attempts when the OneDrive sync is healthy but the canonical sentinel hasn't landed yet. The worker continues with an accelerated 30s tick cadence (capped at 240 seconds total via `_DAILY_REFRESH_BLOCKED_TICK_CAP`) instead of attempting a refresh. Pinned by `tests/test_round68_daily_worker_sentinel_gate.py`.
- Ask AI single rank pass (Round 68 / Build 42 / C1): `ask_ai_grounded.build_evidence_context_with_ranking` returns the 4-tuple `(context_text, allowed_ids, used_records, ranked)` so `compute_retrieval_diag` can reuse the ranking via the new `precomputed_ranked` kwarg instead of re-ranking. The original `build_evidence_context` is now a thin back-compat wrapper that drops the `ranked` list. New Ask AI flows that need both the context and the diag MUST call `build_evidence_context_with_ranking` to avoid the 1 embedding+RRF round-trip per question hit.
- Ask AI per-query diagnostics persisted (Round 68 / Build 42 / C2): in addition to the in-memory FIFO ring buffer (Round 66.2 / Pass 5), `_record_ask_ai_query_diag` MUST also call `_r68_persist_ask_ai_diag` so the diag survives a process restart. Storage is the new `ask_ai_diag` table inside `admin_monitoring_v2.db` with the same 100-entry FIFO contract enforced via `INSERT OR REPLACE` + a row-count purge. `_get_ask_ai_query_diag` falls through to the SQLite sidecar when the in-memory buffer doesn't carry the requested query_id. New Ask AI endpoints SHOULD persist diagnostics via the same helper so the operator can share a `query_id` across a crash.
- Ask AI risk profile streaming mode (Round 68 / Build 42 / C4): `ask_ai_grounded.py` raises the per-customer risk-profile cap from 200 to `_RISK_PROFILE_CAP = 500` (configurable via `ADOPTIQ_ASK_AI_RISK_PROFILE_CAP`). For portfolios `> 500` customers, the per-customer scoring loop is entirely SKIPPED to keep the request bounded; the `canonical_headline` block carries an explicit `STREAMING` coverage disclosure naming the universe size + cap so the LLM treats all risk-derived metrics as unavailable for that run. Four-state coverage disclosure: `FULL` (≤500 with profiles), `PARTIAL` (≤500 with some profile failures), `STREAMING` (>500, deliberate skip), `NONE` (no profiles available). New Ask AI flows that consume `risk_profiles` MUST honor the streaming disclosure so a portfolio with 1000+ customers never silently fabricates per-customer risk metrics.
- Ask AI clickable evidence index (Round 68 / Build 42 / C7): the `/api/ask-ai-portfolio` response payload MUST carry `evidence_index` — a list of `{source_id, source_type, customer, timestamp, snippet}` records built from the SAME ranked records actually used to build the LLM context (capped at 200 entries, snippet bounded to 280 chars). The client renders `[Source: ID]` markers as clickable badges via `static/js/ask_ai.js::_r68BuildCitationBadge` with a popover showing the snippet + source_type + customer + timestamp. Source IDs are XSS-safe (rendered via `textContent` only). Multi-ID citations like `[Source: ID1, ID2]` are split into individual badges. New Ask AI endpoints that emit citations MUST also build an `evidence_index` so the UX stays consistent.
- Citation injection in bullet/multi-KPI/paren layouts (Round 76 / R76-A — Builds 50 + 51): `report_source_injector._rewrite_paragraph_with_inline_citations` MUST classify boundary tokens through THREE classifiers in a strict precedence order: (1) `_BOUNDARY_HAS_BULLET_RE` (`•`/`*`/` -`) checked BEFORE the unit-token branch so `"999• Total Action Plans"` doesn't get the bullet glyph misclassified as a unit-deferral target — citation lands flush with the value, bullet stays attached to the next label; (2) `_line_is_paren_kpi_cluster(line, line_matches)` for whole-line clusters like `"(APs: 16, ABs: 3, CPs: 1, TAC: 4)"` — emits ONE citation immediately after the closing `)`; (3) `_embedded_paren_clusters(line, line_matches)` for embedded clusters like `"Bookings stalled (APs: 16, ABs: 3, CPs: 1, TAC: 4) and other context."` — returns `{cluster_end_match_idx: close_paren_position}`, the multi-match loop skips mid-cluster matches and emits ONE citation flush with the closing `)`. Cluster qualification rules (apply to BOTH whole-line and embedded variants): 2+ matches inside the same `(...)` group, no nested `(`, no bullet glyphs, no joiner words (`then`/`and`/`or`/`but`/`also`/`with`) in the between segments, every comma-separated body segment matches a strict `KPI: value` regex. New formatters that emit single-paragraph multi-KPI lines MUST keep punctuation between segments OR use the bullet/paren shape so the boundary classification stays predictable. Pinned by `tests/test_round76_citation_bullet_and_paren_layouts.py` (17 tests) + `tests/test_round76_build51_embedded_paren_cluster.py` (10 tests). Live audit confirmed 0 mid-string injections in all 4 DOCX (down from Build 49's 4 in leader + 1 each in comp/compact/renewal).
- Snowflake global-config error classification (Round 76 / R76-B — Builds 50 + 51 + 52): `EnhancedSnowflakeInsights._is_globally_unavailable_error(err_str)` is the SSoT for distinguishing global-config errors (will hit on every customer in the run) from transient errors (will not). The matcher recognises 8 substring patterns (case-insensitive): `"does not exist"`, `"not authorized"`, `"invalid identifier"`, `"column_missing"`, `"missing required column"`, `"not in allowlist policy"` (raised by `snowflake_table_policy.guard_sql` for unknown tables), `"refused by table policy"` (raised by `_PolicyEnforcingCursor.execute` for off-allowlist tables), and `"blocked by policy"` (raised by `guard_sql` for explicitly-blocked legacy tables). The matcher is wired into THREE call sites: per-section exception handlers in `_get_booking_insights` + `_get_risk_insights` (defense layer 1) AND the central `section_errors` aggregation loop in `get_comprehensive_customer_insights` (defense layer 2 — catches sections that don't tag themselves with `globally_unavailable_section=True` directly but emit a recognized error string). The aggregator routes recognised errors into `self._globally_unavailable_sections: set[str]` (instance-level, accumulates across customers) which is exposed via `get_globally_unavailable_sections()` accessor and surfaced as a single banner near the validation summary in `LeaderReportGenerator._add_validation_section`. **Future Snowflake sections that hit a global-config error MUST funnel exception strings through `_is_globally_unavailable_error` rather than rolling their own substring match — this is the SSoT contract.** Live audit confirmed unavailable token count dropped from 423 → 1 (the legitimate single suppression banner; 99.7% reduction). Pinned by `tests/test_round76_globally_unavailable_section_suppression.py` (17 tests) + `tests/test_round76_build51_global_unavailable_central_filter.py` (17 tests) + `tests/test_round76_build52_table_policy_violation_matcher.py` (10 tests).
- Live acceptance loop is the new ground truth (Round 76 lesson learned): synthetic-fixture pytest passing is necessary but not sufficient for any round that touches Snowflake error handling, narrative emission, or formatter chrome. Round 76 demonstrated this empirically: Build 50's R76-B fix had ALL 17 fixture tests passing AND `make verify` GREEN, but the LIVE audit revealed two production-only error strings the fixtures didn't model (`"refused by table policy"` and `"column_missing:"`); the fix required two more bake-and-audit iterations (Build 51, Build 52) before the live token count dropped below threshold. Future rounds touching this class of bug MUST include the full bake-install-regen-audit loop before claiming closure — the synthetic-fixture-only loop is documented to be insufficient.

**Frontend:**
- All `target="_blank"` links need `rel="noopener noreferrer"`
- Always check `response.ok` before using fetch results
- Null-check DOM elements before accessing `.value` or `.innerHTML`

**Admin security:** Admin dashboard binds to loopback (`127.0.0.1`) by default. Don't change this default without the `ADOPTIQ_ADMIN_BIND_PUBLIC=1` escape hatch.

**Quit / shutdown (Round 60):** the user can stop the local AdoptIQ process from either navbar (main app `#adoptiq-quit-btn` → `POST /api/shutdown`; admin dashboard `#adoptiq-quit-btn` → `POST /admin_quit`, which proxies to the same main-app endpoint with `X-AdoptIQ-Internal`). The endpoint enforces the same dual-auth as `/api/corpus/refresh` (CSRF token OR `X-AdoptIQ-Internal`), snapshots `analysis_status` under the lock, and returns 409 + `needs_force=True` when one or more entries are `status='running'` so the browser can prompt the user before killing in-flight work. Shutdown is `signal.SIGTERM` (NOT `os._exit(0)` / `SIGKILL`) scheduled via a 0.5s `threading.Timer` so the HTTP response flushes BEFORE the process dies AND the existing `atexit` handlers fire — `_shutdown_handler` saves `analysis_status.json` and `_r17_corpus_shutdown` scrubs the in-memory plaintext corpus temp file. `os._exit(0)` would skip both. In TESTING mode (Flask config OR `ADOPTIQ_TESTING=1` env) the endpoint returns `would_shutdown=True` and skips the kill so pytest does not terminate itself. Source-shape pinned by `tests/test_round60_shutdown_endpoint.py` (19 endpoint tests) and `tests/test_round60_quit_button_template.py` (11 template tests).

**Operator-flippable LLM model selection (Round 69 / Build 43):** the active LLM model for both Ask AI (`ask_ai_model_name`) AND report narratives (`report_model_name`) is independently flippable from the analyze-page card, the ask-ai-page card, AND the admin console. Both surfaces use the **same** dual-auth as `/api/corpus/refresh` (CSRF token OR `X-AdoptIQ-Internal`) and a **shared test-before-save UX** implemented in `static/js/r69_model_preferences.js` — the Save button stays disabled until the operator clicks Test and `POST /api/llm/ping` returns `ok=true` for the proposed model name (empty string is also accepted to clear the override and fall back through the precedence chain). Settings precedence (highest first): `settings.json` (`ask_ai_model_name` / `report_model_name`) → per-feature env (`CIRCUIT_MODEL_NAME_ASK_AI` / `CIRCUIT_MODEL_NAME_REPORT`) → general env (`CIRCUIT_MODEL_NAME`) → hardcoded default (`gpt-5-nano`). All resolution goes through `model_resolver.get_active_ask_ai_model()` / `get_active_report_model()` — these helpers do NOT cache so a UI flip takes effect on the very next call (audit-pinned). `generate_llm_response` and `generate_llm_json_response` accept a keyword-only `model_name=...` that overrides the fallback for one call only; threading is asymmetric by design (Ask AI sites use the Ask AI resolver, report sites use the Report resolver) so the two surfaces stay risk-isolated. Validation is allow-list `^[A-Za-z0-9._-]{1,128}$` (`adoptiq_settings._is_valid_model_name`); upstream CircuIT errors flowing through `/api/llm/ping` are sanitised by `_r69_sanitize_llm_error` before echo (Bearer/JWT/api_key/Authorization redacted, message capped at 200 chars). The active model name is stamped onto every Ask AI diagnostic record AND surfaced in `/api/ask-ai-portfolio` responses + the R68 debug chip footer so the operator can prove which model produced a given answer. **Future endpoints that accept a model name MUST validate via `adoptiq_settings.is_valid_model_name(...)` AND require a successful `/api/llm/ping` BEFORE persisting** — this is the contract the test-before-save UX depends on, and silently writing an unprovisioned model name will land an honest 400/500 in production but only a deferred error after the next LLM call (the Round 67 stale-binary trap class of bug). Source-shape pinned by `tests/test_round69_model_preferences.py` (43 tests covering settings/validation/precedence/threading/endpoints/admin proxy/UI source-shape/diag surface/build metadata + the wires-don't-cross regression guard).

## Development Workflow

Branch model: develop on machine-specific branches (`pc-sync-YYYY-MM-DD`, `mac-sync-YYYY-MM-DD`), integrate to `master` only after `make verify` passes and platform build + smoke test complete.

Quality gate (from `.cursor/rules/quality-gate.mdc`): Never hide or skip tests. When fixing bugs, add a regression test. Run the narrowest relevant check first (`pytest tests/test_<module>.py`) before the full suite. Changes must report: files changed, tests added/updated, commands run, pass/fail status.

Audit log convention: when adding a Round-N audit, use `# Round N` markers in the affected source so `git diff <file> | grep 'Round N'` gives a per-file footprint; document phase-by-phase findings, residual risks, and follow-ups in `QUALITY_AUDIT.md`. See Round 14/15/16 sections for the established format.

## Loop conventions (Cursor ↔ Claude Code)

This repo runs a two-tool loop: **Cursor generates code, Claude Code audits and writes a review back**. The loop is bootstrapped in Round 0 (see `QUALITY_AUDIT.md`).

**Where things live:**
- LLM-bible (this file): `CLAUDE.md` — invariants, SSoT modules, critical rules.
- Audit journal: `QUALITY_AUDIT.md` — per-round handoff + review log.
- Verify gate: `make verify` — lint + security + audit + test. **Local contract**; CI (`.github/workflows/build.yml`) runs only `pytest -q`.
- Cursor session-close handoff: appended to `QUALITY_AUDIT.md` under `## Round N — handoff <date>`. Format pinned in `.cursor/rules/session-handoff.mdc`.
- Claude session review: appended under the same Round, in a `## Round N — Claude review` subsection.
- Review standards: `.cursor/rules/quality-gate.mdc` (in-session gate) and `.cursor/BUGBOT.md` (PR-style review checklist) — both apply to Claude as well.

**Commit trailers** (existing convention — keep):
- Cursor commits: `Made-with: Cursor`
- Claude commits: `Made-with: Claude Opus 4.7 (1M context)` (or current model)

**Round-N source markers** (existing convention — keep): when changing code as part of Round N, drop a `# Round N` comment so `git diff <file> | grep 'Round N'` shows the per-file footprint.

**Floor:** every round must hold the Round 0 floor — `make verify` clean with at least the test count recorded in Round 0. A round that lowers the floor is a regression and must explain why in the handoff under **Known deferrals**.
