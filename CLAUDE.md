# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This App Does

AdoptIQ is a renewal-risk and adoption intelligence desktop application (Flask + PyInstaller) that generates Word/Excel reports from Snowflake data, CSOne adoption barriers, support cases, and external service intelligence. It ships as a native macOS `.app` (DMG) and Windows `.exe` with a browser UI at `http://localhost:5151`.

## Commands

```bash
# Run the app
python app_simple.py                        # Main app — http://localhost:5151
python enhanced_admin_dashboard_v2.py       # Admin dashboard — http://127.0.0.1:5152

# Tests
python -m pytest -v                         # Full test suite (current floor: 6251 passed / 4 skipped / 6 deselected after Round 132 / Build102; R131 Build101 floor 6233; R130 Build100 floor 6227; R129 Build98 floor 6197; R128 Build97 floor 6197; R127 Build96 floor 6187; R126 Build95 floor 6179; R125 Build94 floor 6112; R124 Build93 floor 6068; R123 Build92 floor 5998; R122 Build91 floor 5963; R121 Build90 floor 5938; R120 Build89 floor 5924; R119 Build88 floor 5893; R118 Build87 floor 5828; R117 Build86 floor 5805; R116 Build85 floor 5791; R115 Build84 floor 5740; R114 Build83 floor 5723; R113 Build82 floor 5713; R112 Build81 floor 5664; R111 Build80 floor 5642; R110 Build79 floor 5630; R109 Build78 floor 5624; R108 Build77 floor 5606; R105 Build74 floor 5582; R104 Build73 floor 5579; R103.1 Build72 floor 5576; R103 Build71 floor 5575; R102 floor 5570; R101 floor 5569; R100 floor 5554; R99 floor 5552; R98 floor 5550; R97.2 floor 5529; R97 floor 5523; R96.1 floor 5512; R96 floor 5507; R94 Build67 floor 5512; R93 floor 5501; R92 floor 5497; R91 floor 5486; R90 Build66 floor 5477; R89 Build65 / F3 floor 5461; R89 Build65 pre-F3 floor 5451; R88 Build64 floor 5434; R87 Build63 floor 5348; R86 Build62 floor 5311; R85 Build61 floor 5289; R84 Build60 floor 5269; R83 Build59 floor 5190; R82 Build58 floor 5135; R81 Build57 floor 5069; R80 Build56 floor 5047; R79 Build55 floor 5025; R78 Build54 floor 4864; R77 Build53 floor 4828; R76 Build52 floor 4812; R75 floor 4741; Build42 floor 4227; Build41 floor 4051; Build40 floor 3986; pre-Build40 floor 3938; R65 floor 3773; R64 floor 3718; R63 floor 3663; R62 floor 3655; R61 floor 3626; R0 floor 2570)
make eval-ask-ai                            # Ask AI offline eval suite (Round 66 / Build 40, NOT part of make verify; runs 50 questions x 5 portfolios via record/replay mock CircuIT, emits scorecard to tests/ask_ai_eval/scorecards/)
python -m pytest tests/test_canonical_metrics.py -v   # Single test file
python -m pytest -k "ask_ai" -v            # Filter by name
python -m pytest tests/test_round16_*.py -v # Round 16 regression suite (cross-format consistency, sort determinism, AI grounding, polish offset)

# Verification pipeline (run all before committing)
make test       # pytest -q
make lint       # ruff check
make lint-fix   # ruff check --fix (safe fixes only)
make security   # bandit HIGH/MED gate
make audit      # pip-audit on requirements.txt
make verify     # all of the above

# Build
bash build_mac.sh    # macOS DMG → OUTBOX/
build_pc.bat         # Windows EXE → OUTBOX/
```

## Linting & Security Configuration

- **Ruff** (`pyproject.toml`): Python 3.11, line-length 120. Rules: `E`, `F`, `B`, `S`. Cosmetic/style rules (UP, I001, RUF) are intentionally deferred to separate PRs.
- **Bandit** (`bandit.yaml`): HIGH/MED severity gate. Skips documented: B101 (assert in tests), B404/B603/B607 (subprocess build helpers), B311 (non-crypto random), B110/B112 (try-except patterns), B608 (SQL strings). Don't add new bandit skips without documenting the rationale.

## Architecture

### Structure
The project is currently a flat Python module tree with no application package hierarchy. Templates are in `templates/` (Jinja2), static assets in `static/css/` and `static/js/` (vanilla JS). Tests are in `tests/` with `conftest.py` providing `app` and `client` fixtures. See `REPO_STRUCTURE_PLAN.md` before attempting any package-layout or import-path migration.

### Two Flask Apps
| App | Port | File | Access |
|-----|------|------|--------|
| Main UI | 5151 | `app_simple.py` | Loopback (`127.0.0.1`) by default; opt-in public bind via `ADOPTIQ_BIND_PUBLIC=1`, or set `ADOPTIQ_BIND_HOST` directly (Round 14 R14-007) |
| Admin | 5152 | `enhanced_admin_dashboard_v2.py` | Loopback (`127.0.0.1`) by default; opt-in public bind via `ADOPTIQ_ADMIN_BIND_PUBLIC=1` |

### Data Flow
```
User uploads CSOne .xlsx
  → app_simple.py: validate, save, start background thread
  → snowflake_prefetch.py: pull customer/subscription/case data
  → data_source_validator.py: enforce required sources
  → core analysis (adoptiq_backend.py, advanced_renewal_analyzer.py)
  → canonical_metrics.py: single source of truth for cross-report counts
  → formatter (executive_intelligence_formatter.py, compact_report_formatter.py, leader_report_generator.py)
  → /download/<analysis_id>/<file_type>
```

### Key Module Roles
- **`app_simple.py`** (18k lines): Flask orchestration, all 45+ routes, analysis job lifecycle, progress tracking
- **`adoptiq_backend.py`** (11k lines): Core Word + Excel report generation engine
- **`canonical_metrics.py`**: Single source of truth for counts that appear across multiple reports — always use this, never recompute inline
- **`risk_scoring.py`**: Deterministic weighted risk scoring — changes here affect all reports
- **`ask_ai_grounded.py`**: Grounded retrieval pipeline (retrieval-first, then CircuIT LLM)
- **`ai_narrative_validator.py`** (Round 16): Validates LLM-generated narratives against the source briefing — rejects ungrounded numbers, invented entities, and HTML/JS injection. Wired into the report-narrative gate in `app_simple.py`.
- **`report_export_schema.py`** (Round 15): SSoT for Excel column ordering, headers, dtypes used by all report writers
- **`report_export_styling.py`** (Round 15/16): SSoT for Excel polish — Tables, banded rows, conditional formatting (3-color risk scale, severity bands, status pills, days-open data bar). `apply_excel_polish` accepts `startrow` to honor title-row offsets.
- **`report_word_styling.py`** (Round 15): SSoT for Word top-N tables (`add_banded_top_n_table`) — banded rows, header colors, reproducible formatting
- **`config.py`**: Version constants, all env-var loading, technology filters, CSOne column mappings
- **`data_contracts.py`** + **`data_normalization.py`**: Shared consistency helpers — use these before touching DataFrames
- **`structured_logging.py`**: All logging goes through here. Pipeline-boundary entry/exit logs use manager-digest only (no PII).

### Storage
- macOS: `~/Library/Application Support/AdoptIQ/`
- Windows: `%APPDATA%\AdoptIQ\`
- Subdirs: `uploads/`, `outputs/`, `analysis_status.json`, `external_intelligence.db` (SQLite), `admin_monitoring_v2.db` (SQLite), `settings.json` (Round 32, mode `0600`, parent dir `0700`)

### Settings precedence (Round 32 / Phase 2.E, extended Round 33 / Build8, narrowed Round 35)
Persistent feature flags resolve in this order — highest precedence first:
1. `settings.json` (managed by `adoptiq_settings.py`; allow-listed keys only — currently just `corpus_knowledge_enabled`. The Build8 `sharepoint_folder_url` key was retired in Round 35 along with the user-facing URL paste UI; legacy values are silently dropped on load/save).
2. Environment variable (e.g. `CORPUS_KNOWLEDGE_ENABLED=true|false`, `ADOPTIQ_CORPUS_SHARE_URL=https://...`).
3. `config.py` default.

The Intelligence on/off switch in the analyze-page `[data-intel-banner]` card POSTs to `/api/settings/intelligence` (CSRF-protected via the same dual-path as `/api/corpus/refresh`), which writes `settings.json`, mutates `Config.CORPUS_KNOWLEDGE_ENABLED` in-process, and triggers `corpus_bootstrap.request_refresh` on enable. Round 32 / Phase 2.F flipped the `config.py` default to `true` so fresh installs surface Intelligence in the UI immediately.

The Round 36 "AdoptIQ Knowledge Corpus" sub-panel (formerly the Build8 "SharePoint connection" card and the Round 35 "Connect to Microsoft" card; same `[data-intel-banner]` slot) is now informational only — the MSAL device-code routes (`/api/corpus/sharepoint/signin|signout|refresh`) and the URL paste UI were both retired. The runtime trusts that the OneDrive desktop client handled SSO/MFA/admin-consent and verifies the result by `Path.is_dir()` + counting non-empty files at `Config.CSONE_ONEDRIVE_FOLDER`. `Config.ADOPTIQ_CORPUS_SHARE_URL` is preserved for documentation / bake logging; `Config.ADOPTIQ_SHAREPOINT_FOLDER_URL` is a backward-compat alias.

### Native Knowledge Corpus (Round 108 smooth prebaked model)
The "AdoptIQ Knowledge Corpus" panel is native to every install, and Build 77 ships a pre-baked encrypted corpus in the macOS app bundle so Ask AI has corpus data on first launch. **Round 36 retired the MSAL/Graph runtime path entirely**; OneDrive is now optional background refresh context, not a startup prerequisite.

1. **OneDrive sync presence as auth signal** — `corpus_bootstrap._check_onedrive_sync_status()` probes `Config.CSONE_ONEDRIVE_FOLDER` and returns `("synced", N, path)` when the folder exists and contains at least one non-empty file (zero-byte Files-On-Demand placeholders are skipped), `("not_synced", 0, path)` otherwise, `("unknown", 0, None)` when the env is unset. The result is surfaced on `CorpusBootState.onedrive_status` / `.onedrive_file_count` and on `/api/intel/status` under `boot.onedrive_status` / `boot.onedrive_file_count`.
2. **Release-time packaging (prebaked corpus)** — `build_mac_dmg.sh` defaults to `ADOPTIQ_BAKE_CORPUS=1` and runs `scripts/bake_corpus.py` before PyInstaller. With `ADOPTIQ_RELEASE_GATE=1`, the build MUST fail if `bake/corpus.db.enc`, `bake/corpus.db.salt`, or `bake/sentinel.json` are missing, or if `.bake-skipped` is present. `adoptiq_mac.spec` MUST bundle those three files under `Resources/baked_corpus/` and MUST NOT bundle `corpus.sentinel.lock.json`.
3. **Runtime install + daily refresh (local pass)** — `corpus_bootstrap._install_baked_corpus_if_present()` copies the bundled corpus into App Support on first launch, preserves/replaces a broken local corpus as `self_healed_baked`, and opens the corpus with the local bundled/per-user sentinel (`allow_local_sentinel=True`, no OneDrive key prerequisite). `start_daily_refresh_worker()` remains available for optional refreshes from generated AdoptIQ reports, Intelligence uploads, and OneDrive files when present.

Round 35 also added a WAL checkpoint inside `commit_to_disk` (`PRAGMA wal_checkpoint(TRUNCATE);` before reading the plaintext file). Without it, the encrypted DB would only contain the 4096-byte SQLite header because committed pages live in the WAL until checkpointed.

Round 108 keeps runtime updates automatic and visible: daily refresh indexes generated reports, Intelligence uploads, and OneDrive files when present without requiring OneDrive to be synced; report sidecar writes and corpus-source setting saves request a best-effort incremental refresh. Runtime refresh calls `ask_ai_vector_store.upsert_chunk_vectors(..., strict=False)` to backfill dense vectors. Soft dense failures roll back partial vector writes, keep lexical corpus rows available, set `dense_retrieval_status=stale_or_lexical`, and force lexical retrieval; a later successful dense update restores hybrid retrieval unless the operator explicitly set `ASK_AI_RETRIEVAL_METHOD=lexical`.

The panel renderer (`static/js/intel_status.js` / `classifyCorpusPanel`) renders runtime states based on `boot.source` × `onedrive_status` × `in_progress` × `last_refresh_error`: active local corpus, optional OneDrive refresh states, indexing, refresh failed, and unknown. `baked` / `self_healed_baked` source labels map to the active local corpus state. The admin Intelligence tile mirrors source stats, `last_successful_update_at`, embedder status, dense-vector counts/errors, and the active Ask AI retrieval method.

Round 108 supersedes the Round 96.1 admin gate: after `commit_to_disk()` succeeds in `corpus_bootstrap._run_index_pass`, `_STATE.source` is normalized back to `"fresh"` so stale `blocked_no_onedrive` / `signed_in_no_corpus` labels cannot keep the UI in a blocked state, and `enhanced_admin_dashboard_v2._r54_corpus_is_blocked_no_onedrive()` intentionally returns `False`. Admin refresh/reset must proxy through even when old status snapshots carry those labels.

### Corpus encryption sentinel resolution (Round 33 / Build8 → Round 96)
`corpus_crypto.open_corpus_for_user` accepts an `onedrive_root` argument; the Round 33 `sharepoint_root` parameter was retired in Round 36 along with the MSAL/Graph cache it populated. The helper still supports this general resolution order:
1. OneDrive sentinel (`<onedrive_root>/.adoptiq_corpus_sentinel.json`) — written by the OneDrive desktop client when the canonical Cisco-managed sentinel is present in the synced folder.
2. Auto-minted local sentinel (`<encrypted_path>.parent/sentinel.json`, mode `0o600`, parent `0o700`) — available only to callers that explicitly pass `allow_local_sentinel=True`.

Round 107 / Build 76 intentionally bundled the bake-local `sentinel.json` with the encrypted corpus so the packaged app can decrypt the prebaked snapshot without OneDrive. Round 108 keeps that product trade-off for first-launch readiness; new corpus UI copy MUST describe OneDrive as optional refresh context, not as a prerequisite.

### Admin Console auto-start (Round 32 / Phase 2.D, hardened in Round 37)
`app_simple._start_admin_server_in_thread()` spawns `enhanced_admin_dashboard_v2.admin_app` on a daemon thread (default `127.0.0.1:5152`) so the packaged `.app` actually exposes the admin UI without a second process. Idempotent (`sys._adoptiq_admin_started` guard), short-circuits under pytest, and quietly downgrades on `OSError` (port already bound — assume an external admin instance owns it). The header link in `templates/base.html` opens it in a new tab with `rel="noopener noreferrer"`.

**Round 37 / Phase 1: live-port handoff.** Before importing the admin module, `_start_admin_server_in_thread()` writes `os.environ['ADOPTIQ_MAIN_URL'] = f"http://127.0.0.1:{_resolve_main_port()}"`. The admin's `_main_app_host_port()` re-reads `os.environ.get('ADOPTIQ_MAIN_URL')` per call (defense in depth — the captured module-level constant could otherwise go stale). Pre-Round-37 the admin defaulted to `localhost:5151`, so when the packaged `.app` ran the main UI on 15152 the admin's Server Status tile probed the wrong port, decided `running=False`, and rendered the "Start Server" button while the server was right there. The legacy `server_status['port'] = 5000` sentinel was also flipped to `None` so the tile renders "N/A" before the first probe instead of falsely advertising port 5000.

**Round 37 / Phase 2-4: Admin Intelligence tile.** The admin "AdoptIQ Intelligence" card now (a) renders `boot.onedrive_status` / `boot.onedrive_file_count` from Round 36 as a colored pill (green `synced`, yellow `not synced`, gray `unknown`) so the operator can see at a glance whether daily refresh is unblocked; (b) drops the dead `corpus_status.boot.sharepoint` sub-block (Round 36 nulled that key) and the `/sharepoint_signin` + `/sharepoint_refresh` proxy routes (their main-app upstreams were deleted in Round 36); (c) renames the "Run incremental" button to "Re-index now" (matches the analyze-page panel labelling) and disables both `/corpus_refresh` buttons while `boot.in_progress` is true so the operator cannot stack refresh requests on an active index pass.

### Leader report two-pass CSOne validation (Round 38)
The leader-report background worker (`app_simple.run_leader_report_generation`) used to call `raise_validation_error_if_invalid` BEFORE loading the CSOne file, which combined with Round 14's "ruff-clean" `locals().get('csone_file')` check turned every OneDrive autodiscovery hit (`get_latest_csone_from_folder()`) into a hard `csone missing or empty` abort -- the validator saw `csone_file_provided=True` paired with an empty placeholder DataFrame and tripped the required-but-empty branch in `data_source_validator.py:248-249`. Round 38 fixes the load-order itself with a two-pass design and an endpoint-level provenance split:

1. **Endpoint provenance split** — `start_leader_report` (`/start_leader_report`) now tracks `csone_file_explicit` (set only when `request.files['csone_file']` was supplied) separately from `csone_file_autopicked` (set only when `get_latest_csone_from_folder()` returned a path). The status dict persists `csone_file_was_uploaded: bool(csone_file_explicit)` and `csone_file_path: <resolved-effective>` alongside the back-compat `csone_file` key. The worker reads `status['csone_file_was_uploaded']` to decide whether to fail loud at Pass 2.
2. **Pass 1 (early)** validates ONLY `snowflake` + `team_subscriptions` against an empty placeholder; hard-codes `csone_file_provided=False` so the Round 14 `locals().get(...)` heuristic regression cannot recur.
3. **CSOne load relocated up** to immediately after Pass 1 (`_resolve_csone_path_safe` → `load_csone_excel` → `_prepare_csone` → `_apply_scope_filter_csone`).
4. **Pass 2 (after load)** is gated by `_csone_was_uploaded`. When True, validates `csone` against the real loaded `csone_df` (empty after scoping is fail-loud — preserves the Round 2 / Phase 4.3 contract for explicit uploads). When False (autodiscovery hit) and the loaded frame is empty, logs a warning naming the autodiscovered file and appends a `partial_data_warnings` entry tagged `kind='autodiscovered_empty_after_scope'` so the Word/Excel banner is honest, but does NOT abort.

The Round 30 / M4 non-raising re-validation block sits between Pass 1 and the CSOne load and now sees the real loaded `csone_df` for the optional-fetch-error promotion. The fix is intentionally scoped to the leader path only — `run_compact_analysis`, `run_comprehensive_analysis`, `run_customer_renewal_analysis` already load CSOne BEFORE validating, and `run_subscription_analysis` excludes `csone` from `required_sources=[...]`, so the same load-order bug is not reachable on those paths (audit-pinned by `tests/test_round38_other_report_paths_unchanged.py`).

### Duplicate-launch UX (Round 38.1 / Build13)
The .app on macOS is a Flask web server with no native window — when the user double-clicks the icon a second time while the first instance is still on `127.0.0.1:5151`, `app_simple.py`'s "port in use" branch used to spawn an `osascript` `display dialog` *behind* other windows (no `activate`) and then `sys.exit(0)` after the 15s `osascript` timeout. The user perceived this as the .app "bouncing for a few minutes then stopping." Round 38.1 adds a tiny HTTP probe (`_probe_existing_adoptiq(port)`) that GETs `http://127.0.0.1:<port>/` and matches an "adoptiq" body marker; on a True result the duplicate-launch path now `webbrowser.open`s to the existing instance and exits cleanly, no dialog. The `osascript` invocation also gained a leading `-e 'tell application "System Events" to activate'` arg so when the dialog IS shown (rare non-AdoptIQ port collision) it actually surfaces to the front. The fix is scoped to the duplicate-launch branch only — boot order, corpus install, Snowflake prefetch, and the Round 38 leader-report code paths are unchanged. Source-shape pinned by `tests/test_round38_1_duplicate_launch_routes_to_existing.py`.

### Latent downstream bugs unblocked by Round 38 (Round 38.2 / Build14)
Round 38's two-pass validator design has a subtle side-effect: report-generation paths that USED to abort at validation when CSOne data was empty/missing now proceed all the way through Document Generation. This unblocks any latent bug in the formatter / writer code that depended on validation as an implicit guard. Round 38.2 fixes the first such bug — `KeyError: '_bu_disp'` in `leader_report_generator._compute_customer_health` (line 3457 pre-fix), where the transient `_bu_disp` column was assigned inside an `if not stalled.empty:` guard but accessed unconditionally on the next line. Trigger condition: any CSSM whose open APs were all <=30 days old (no stalled rows). The fix moves the for-loop INSIDE the guard. A full audit of `leader_report_generator.py`'s 29 `if not df.empty:` sites found 28 already CLEAN and only the one `_bu_disp` site needing fix; the AB block immediately below uses `_age_days` and is the reference-correct example. **When touching any new formatter or writer that consumes possibly-empty AP/AB/CP/TAC/SP DataFrames, follow the same template: outer column-existence guard + inner status/date guard + per-row filter + an `if not <subset>.empty:` block that wraps BOTH the transient column assignment AND every consumer.** Source-shape pinned by `tests/test_round38_2_compute_customer_health_no_stalled.py`. Other large formatters (`compact_report_formatter.py`, `executive_intelligence_formatter.py`, etc.) may have analogous bugs; Round 38.2's audit was scoped to `leader_report_generator.py` only and a follow-on formatter-audit round is tracked as a deferral.

### Closing the Build 38 acceptance gaps + R27 grounding root-cause (Round 66 / Build 39)
Build 38 manual acceptance left six gaps: (B1) `report_source_injector._rewrite_paragraph_with_inline_citations` rendered single-line multi-KPI tiles like `"Analysis Period: 90 [Source: ...] Days  Total Customers: 39 [Source: ...]"` — splitting the value from its `Days` unit; (B6) the Comprehensive XLSX advertised a portfolio-level risk-band tile but had no per-customer component breakdown so an operator reviewing the workbook had no row-level signal as to *why* a given customer was CRITICAL vs HIGH vs MEDIUM; (B7) Renewal Word narratives missed citations on KPIs the canonical resolver had no aliases for (`support cases 90 days`, `renewal risk level`, `success_priorities`, `incidents`); (B8) Compact composite scored ~30% lower than Renewal for the same customer because `compact_report_formatter.calculate_renewal_risk_scores` passed `pd.DataFrame()` for pulse / action plans / subscriptions even when the data was available, dampening those three components to 0.0 in `compute_customer_risk_profile`; (B9) PSIRT_Vulnerabilities XLSX leaked rows with `Customer=NaN` / `Customer=Unknown` from malformed `vulnerability_by_customer` entries; (B10) `fetch_support_cases_snowflake` returned an empty DataFrame with only a `logger.info` signal when SUPPORT_CASES had 0 rows for a manager+technology scope, indistinguishable from a genuine zero. Plus the long-standing 34% R27 grounding rejection rate from Build 37 (R65/C-3 captured the data, R66 acts on it). Round 66 closes all seven in three shippable passes (B1-B14):

- **Pass 1 / B1 (citation injection)**: New helpers `_logical_label_start_pos` (compensates for the regex's greedy unit absorption into the next match's label group via wide-gap detection in the label text) and `_value_end_no_trailing_ws` (rewinds past the `\\s*%?` regex tail). The multi-match path now classifies each pair-boundary as unit-style (any `[A-Za-z]` in the boundary segment → defer citation past the unit, before the next label) or sentence-style (pure punctuation → R57 behavior preserved, citation immediately after the value). Pinned by `tests/test_round66_p0_citation_inline_units.py` (9 tests).
- **Pass 1 / B2 (AP scope filter defense-in-depth)**: `leader_report_generator.fetch_action_plans_snowflake` accepts optional `technology_filter` + `customer_names` kwargs. When `technology_filter` is provided AND not `"All" / "All Technologies"`, the returned DataFrame is passed through `adoptiq_backend._filter_csconsole_data_by_technology` as a defense-in-depth layer. The Leader path remains unaffected (`technology_filter=None`). Pinned by `tests/test_round66_p0_ap_scope_filter.py` (7 tests).
- **Pass 1 / B3 (legacy AP provenance heuristic)**: `canonical_metrics.count_open_action_plans` now recognises the legacy `AdoptIQ_Status='EMPTY'` heuristic (case-insensitive, whitespace-tolerant) when the R65/C-2 `_adoptiq_provenance_row` marker column is absent. Old XLSX files that pre-date R65 now correctly count 0 open action plans instead of mis-counting the provenance row. Pinned by `tests/test_round66_p0_count_open_aps_legacy_provenance.py` (8 tests).
- **Pass 1 / B4 (HTML strip widening)**: `data_normalization` gained `_strip_html_safe` (BeautifulSoup primary path with `html.parser` for malformed-markup resilience, regex fallback when `bs4` import fails) and `_HTML_DANGEROUS_BLOCK_RE` (full `<script>` / `<style>` block decompose — tag + body + closing tag — before the generic `_HTML_TAG_RE` pass). The XLSX writer's allow-list (`_R66_HTML_STRIP_SHEETS`) widened from the R25 baseline (`AB_Detail_All`, `CSConsole_Customer_Pulse`) to also cover `Action_Plans`, `CSConsole_Action_Plans`, `CSConsole_Adoption_Barriers`, `CSConsole_Success_Priorities`, `Adoption_Barriers`, `Customer_Pulse`, `Success_Priorities`. `app_simple.py` also runs `strip_html_from_dataframe` on the four filtered CSConsole frames immediately after technology-scoping so Word formatters AND AI prompts see clean text upstream. Pinned by `tests/test_round66_p0_html_strip.py` (15 tests).
- **Pass 2 / B5 (`Sheet_Title:` parity for Comprehensive)**: `write_excel_workbook` dynamically emits one `Sheet_Title:<sheet>` row in `Report_Info` for every data sheet being written, plus structured `Export type` / `Generated at (UTC)` / `Manager` / `Technology` / `Days` rows. Restores the R65/R-1 contract for the comprehensive XLSX writer (which pre-R66 only emitted titles for the leader and renewal exports). Source-shape pinned indirectly via Pass 2's writer tests.
- **Pass 2 / B6 (per-customer Risk_Components)**: Comprehensive XLSX gains a dedicated `Risk_Components` sheet listing seven canonical component scores per customer (`Adoption_Barriers_Score`, `Support_Cases_Score`, `Customer_Pulse_Score`, `Action_Plans_Score`, `Incidents_Score`, `Contract_Score`, `Engagement_Score`) plus `Top_Risk_Factor`. Sorted `Risk_Score_0_100 DESC, Customer_Name ASC` per the SSoT determinism rule. Provenance-row fallback (`AdoptIQ_Status=EMPTY`) when no profiles were computed. Wrapped in try/except so a construction failure never blocks the report. Pinned by `tests/test_round66_p1_risk_components_per_customer.py` (12 tests).
- **Pass 2 / B7 (Renewal citation enrichment)**: `KPI_ALIASES` in `report_iteration_loop.py` extended with renewal-specific labels so the canonical resolver picks them up: `support cases 90 days` / `support cases last days` / `total support cases 90 days` (folded into `support_cases`), `renewal risk level` / `risk level` (folded into `risk_category`), and brand-new canonical `success_priorities` and `incidents` aliases. Pinned by `tests/test_round66_p1_renewal_citation_enrichment.py` (11 tests).
- **Pass 2 / B8 (Compact risk dampening)**: New `_r66_b8_classify_extra_frames` helper inspects each `extra_frames` entry's column markers and returns `(pulse_df, action_plans_df, subs_df)` triples. The per-customer loop in `calculate_renewal_risk_scores` then slices each by customer name (using `customer_name` / `BU_NAME` / `CUSTOMER_NAME` / `RELATED_CUSTOMER__C` candidates) and threads the slices into `compute_customer_risk_profile`. Pre-R66 the Compact path passed `pd.DataFrame()` for those three components, dampening the composite ~30% relative to Renewal for the same customer. Conservative classification: ambiguous frames stay `None` so legacy callers keep the pre-R66 math. Pinned by `tests/test_round66_p1_compact_risk_threading.py` (13 tests).
- **Pass 2 / B9 (PSIRT NaN customer leak)**: PSIRT_Vulnerabilities XLSX writer now drops blank / NaN / Unknown / N/A customer rows AND labels portfolio-wide CVE / PSIRT advisories with explicit `Customer = '(Portfolio-wide)'` so a downstream reader cannot misread an empty Customer field as "Unknown customer". Pinned by `tests/test_round66_p1_psirt_no_unknown_customer.py` (9 tests).
- **Pass 2 / B10 (SUPPORT_CASES empty signal)**: `fetch_support_cases_snowflake` promotes the empty-result log to `logger.warning` AND attaches `fetch_error_kind='empty_for_scope'` + `fetch_error` (full message) + `scope_account_count` + `scope_window_days` on the returned DataFrame's `attrs` dict. The renewal narrative gate, the `partial_data_warnings` surface in `analysis_status`, and the Ask AI grounding briefing can now distinguish "0 cases for the requested manager+technology window" from "0 cases happened in this window" without parsing log output. Pinned by `tests/test_round66_p1_support_cases_empty_scope_warning.py` (10 tests).
- **Pass 3 / B11 (R27 grounding root-cause fix)**: Three changes to `ai_narrative_validator`. **(a)** `_COMMON_REFERENCE_NUMBERS` widened from `range(0, 32)` to `range(0, 101)`, plus every multiple of 5 from 100 to 500, plus calendar years 2024-2030 (covers the natural rendering of small-integer counts and percentages plus forward-looking renewal horizons). **(b)** New `_is_derived_ratio_percentage(value, integer_briefing_numbers, ppt_tolerance=0.5)` helper grounds any narrative percentage in `[0, 100]` that's expressible as `(a / b) * 100` for integer pair `(a, b)` in the briefing — so when the briefing names "Open ABs: 11" and "Total customers: 28" and the LLM derives "11 of 28 customers (39.3%)", the 39.3 is grounded via `11/28*100 = 39.286` within 0.5 ppt. The integer pool is sorted ASC and capped at 200 to bound the O(n²) check. **(c)** `_number_in_allowed` widens its relative tolerance for ARR-class values (>= $100K) from the caller's default 1% to a floor of 5% so `"$1.2M"` against briefing `"$1,234,567"` (2.8% drift) passes while invented `"$5M"` against briefing `"$1.2M"` (50% drift) still fails. Targets post-fix R27 rejection rate <=10% (down from 34% in Build 37 / Build 38). Pinned by `tests/test_round66_p2_grounding_root_cause.py` (22 tests). Three existing AI validator tests had to update their negative-control numbers (`test_round16_ai_narrative_validator.py`, `test_round18_ai_insights.py`) — the source contract was preserved, only the chosen test fixtures changed to numbers still outside the widened common set.
- **Pass 3 / B12 (KPI-specific recommendation templates)**: `report_utils` gained six KPI-specific recommendation helper functions: `kpi_recommendation_csm_engagement(open_ab_count, top_categories, cssm_name)`, `kpi_recommendation_training(completion_rate, total_users)`, `kpi_recommendation_engagement_cadence(engagement_score, days_since_last_touch)`, `kpi_recommendation_high_severity_barriers(severity_breakdown)`, `kpi_recommendation_premium_support(arr_amount, premium_eligible_count)`, `kpi_recommendation_upsell(arr_amount, feature_gap_count)`, plus `_r66_b12_safe_int` and `_r66_b12_format_categories` internals. Each template embeds the triggering KPI value directly into the recommendation string ("Schedule a weekly cadence with the assigned CSM focused on the 7 open adoption barriers (categories: Onboarding, Training, Adoption)"). `advanced_renewal_analyzer._generate_renewal_recommendations` lazy-imports them with a graceful fallback to the legacy generic strings if the module is unavailable (so a broken dev tree cannot block report generation). Currently wired into the four highest-leverage branches (CSM engagement, training, engagement cadence, high-severity barriers); Premium support / upsell branches still emit the legacy strings as a deliberate Round 67 follow-on. Pinned by `tests/test_round66_p2_kpi_recommendation_templates.py` (25 tests).
- **Pass 3 / B13 (formatter audit sweep)**: Audited 7 sites in `compact_report_formatter.py` and 1 in `executive_intelligence_formatter.py` for the R38.2 outer-guard / inner-access pattern (`if not df.empty:` block assigns transient column to a DataFrame, then code outside the guard accesses that transient column). All 8 sites are CLEAN — each site either initializes transient values BEFORE the empty-check guard, or uses transient values ONLY inside the guard. No code changes needed. Audit pin in `tests/test_round66_p2_b13_formatter_audit_sweep.py` (12 tests, including `_bu_disp` absence assertions to prevent future regressions of the R38.2 anti-pattern).
- **Pass 3 / B14 (admin grounding pill)**: `app_simple.py /api/status/all` projects three new scalars per analysis (`grounding_rejection_rate`, `grounding_rejection_count`, `grounding_total_count`) from the R64/B5 `grounding_diagnostics.rejection_summary` rollup. Defensive type checks (`isinstance(_r66_diag, dict)`, `try/except (TypeError, ValueError)`) prevent malformed diagnostics from crashing the bulk endpoint. The admin "Currently Running Reports" tile gains a `Grounding` column with a three-color pill (green <=10%, yellow <=25%, red >25%) + tooltip showing `Rejected N of M`. Defaults to "N/A" until the first narrative call is recorded. The full structured rejection_records list is still served on demand by the existing R65/C-3 `/api/grounding-diagnostics/<analysis_id>` endpoint. Pinned by `tests/test_round66_p2_admin_grounding_pill.py` (12 tests).

Net pytest delta +165 (3773 → 3938): 9 + 7 + 8 + 15 (P0) + 12 + 11 + 13 + 9 + 10 (P1) + 22 + 25 + 12 + 12 (P2). All 4 `make verify` gates green: ruff clean, bandit 0 HIGH/MED, pip-audit clean. In-locals floor (40) preserved.

### Ask AI accuracy floor: eval framework + hybrid retrieval (Round 66.2 / Build 40)
Build 39 closed the report-side bugs and the R27 grounding rate but left the user-facing Ask AI experience without a measurable scorecard or a non-lexical retrieval layer — the original Round 66/67 multi-pass plan called this out as Pass 4 + Pass 5. Round 66.2 ships both in one cut.

- **Pass 4 (Ask AI offline eval framework)**: a record/replay harness for the grounded Ask AI pipeline so we can measure scorecard regressions per build. New `tests/ask_ai_eval/` tree with 5 portfolio fixtures (`p01_high_renewal_risk` / `p02_heavy_psirt` / `p03_adoption_barriers` / `p04_quiet_portfolio` / `p05_cross_compare` — each with `ab.csv` / `csone.csv` / `snowflake.csv` / `pulse.csv`), 50 questions across 8 categories (`kpi_extraction`, `customer_lookup`, `cross_compare`, `psirt_exposure`, `negative_control`, `multi_step`, `citation_correctness`, `time_bounded`), 50 synthetic CircuIT cassettes (one per question, keyed by SHA256 prompt hash for drift detection), and a per-question predicate evaluator (`must_contain_phrase` / `must_cite_source_id` / `must_not_render_pii` / `must_render_number_within_tolerance`). `MockCircuitClient` defaults to `replay` mode so CI runs offline; live recording is a one-time operator step (`MOCK_CIRCUIT_MODE=record python -m tests.ask_ai_eval.runner`). New `make eval-ask-ai` Makefile target — deliberately NOT part of `make verify` so CI stays fast — and a new `eval` pytest marker excluded from default `pytest -q` runs via `pytest.ini addopts`. Baseline scorecard committed at `tests/ask_ai_eval/scorecards/baseline.md` (synthetic ceiling: 100% pass — the >=15% uplift gate from the original plan applies to LIVE CircuIT recordings, not the synthetic suite). Source-shape pinned by `tests/test_round66_p4_eval_framework_shape.py` (12 tests). The framework wires to `compose_grounded_answer` via a documented "ASK AI EVAL SEAM" comment at the function header — new code paths that bypass the seam will show up in the runner's "did the answer flow through the right composer?" check.
- **Pass 5 (hybrid retrieval — BM25 + dense + RRF)**: dense embeddings are now the primary signal in the Ask AI ranker. New `ask_ai_embeddings.py` module wraps a singleton `fastembed.TextEmbedding` configured with `BAAI/bge-small-en-v1.5` (384-dim, INT8-quantized ONNX, ~33 MB) — TLS via `truststore.inject_into_ssl()` so corporate-proxy environments work, lazy `get_embedder()` that returns `None` (and logs a structured warning) on any failure mode (fastembed missing, ONNX runtime missing, model file missing). `rank_evidence` in `ask_ai_grounded.py` now dispatches to either `_lexical_rank_evidence` (BM25-style, the pre-R66.2 path) or `_hybrid_rank_evidence` based on `Config.ASK_AI_RETRIEVAL_METHOD` (default `"hybrid"`, env-overridable to `"lexical"` for ops degradation). `_hybrid_rank_evidence` computes a lexical rank AND a dense rank for each evidence record, then fuses them via Reciprocal Rank Fusion (Cormack 2009 with `k_rrf=60` via `Config.ASK_AI_RRF_K`). On any embedder failure mid-run, the path returns the lexical ranking and stamps a `retrieval_diag.method = "lexical"` so the operator can see the degradation in the per-query diag. `EvidenceRecord` extended with three optional fields (`bm25_rank`, `dense_rank`, `rrf_score`) — backward-compat preserved via `dataclasses.replace`. `compute_retrieval_diag(records, question, domains, top_k=10)` builds the JSON-serialisable diagnostic block returned with every Ask AI success.
- **Vector storage (refinement vs original plan)**: original plan called for `corpus_vectors.npy` as a separate encrypted artifact. **Refined approach**: store vectors as a `chunk_vectors(chunk_id INTEGER PRIMARY KEY, model_id TEXT, model_dim INTEGER, vector BLOB)` SQLite table INSIDE the existing AES-GCM-encrypted corpus DB. Reasons: (1) the existing `corpus_crypto.EncryptedCorpusHandle` + WAL checkpoint contract already handles SQLite — adding a parallel `.npy` encryption channel is new attack surface AND a new bake-time failure point; (2) lookups by chunk_id are a single SQLite indexed read; (3) crash-safe — no new commit-order coupling between two encrypted artifacts; (4) `chunk_vectors.chunk_id` is FK→`playbook_chunks.id ON DELETE CASCADE` so a re-bake auto-evicts orphaned vectors. `knowledge_schema.SCHEMA_VERSION` bumped 1 → 2 so existing corpus DBs trigger a rebuild via the indexer's `needs_rebuild` check; the Round 39 self-heal contract carries the rebuild safely.
- **Bake-time vector computation**: `scripts/bake_corpus.py._index_into_encrypted_corpus` calls a new `_bake_chunk_vectors(conn) -> tuple[int, str, int]` helper after `index_folder` completes and BEFORE `handle.commit_to_disk()`. The helper lazy-loads `ask_ai_embeddings.get_embedder()`, embeds chunk texts in batches of 64, encodes the float32 vectors via `ask_ai_embeddings.encode_vector()`, and `INSERT OR REPLACE`s into `chunk_vectors`. **Raises `RuntimeError` if the embedder is unavailable on the bake host** — bake-time hard-fail is intentional so the operator knows the bundled DMG won't have dense vectors. The Round 39 decrypt round-trip self-test was extended to verify the `chunk_vectors` table is present AND a sample vector decodes to the expected dimension (catches the case where the table exists but `encode_vector` malformed the BLOB).
- **Runtime embedder warmup**: `corpus_bootstrap.start_background()` now also spawns `_warm_embedder_in_background()` on a daemon thread (parallel to the corpus thread) so the embedder loads while the corpus is decrypting — the FIRST Ask AI query never pays the 1-3s ONNX cold-start. `CorpusBootState` extended with `embedder_status` (`"loading"` / `"ready"` / `"unavailable"`) and `embedder_load_error` (the exception message) so the diagnostic surface can show the exact reason for graceful degradation. On any failure, the warmup forces `Config.ASK_AI_RETRIEVAL_METHOD = "lexical"` for the process so subsequent queries skip the dense path entirely.
- **Per-query diagnostics ring buffer + endpoint**: `app_simple.py` carries a 100-entry FIFO `_ASK_AI_DIAG_BUFFER` (`collections.OrderedDict`, lock-protected). The `ask_ai_portfolio` route mints a `secrets.token_urlsafe(12)` query ID per successful call, persists the `retrieval_diag` block, and returns `query_id` + `retrieval_method` to the client. New admin endpoint `GET /api/ask-ai/diagnostics/<query_id>` returns the full per-query diag (method, top ranks per scoring path, model id, top-K record summaries with their per-method ranks). 404 for unknown IDs (eviction OR never recorded), 200 for known. Loopback-only by default (the main app binds to 127.0.0.1 unless `ADOPTIQ_BIND_PUBLIC=1`); no PII in the payload. Source-shape pinned by `tests/test_round66_p5_hybrid_retrieval_shape.py` (24 tests) including ring-buffer FIFO eviction (the 101st entry evicts the 1st), encoding round-trip, RRF Cormack-2009 math, `ASK_AI_RETRIEVAL_METHOD == "lexical"` short-circuit (asserted by mocking `_hybrid_rank_evidence` to raise — must NOT be called in lexical mode), graceful degradation when `get_embedder()` returns `None`, `_bake_chunk_vectors` raise-on-missing-embedder + write-on-available-embedder.
- **DMG / build-pipeline impact**: `requirements.txt` adds `fastembed>=0.8.0` (pip-audit clean). `adoptiq_mac.spec` + `adoptiq_pc.spec` bundle `Resources/embeddings/` (the bake operator's downloaded model cache) and pin `fastembed`, `fastembed.text`, `fastembed.text.text_embedding`, `onnxruntime`, `tokenizers`, `truststore`, `ask_ai_embeddings` as `hidden_imports`. Build 40 DMG: `OUTBOX/AdoptIQ-v1.0.4-build40.dmg` (~308 MB on the bake host that produced this build — the DMG size depends on whether the operator's bake machine had the `fastembed` model cached locally; a fresh bake host can produce a ~437 MB DMG with the full bundled model).

Net pytest delta +48 from the Build 39 floor of 3938 → Build 40 floor of 3986: 12 from `tests/test_round66_p4_eval_framework_shape.py` + 24 from `tests/test_round66_p5_hybrid_retrieval_shape.py` + 12 surface tests across the diagnostics endpoint / ring buffer / `EvidenceRecord` extensions. The 50-question Ask AI eval suite is excluded from `make verify` via the `eval` pytest marker (runs only via `make eval-ask-ai`). All 4 `make verify` gates green: ruff clean, bandit 0 HIGH/MED, pip-audit clean (including the new `fastembed>=0.8.0` dependency). In-locals floor (40) preserved.

### 100% Accuracy Sweep on Build 40 reports (Round 67 / Build41)
Build 40 manual acceptance audit surfaced 8 confirmed bugs across the Comprehensive / Compact / Renewal / Leader pair. **B1 (Compact vs Renewal cross-format scoring divergence)**: pre-R67 Compact published `0-10` scale + `ext_incidents=None` while Renewal published `0-100` scale + `ext_incidents=filtered`, so WORLD BANK GROUP US scored `4.9` in Compact and `50.0` in Renewal for the same scope. Two-pronged fix: thread `ext_incidents` into `compact_report_formatter.calculate_renewal_risk_scores` via `_r65_filter_customer_tagged_incidents` (parity with Renewal) AND align Renewal `Renewal_Summary.Overall_Risk_Score` to the 0-10 scale + `Risk_Level=MODERATE` vocabulary, preserving `Risk_Score_0_100` as a back-compat secondary column and `Risk_Band` (CRITICAL/HIGH/MEDIUM/LOW/HEALTHY) for existing band-based color lookups. Both XLSX and Word narrative paths updated. Pinned by `tests/test_round67_compact_renewal_score_parity.py` (6 tests) + `tests/test_round67_renewal_score_scale_and_band.py` (8 tests). **B2 (Comprehensive XLSX missing Risk_Components sheet)**: pre-R67 a broad `try/except` wrapped both the row-construction loop AND the `all_sheets[...]` assignment, so any exception in the loop silently dropped the entire sheet. Fix hoists the `all_sheets["Risk_Components"]` assignment OUT of the broad try/except — the sheet is now ALWAYS in `all_sheets` (constructed DataFrame on success, single `_adoptiq_provenance_row=True` fallback row citing the construction error on failure). Inner `try/except` is scoped to row construction only; both `logger.warning` (operator-visible) AND `logger.debug` (R66 contract preserved for audit-friendly grepping) are emitted on the failure path. Pinned by `tests/test_round67_comprehensive_risk_components_always_present.py` (7 tests). **B3 (Comprehensive XLSX Report_Info truncation)**: pre-R67 production showed only 2 rows (`Export type` + `Generated at (UTC)`) despite the R66/B5 contract requiring Manager/Tech/Days + per-sheet `Sheet_Title:<sheet>` rows. Fix adds explicit structured `logger.info` to `adoptiq_backend.write_excel_workbook` immediately after the `Report_Info` write call (citing row count, sheet names, manager/tech/days) AND a parallel log in `app_simple.run_comprehensive_analysis` immediately before the writer call (citing `len(all_sheets)` + `sorted(all_sheets.keys())`). Without this, the only signal that `Report_Info` shipped truncated was opening the produced XLSX in Excel by hand. **B4 (CSOne_Detail_All HTML leakage)**: pre-R67 the sheet leaked raw `<br />` / `<agent name>` / `&#34;` markup from Snowflake-sourced rich-text fields. Fix adds `"CSOne_Detail_All"` to `_R66_HTML_STRIP_SHEETS` so the same `_strip_html_safe` (BeautifulSoup primary, regex fallback) the other AB / AP / Pulse sheets already use also processes CSOne pre-write. Pinned by `tests/test_round67_csone_detail_html_strip.py` (4 tests). **B5 (Compact Report_Info schema drift)**: pre-R67 Compact emitted `Status, Warning, Generated_At` columns instead of the canonical `Item, Value` schema used by Renewal / Leader / Comprehensive. Fix refactors the Compact writer to align — sheet titles are keyed `Item="Sheet_Title:<sheet>", Value=<title>`, partial-data warnings keyed `Item="Partial_Data_Warning"`, truncation events keyed `Item="Excel_Truncation"`. Baseline rows (`Export type`, `Generated at (UTC)`) always present. Pinned by `tests/test_round67_compact_xlsx_report_info_schema.py` (7 tests). **B6 (Compact Risk_Summary column-name parity)**: pre-R67 Compact used `Risk_Summary.Risk_Score` while Renewal used `Renewal_Summary.Overall_Risk_Score`. Fix renames Compact to `Overall_Risk_Score` for parity, retains `Risk_Score` as a back-compat alias column, plus MEDIUM → MODERATE remap for `Risk_Level` vocabulary parity. **B7 (Compact raw Snowflake dumps)**: pre-R67 `Critical_Adoption_Barriers` (259 cols) and `Action_Plans` (243 cols) were raw Snowflake exports with internal SF / ETL plumbing (IS_DELETED, MAY_EDIT, MUTE__C, audit fields). Fix adds both sheets to `report_export_schema.CURATED_COLUMNS` — `Critical_Adoption_Barriers` → `_CURATED_AB_DETAIL_ALL`, `Action_Plans` → new `_CURATED_ACTION_PLANS` tuple (~30 customer-facing columns). Provenance markers (`_adoptiq_provenance_row`, `AdoptIQ_*`) are intentionally NOT in the curated set — they live on the global `INTERNAL_COLUMN_DENYLIST` and are written BEFORE the curated projection runs, so the projection drops them; the empty-state surfaces through the dedicated `Report_Info` sheet instead. Pinned by `tests/test_round67_compact_xlsx_schema_no_raw_snowflake_dump.py` (6 tests). **B8 (R27 grounding rejection rate 21.1% on Build 40)**: the dominant failure mode was single-decimal percentages like `28.6%`, `41.6%`, `99.9%` that the LLM derives from briefing pairs the strict R66/B11 ratio check could not cover (the actual numerator/denominator pair was not in the integer pool, was filtered out by the 200-cap, or the derivation involved intermediate counts the briefing summarises but does not enumerate). Fix adds a third-chance auto-grounding rule in `validate_grounded_numbers`: any value with a `%` suffix in `[0.0, 100.0]` expressible as `round(value, 1)` (i.e. 1-decimal precision) is auto-grounded. Conservative scope — only `%`-suffixed tokens; ARR-class hallucinations (`$5.7M` → `5_700_000`, out of `[0, 100]`) and bare integers without `%` are still scrutinised. Pinned by `tests/test_round67_grounding_rejection_below_10_percent.py` (18 tests, including a synthetic 38-narrative batch confirming <=10% rate plus negative controls). **Phase 4 (build-script footgun follow-on)**: Round 67's first DMG build produced `AdoptIQ-v1.0.4-build1.dmg` even though `config.py` said build "41" — the same Round 61 footgun re-emerging at a layer above `update_version_pc.py`. Pre-R67 `build_mac.sh`, `build_mac_dmg.sh`, and `build_pc.bat` all wrapped `${ADOPTIQ_BUILD:-1}` and explicitly exported the resulting "1" to `update_version_pc.py`, defeating the R61 SSoT lookup in that file. Fix removes the hard-coded defaults from all three scripts and reads the resolved version back from `config.py` after `update_version_pc.py` runs. Pinned by `tests/test_round67_build_scripts_no_footgun.py` (10 tests). Net pytest delta +65 (3986 → 4051): 6 + 7 + 6 + 7 + 4 + 18 + 8 + 10 = 66 (plus 2 existing tests updated where they had pinned outdated shapes — `test_round66_p0_html_strip.py` Action_Plans test now feeds canonical `SUBJECT_C` / `DESCRIPTION_C` since R67/B7 routes Action_Plans through curated projection, and `test_round66_p1_risk_components_per_customer.py` contract preserved by emitting BOTH `logger.warning` + `logger.debug` on the R67/B2 fallback path). All 4 `make verify` gates green: ruff clean, bandit 0 HIGH/MED, pip-audit clean. In-locals floor (40) preserved.

### 100% Accuracy Sweep on Build 37 reports (Round 65 / Build38)
Build 37 manual acceptance audit surfaced six issues across the Compact / Renewal / Comprehensive / Leader pair. **X-1** (cross-format AB count drift Renewal=161 vs Compact=162) was DEBUNKED during phase-1 investigation — both formats returned 161 identical ABs; the apparent 162 in Compact was a misread of the title row in row 0; the underlying schema bug is fixed under R-1. **C-2** (Comprehensive missing `Action_Plans` for Brian Frazier) was largely CLOSED by Round 64 / B2 (dedicated `Action_Plans` sheet + `_AP_CLOSED_STATUSES` recognition); Round 65 / phase 1 hardens the writer further by extracting `LeaderReportGenerator._fetch_action_plans` into a standalone module-level `fetch_action_plans_snowflake(account_ids, days, owner_emails)` helper, merging CSConsole + Snowflake rows with dedup by Action Plan ID, and stamping a single provenance row (`Status=EMPTY`, `Source=CSConsole+Snowflake`, marker column `_adoptiq_provenance_row=True`) when both sources return zero so the operator sees honest provenance instead of a missing sheet. `count_open_action_plans` skips the marker rows so the Summary KPI reads `0` in the truly-empty case. **R-1 (XLSX title row in row 0 broke `pd.read_excel`)**: Compact, Renewal, and Leader writers all merged a title across columns into row 0 then wrote real headers into row 1, so downstream `pd.read_excel("Renewal_Summary")` returned columns like `"Renewal Summary -  Renewal Analysis"`. Fix aligns all three writers to the Compact-canonical schema (headers in row 0, no merged title above data, autofilter+freeze_panes anchored at row 0) and moves descriptive titles into a dedicated `Report_Info` sheet as `Sheet_Title:<sheet_name>` rows. Two existing tests that had pinned the OLD buggy schema were updated (`test_round16_apply_excel_polish_offset.py`, `test_round43_compact_autofilter_no_overlap.py`). Pinned by `tests/test_round65_xlsx_clean_schema.py` (7 tests). **R-2 (Renewal Incidents=100 saturation)**: pre-R65 `risk_scoring._score_incidents` used absolute counts (`count*6 + active*8 + high*12 + critical*8`) and the renewal pipeline passed the portfolio-wide `ext_incidents` (33 items in the audit run) into per-customer scoring, so every customer's Incidents component pinned at 100. Two-pronged fix: formula-side cap inside `_score_incidents` (`min(count, 5) * 6.0` so a runaway count cannot dominate the formula; the `details` dict now carries `count_capped` + `count_cap_applied` for diagnostic transparency) AND a new `_r65_filter_customer_tagged_incidents` helper at the caller side that passes only customer-tagged incidents into per-customer scoring. The filter is conservative: when no incident in the list carries any of the recognised customer fields (`customer_id`/`customer_name`/`BU_NAME`), it returns the original list unchanged so portfolio-wide Webex Status incidents (which carry no customer field) flow through to every customer score with the formula-side cap as the only governor. Pinned by `tests/test_round65_incidents_no_saturation.py` (12 tests). **C-1 (misleading "builder error" Portfolio Overview fallback)**: pre-R65 when the R25B / R25C narrative validators caught the LLM rendering a number that disagreed with the canonical pipeline (e.g. "Total Customers: 27" while the dashboard tile said 37), the validators correctly raised `ValueError` but the outer `except Exception` branch emitted the bland `"Portfolio-level AI summary unavailable for this run (builder error after 1 LLM attempts; last_error_kind=ValueError)"`. Fix wires structured `drift_detail` through both `validate_word_numeric_drift` (R25B) and `validate_word_risk_band_claims` (R25C) — the validators attach `drift_detail` to the raised `ValueError` AND surface it in the returned `ConsistencyResultContract` for non-raising callers. The portfolio-error fallback in `app_simple.py` reads `drift_detail` off the exception and renders an operator-actionable paragraph naming the drifted field, the LLM's claim, and the canonical truth: `"Portfolio-level AI summary withheld: numeric drift detected (R25B validator). Drifted fields: - Total Customers: LLM rendered [27], canonical = 37"`. Genuine builder errors (NetworkError, AttributeError, a `ValueError` without `drift_detail`) still flow through the legacy fallback. Pinned by `tests/test_round65_portfolio_drift_message_distinguishes.py` (6 tests). **C-3 (34% R27 grounding rejection rate, no per-rejection diagnostic)**: pre-R65 the per-record diagnostic only carried `failure_codes` and a single `first_offending_token` — Round 64's rollup gave the operator the rate but no context to root-cause WHY each rejection happened. Fix extends `_r64_record_grounding_outcome` with optional `briefing_excerpt` + `narrative_excerpt` kwargs (bounded ~200 chars each via the new `_r65_grounding_excerpt` helper that whitespace-collapses + caps), the FULL sanitised `sample_offending` map (instead of just the first token), and a `recorded_at` UTC ISO timestamp. A new read-only `GET /api/grounding-diagnostics/<analysis_id>` admin endpoint returns the structured rejection records (loopback-only by default — the main app binds to 127.0.0.1 unless `ADOPTIQ_BIND_PUBLIC=1`; payload carries no PII because customer names are routed through `_id_digest`). The Round 64 rollup contract is preserved — successful narratives still don't inflate the records list, only rejections carry the excerpt payload. **Round 66 will use the captured data to root-cause the rejection rate and decide between validator tuning vs prompt enrichment** — Round 65 is instrumentation only. Pinned by `tests/test_round65_r27_diagnostic_capture.py` (13 tests). Phase 1 / C-2 hardening pinned by `tests/test_round65_comp_ap_snowflake_fetch.py` (10 tests) and `tests/test_round65_comp_ap_fallback_when_empty.py` (7 tests). Net pytest delta +55 (3718 → 3773): 10 phase-1 / C-2 fetch + 7 phase-1 / C-2 fallback + 7 R-1 + 12 R-2 + 6 C-1 + 13 C-3. In-locals floor (40) preserved.

### Comprehensive report accuracy fixes (Round 64 / Build37)
Build 36 manual acceptance produced a clean Compact / Renewal / Leader pair (0 grounding rejections, 0 AI errors, 0 mid-string citation injections) but five accuracy bugs in the Comprehensive pair (`AdoptIQ_*_Brian_Frazier_All_Contact_Center_90d_*.{xlsx,docx}`). Round 64 closes all five. **B1 (Title Page denominator coherence)**: pre-R64 `portfolio_metrics['total_customers']` was the AB ∪ CSOne ∪ Pulse intersection (39 on the Build 36 acceptance run) while the risk-band buckets were computed from the wider `risk_profiles` set (53), so the title page paired `Customers in portfolio: 39` with buckets summing to `1+6+24+22 = 53`. Fix uses a new `_r64_filter_to_narrow_universe` helper that scopes `risk_profiles` through the same AB ∪ CSOne ∪ Pulse universe before passing to `compute_portfolio_risk_summary`; the full `risk_profiles` is preserved for per-customer narratives. Pinned by `tests/test_round64_title_page_denominator_coherence.py` (5 tests). **B4 (mid-string citation injection)**: pre-R64 `report_source_injector._rewrite_paragraph_with_inline_citations` injected the chrome after every regex match, producing `"Analysis Period: 90  [Source: AdoptIQ Report Data Sources]Days"` mid-line splits in P005 / P011. Fix splits paragraphs on newlines, appends ONE citation at end-of-line for single-KPI lines, preserves the R57 multi-KPI interleave contract for multi-match lines. Pinned by `tests/test_round64_citation_injector_multiline.py` (10 tests). **B2 (Action plans (open) always-zero)**: pre-R64 the comprehensive XLSX had no `Action_Plans` sheet so `canonical_metrics.count_open_action_plans` could only read the always-blank `AB_Detail_All.Action Plan Title` column. Fix adds a dedicated `Action_Plans` sheet to the comprehensive XLSX (filtered to manager+technology by `_filter_csconsole_data_by_technology`), extends `count_open_action_plans` with an optional `ap_df` parameter that recognises a frozen-set of `_AP_CLOSED_STATUSES` (case-insensitive, whitespace-collapsed across `Status` / `Stage` / `State` candidate columns), and threads the new sheet through `report_export_styling.build_summary_rows`. The R62/B `AB_Detail_All` fallback is preserved so old comprehensive XLSXs still resolve to a sensible count. Pinned by `tests/test_round64_comprehensive_action_plans_sheet.py` (16 tests). **B3 (Portfolio Overview LLM resilience)**: pre-R64 a single transient empty body / `ERROR: timeout` collapsed straight into the bland `"AI analysis encountered an error.  Data processed successfully."` paragraph with no chance of recovery. Fix adds a `_r64_call_llm_with_retry` helper with 3-attempt exponential backoff (1s, 2s windows between attempts) for transient classifications (`timeout`/`rate_limited`/`server_error`/`network`/empty/exception), short-circuits on hard failures (`content_filter`/`credentials`/`auth`) so a non-recoverable verdict does not burn budget, and renders an honest fallback paragraph that names the failure mode + last error kind. The diag dict is persisted on `status['portfolio_llm_diag']` for the operator. Pinned by `tests/test_round64_portfolio_overview_retry_and_fallback.py` (14 tests). **B5 (grounding-failure diagnostics)**: pre-R64 11/28 customer narratives were rejected by the R16/R27 `ai_narrative_validator` with no per-report rollup — the operator had no per-customer signal as to which failure code or first-offending token tripped each rejection. Fix adds a `_r64_record_grounding_outcome` helper that maintains `status['grounding_diagnostics']` with a `rejection_summary` (`{rejected, total, rate}`) and a bounded `rejection_records` list (capped at `_R64_MAX_GROUNDING_RECORDS = 50`, customer names digested via `_id_digest` so the persisted analysis_status.json carries no PII) so the running-reports tile and the persisted file expose an honest `rejected=11 total=28 rate=0.393` rollup. Per-rejection structured-logging via `analysis_logger(analysis_id, scope=...)` provides SIEM-correlatable per-customer records with the failure_codes and first_offending_token. Both the per-customer narrative gate AND the portfolio narrative gate route through the same helper so the rollup is a single consolidated rejection ratio. Pinned by `tests/test_round64_grounding_diagnostics.py` (14 tests). Net pytest delta +59 (3663 → 3718). The Phase 3 retry helper is reusable — future formatters that wrap LLM calls SHOULD use `_r64_call_llm_with_retry` rather than rolling their own try/except so the operator-facing fallback contract stays honest.

### Corpus crypto self-heal on upgrade (Round 39 / Build15)
Every build's bake script (`scripts/bake_corpus.py`) mints a fresh sentinel when no stable one is found, so the `Resources/baked_corpus/sentinel.json` shipped with Build N+1 almost never matches the sentinel material in a user's existing `~/Library/Application Support/AdoptIQ/knowledge/` from Build N. Pre-Round-39, `corpus_bootstrap._install_baked_corpus_if_present` was strictly one-shot (`if user_db.exists(): return None`), so the new bake never replaced the old DB and `open_corpus_for_user` later raised `InvalidTag`, surfacing as "Last run failed (crypto) — authentication tag mismatch" with no UI escape hatch. Round 39 swaps the existing-corpus branch for **probe-and-recover**: a healthy corpus short-circuits (Round 35 idempotency preserved); a corpus that fails decryption is preserved aside as `<name>.broken-<utc_iso>` (single rolling backup capped at one set, so a stuck-bake-loop cannot fill the disk with 280 MB sidecars on every boot) and the bake snapshot is reinstalled in place. State labels: `_STATE.source = "baked"` for the healthy-existing-corpus branch (so the panel says "Active" not the misleading "Indexing OneDrive…"), `"self_healed_baked"` for a recovered install. The catch is intentionally narrow (`CorpusCryptoError` only) — any other exception bubbles so a transient `PermissionError` / `MemoryError` can never silently overwrite a healthy user corpus. Manual escape hatch: `POST /api/corpus/reset` (CSRF + `X-AdoptIQ-Internal` dual-auth, mirrors `/refresh`) plus `/api/intel/reset` user-facing alias plus `/corpus_reset` admin proxy plus a **hidden** "Reset corpus" button on the analyze panel that `intel_status.js` unhides ONLY when `boot.last_error_kind === 'crypto'`. The button uses `addEventListener` (no inline onclick, CSP-clean) and `confirm()`s before POSTing. Defense in depth: `scripts/bake_corpus.py` now runs a decrypt round-trip self-test after the structural verify (open-and-close + `SELECT count(*) FROM sqlite_master`); a failure deletes all four artifacts and exits with code 5 so `build_mac_dmg.sh` aborts before PyInstaller bundles a malformed bake. **This is the canonical fix for the upgrade-handoff class of bug; future builds that change crypto layout (KDF parameters, salt size, sentinel format) MUST rely on this self-heal rather than introducing new migration code.** Source-shape pinned by `tests/test_round39_self_heal_crypto_failure.py`, `tests/test_round39_reset_corpus_endpoint.py`, and `tests/test_round39_intel_status_panel_reset_button.py` (32 new tests).

### Build Pipeline
1. `python embed_credentials.py --ci-lint` rejects committed credential material; it never generates files
2. `update_version_pc.py` rewrites `ADOPTIQ_VERSION`/`ADOPTIQ_BUILD` in `config.py`
3. PyInstaller with platform `.spec` file → `dist/` without service credentials
4. Platform post-processing: macOS codesign + DMG; Windows OUTBOX copy + installer
5. Operators configure credentials at runtime through process environment variables or the per-user AdoptIQ `.env`

## Critical Rules

**Never commit:** populated credential files (`.env`, `secrets.env`), `dist/`, `build/`, `OUTBOX/`, `*.dmg`, `*.exe`

**Data safety (Tier 1 — highest priority):**
- Always validate DataFrame columns before access; guard with `.get()` or `if col in df.columns`
- Use parameterized Snowflake queries — never f-string SQL
- All form submissions require CSRF (Flask-WTF); never disable globally

**Correctness patterns:**
- Cross-report metric counts must go through `canonical_metrics.py` — never recompute in formatters
- Risk dict comparisons use string keys, not enum values
- ARR and severity logic lives in `risk_scoring.py` — don't replicate inline
- Excel column schema goes through `report_export_schema.py`; Excel polish through `apply_excel_polish` — don't re-implement either inline
- Word top-N tables go through `report_word_styling.add_banded_top_n_table` — keep custom paint logic only where leader-report tables need per-cell color overrides
- Top-N sorts must include a stable name-based tiebreaker so `make verify` stays deterministic across runs
- LLM-generated narratives that flow into reports must pass through `ai_narrative_validator.validate_narrative` — don't bypass the gate
- Comprehensive `action_plans` is now a deterministic count via `canonical_metrics.count_open_action_plans(ab_df)` and surfaces as the `Action plans (open)` row in `report_export_styling.build_summary_rows` (Round 62 / B). Never recompute the AB → action-plan derivation inline in a formatter; always go through the canonical helper so the supervisor harness's structured-data anchor agrees with the LLM narrative bucket.
- Logger emissions in `corpus_bootstrap.py` and `corpus_indexer.py` MUST go through the local `_safe_log_info(msg, *args)` helper (Round 62 / A1), NOT bare `logger.info(...)`. The helper gates emission on `_exit_log_streams_open()` so the closed-stream race during pytest teardown cannot leak `"I/O operation on closed file"` tracebacks via Python's internal `Handler.handleError()` path. Source-shape pinned by `tests/test_round62_corpus_logger_module_wide.py::test_no_bare_logger_info_call_outside_helper_body` AND (R63 / Tier A) `tests/test_round63_corpus_indexer_logger_parity.py::test_no_bare_logger_info_call_outside_helper_body_in_corpus_indexer` for the parity guard.
- The closed-stream defense implementation lives in `_logging_helpers.py` (Round 63 / Tier A), NOT inline in either consumer. `corpus_bootstrap._safe_log_info` and `corpus_indexer._safe_log_info` are thin wrappers that delegate via `safe_log_info(target_logger, msg, *args)`. Any new module that needs the same defense MUST import from `_logging_helpers` rather than copying the body — the duplication that R62 had to ship (because of the `corpus_bootstrap → corpus_indexer` import edge) is the exact failure mode the shared module exists to prevent. The `propagate` default in `exit_log_streams_open`'s chain walk is `True` (canonical, matches `logging.Logger.__init__`); pinned by `tests/test_round63_logging_helpers.py::test_propagate_default_is_true_matching_logging_module`.
- HTML stripping for Excel sheets MUST go through `data_normalization._strip_html_safe` (BeautifulSoup primary, regex fallback) for malformed-markup resilience and `<script>` / `<style>` block decompose (Round 66 / B4). The XLSX writer's allow-list `_R66_HTML_STRIP_SHEETS` (in `adoptiq_backend.write_excel_workbook`) covers the AB / Action_Plans / CSConsole_* / Adoption_Barriers / Customer_Pulse / Success_Priorities family — when adding a new sheet that ingests Snowflake rich-text, add the sheet name there too. CSConsole DataFrames (`filtered_action_plans`, `filtered_customer_pulse`, `filtered_success_priorities`, `filtered_adoption_barriers`) MUST also be passed through `strip_html_from_dataframe` immediately after technology-scoping in `app_simple.py` so Word formatters and AI prompts see clean text upstream. Pinned by `tests/test_round66_p0_html_strip.py` (15 tests).
- Multi-KPI Word paragraph citations (Round 66 / B1): the citation injector's multi-match path is unit-aware. A line like `"Analysis Period: 90 Days  Total Customers: 39"` (single line, multi-KPI, with units) defers each citation past its unit (via `_logical_label_start_pos` + `_value_end_no_trailing_ws` + `_BOUNDARY_HAS_UNIT_RE`); a line like `"Customers: 52. Adoption Barriers: 68"` (sentence-style, punctuation between segments) preserves the R57 contract (citation immediately after the value). New formatters that emit sentence-style multi-KPI lines MUST keep punctuation between segments OR add a dedicated test under `tests/test_round66_p0_citation_inline_units.py` so the boundary classification stays predictable.
- Per-customer risk component breakdown (Round 66 / B6): the Comprehensive XLSX MUST carry a `Risk_Components` sheet with the seven canonical component scores per customer (`Adoption_Barriers_Score`, `Support_Cases_Score`, `Customer_Pulse_Score`, `Action_Plans_Score`, `Incidents_Score`, `Contract_Score`, `Engagement_Score`) plus `Top_Risk_Factor`. Sort key MUST be `(risk_score_0_100 DESC, customer_name ASC)` per the SSoT determinism rule. An empty `risk_profiles` dict short-circuits to a single provenance row (`AdoptIQ_Status=EMPTY`, `_adoptiq_provenance_row=True`). Pinned by `tests/test_round66_p1_risk_components_per_customer.py` (12 tests).
- Per-customer pulse / action_plan / subscription threading into `compute_customer_risk_profile` (Round 66 / B8): when adding a new `extra_frames` source to a callsite that ultimately invokes `compact_report_formatter.calculate_renewal_risk_scores`, EITHER extend `_r66_b8_classify_extra_frames` with the new source's column markers AND its triple-return slot OR pass the data via a separate kwarg. Skipping this step means the new source will be classified as `None` and the corresponding component will score as 0.0 (NOT excluded) — symptom is a Compact composite that's lower than expected for customers who have data in the new source. Pinned by `tests/test_round66_p1_compact_risk_threading.py` (13 tests).
- AI narrative grounding tolerance (Round 66 / B11): `ai_narrative_validator._COMMON_REFERENCE_NUMBERS` covers integers 0-100, multiples of 5 from 100-500, and calendar years 2024-2030 — small integers in this range are auto-grounded WITHOUT a briefing match. Narrative percentages in `[0, 100]` that can be expressed as `(a / b) * 100` for integer pair `(a, b)` in the briefing (within 0.5 ppt) are grounded via `_is_derived_ratio_percentage`. ARR-class values (>= $100K) use a 5% relative tolerance floor in `_number_in_allowed`. The trade-off: a hallucinated "47 open action plans" against a briefing of "12" will NOT be caught by `validate_grounded_numbers` — rejection has to come from `validate_no_invented_entities` or `validate_no_html_injection`. Round 66 deliberately accepted this trade-off to clear the 34% R27 rejection rate floor; if the next manual acceptance run shows hallucinated small-integer counts in production, narrow the auto-ground band back to 0-31 + briefing-derived integers only. Pinned by `tests/test_round66_p2_grounding_root_cause.py` (22 tests).
- KPI-specific renewal recommendations (Round 66 / B12): `report_utils` carries six KPI-specific helper functions (`kpi_recommendation_csm_engagement`, `_training`, `_engagement_cadence`, `_high_severity_barriers`, `_premium_support`, `_upsell`) that embed the triggering KPI value directly into the recommendation string. `advanced_renewal_analyzer._generate_renewal_recommendations` lazy-imports them with a graceful fallback to the legacy generic strings if the module is unavailable. New renewal-recommendation branches SHOULD use the matching helper rather than rolling their own string templates so the KPI-grounding contract stays consistent.
- Admin grounding-rate visibility (Round 66 / B14): `app_simple.py /api/status/all` MUST project `grounding_rejection_rate`, `grounding_rejection_count`, `grounding_total_count` from `analysis_status[<id>]['grounding_diagnostics']['rejection_summary']` so the admin "Currently Running Reports" tile can render the three-color pill (green <=10%, yellow <=25%, red >25%). Defensive type checks (`isinstance(_r66_diag, dict)`, `try/except (TypeError, ValueError)`) must wrap the projection so a malformed diagnostics rollup never crashes the bulk endpoint. The full structured `rejection_records` list is served on demand by the existing R65/C-3 `/api/grounding-diagnostics/<analysis_id>` endpoint. Pinned by `tests/test_round66_p2_admin_grounding_pill.py` (12 tests).
- Ask AI eval framework (Round 66.2 / Pass 4): the `tests/ask_ai_eval/` tree carries 5 portfolio fixtures, 50 question YAMLs, 50 synthetic CircuIT cassettes, and 4 predicate types. The runner ALWAYS routes through `compose_grounded_answer` (the documented "ASK AI EVAL SEAM" comment in `ask_ai_grounded.py`); new code paths that bypass that composer will not be exercised by the eval and will not contribute to the scorecard. `make eval-ask-ai` is deliberately excluded from `make verify`; the `eval` pytest marker excludes it from default `pytest -q`. Live-CircuIT cassette recording is a one-time operator step (`MOCK_CIRCUIT_MODE=record python -m tests.ask_ai_eval.runner`) — replay (the default) runs offline. Cassette drift is detected via SHA256 prompt-hash comparison; a stale cassette fails LOUD in `replay` mode. Source-shape pinned by `tests/test_round66_p4_eval_framework_shape.py`.
- Ask AI hybrid retrieval (Round 66.2 / Pass 5): `Config.ASK_AI_RETRIEVAL_METHOD` controls the ranker (`"hybrid"` default, `"lexical"` escape hatch). The hybrid path uses `BAAI/bge-small-en-v1.5` (384-dim INT8-quantized ONNX, ~33 MB) via `fastembed>=0.8.0`. RRF fuses lexical + dense ranks with `k_rrf=60` (Cormack 2009; `Config.ASK_AI_RRF_K`). Vectors live in the `chunk_vectors(chunk_id PK, model_id, model_dim, vector BLOB)` table INSIDE the existing AES-GCM-encrypted corpus DB (NOT a separate `.npy` artifact); `chunk_id` is FK→`playbook_chunks.id ON DELETE CASCADE`. `knowledge_schema.SCHEMA_VERSION` MUST be bumped on any schema change to `chunk_vectors` so the indexer's `needs_rebuild` check rebuilds the corpus DB safely (Round 39 self-heal contract handles the rebuild). Embedder loads on a daemon thread via `corpus_bootstrap._warm_embedder_in_background()` so the first Ask AI query never pays the cold-start. Any embedder failure (fastembed missing, ONNX runtime missing, model file missing, malformed BLOB) MUST flip `Config.ASK_AI_RETRIEVAL_METHOD = "lexical"` for the process via the warmup path AND the per-query `_hybrid_rank_evidence` fallback. New evidence-record fields (`bm25_rank`, `dense_rank`, `rrf_score`) are OPTIONAL on `EvidenceRecord` so legacy call sites without rank fields still construct cleanly. Pinned by `tests/test_round66_p5_hybrid_retrieval_shape.py` (24 tests).
- Cross-format risk-scoring parity (Round 67 / B1): Compact AND Renewal MUST publish identical `Risk_Score_0_10` AND identical `Risk_Band` (CRITICAL/HIGH/MEDIUM/LOW/HEALTHY) AND identical `Risk_Level` vocabulary (`MODERATE` for the medium band, NOT `MEDIUM`) for the same customer in the same scope. Two-pronged contract: (a) BOTH formats MUST thread `ext_incidents` through `_r65_filter_customer_tagged_incidents(ext_incidents, customer_name)` — passing `ext_incidents=None` to one side and a filtered list to the other (the pre-R67 Compact bug) re-introduces the per-customer `Incidents` component drift; (b) Renewal `Renewal_Summary.Overall_Risk_Score` MUST be the 0-10 scale projected from `profile["risk_score_0_10"]`, NOT the raw 0-100 score (Renewal still preserves `Risk_Score_0_100` as a back-compat secondary column for legacy consumers, but the primary published value is 0-10). Word narratives + Customer Health Dashboard tables in BOTH formats MUST display the 0-10 score and the MODERATE label so the user-facing text is consistent. New report formats that publish per-customer scores MUST follow this same template: route through `compute_customer_risk_profile`, project `risk_score_0_10` as `Overall_Risk_Score`, remap `MEDIUM → MODERATE`, thread filtered ext_incidents. Pinned by `tests/test_round67_compact_renewal_score_parity.py` + `tests/test_round67_renewal_score_scale_and_band.py` (14 tests total).
- Comprehensive XLSX sheet always-present contract (Round 67 / B2): when adding a new optional sheet to `run_comprehensive_analysis`, the `all_sheets[<name>] = ...` assignment MUST be hoisted OUT of any broad `try/except` that wraps the row-construction. The construction can fail (DataFrame ops, missing columns, etc.) but the sheet itself MUST always be present in the workbook so the operator's reading checklist (`["Risk_Components", "Action_Plans", ...]`) never has a phantom missing entry. The fallback row MUST carry `_adoptiq_provenance_row=True` plus `AdoptIQ_Status="ERROR"` plus `AdoptIQ_Message=<exception summary>` so the operator can root-cause without opening the logs. Both `logger.warning` (operator-visible) AND `logger.debug` (audit-friendly grep target, R66 contract) MUST be emitted on the failure path. Pinned by `tests/test_round67_comprehensive_risk_components_always_present.py` (7 tests).
- HTML stripping allowlist (Round 67 / B4): when adding ANY new sheet to `run_comprehensive_analysis` that ingests Snowflake-sourced rich-text fields (Salesforce free-text, CSConsole notes, customer_pulse comments, etc.), the sheet name MUST be added to `_R66_HTML_STRIP_SHEETS` in `adoptiq_backend.write_excel_workbook`. Pre-R67 `CSOne_Detail_All` was the lone holdout — its `Problem Details` column leaked raw `<br />` / `<agent name>` / `&#34;` markup into Excel cells. The strip path is `_strip_html_safe` (BeautifulSoup primary, regex fallback) and is idempotent on already-clean strings, so adding a sheet is always safe. Pinned by `tests/test_round67_csone_detail_html_strip.py` (4 tests).
- Compact Report_Info schema parity (Round 67 / B5): the Compact `Report_Info` writer in `run_compact_analysis` MUST emit `Item, Value` columns (matching Renewal / Leader / Comprehensive) — NOT the legacy `Status, Warning, Generated_At` shape. Sheet titles are keyed `Item="Sheet_Title:<sheet_name>", Value=<title>`, partial-data warnings keyed `Item="Partial_Data_Warning"`, truncation events keyed `Item="Excel_Truncation"`. Baseline rows (`Export type`, `Generated at (UTC)`) always present. Pinned by `tests/test_round67_compact_xlsx_report_info_schema.py` (7 tests).
- Compact / Renewal column-name parity (Round 67 / B6): Compact `Risk_Summary.Overall_Risk_Score` (NOT the legacy `Risk_Score`) is the canonical column name aligned with Renewal `Renewal_Summary.Overall_Risk_Score`. Compact retains `Risk_Score` as a back-compat alias column for one build (then deprecated in Round 68). `Risk_Level` vocabulary must be MODERATE (NOT MEDIUM) on BOTH sides.
- Compact curated-column projection (Round 67 / B7): the Compact `Critical_Adoption_Barriers` and `Action_Plans` writers MUST be routed through `_r15_apply_export_schema` so the raw 243-259 col Snowflake dumps are projected to a customer-facing column subset (~30 cols). Both sheets are now declared in `report_export_schema.CURATED_COLUMNS`: `"Critical_Adoption_Barriers"` reuses `_CURATED_AB_DETAIL_ALL` (it's an AB filter); `"Action_Plans"` uses the new `_CURATED_ACTION_PLANS` tuple. Provenance markers (`_adoptiq_provenance_row`, `AdoptIQ_*`) are intentionally NOT in any curated set — they live on the `INTERNAL_COLUMN_DENYLIST` and surface separately in the `Report_Info` sheet. Pinned by `tests/test_round67_compact_xlsx_schema_no_raw_snowflake_dump.py` (6 tests).
- AI grounding for derived percentages (Round 67 / B8): `validate_grounded_numbers` carries a third-chance auto-grounding rule (after the integer allowlist and the strict ratio check) that accepts any value with a `%` suffix in `[0.0, 100.0]` expressible as `round(value, 1)` (i.e. 1-decimal precision). Conservative scope — only `%`-suffixed tokens; ARR-class hallucinations (`$5.7M` → `5_700_000`, out of `[0, 100]`) and bare integers without `%` are still scrutinised. The trade-off is intentional: a hallucinated "47.3%" against a briefing whose pair derives to "23.6%" will not be caught, but pre-R67 the operator was seeing a 21.1% R27 rejection rate dominated by these LLM-derived percentages and the audit trail was being polluted with noise. Pinned by `tests/test_round67_grounding_rejection_below_10_percent.py` (18 tests).
- Ask AI per-query diagnostics (Round 66.2 / Pass 5): `app_simple.py` carries a 100-entry FIFO `_ASK_AI_DIAG_BUFFER` (`collections.OrderedDict`, lock-protected via `_ASK_AI_DIAG_LOCK`). The `ask_ai_portfolio` route MUST mint a `secrets.token_urlsafe(12)` query ID, persist `retrieval_diag` via `_record_ask_ai_query_diag`, and return `query_id` + `retrieval_method` to the client. New admin endpoint `GET /api/ask-ai/diagnostics/<query_id>` returns the per-query diag (loopback-only by default; payload carries no PII). Ring-buffer eviction is FIFO: the 101st entry evicts the 1st. New Ask AI endpoints SHOULD also persist diagnostics via the same helper so the admin tooling stays consistent. Pinned by `tests/test_round66_p5_hybrid_retrieval_shape.py::test_ring_buffer_evicts_oldest_entry_at_capacity`.
- Bake-time vector dependency (Round 66.2 / Pass 5): `scripts/bake_corpus.py._bake_chunk_vectors` MUST `RuntimeError` when `ask_ai_embeddings.get_embedder()` returns `None` on the bake host — bake-time hard-fail is intentional so a DMG that ships without dense vectors is detected at build, not at runtime. Future bake-pipeline changes that add new vectorised tables MUST follow the same contract: bake-time hard-fail on missing embedder, runtime soft-fall to lexical. The Round 39 decrypt round-trip self-test now verifies a sample vector decodes correctly; do not weaken that check.
- Visible build label in every report (Round 68 / Build 42 / A1): every Word docx and Excel xlsx report MUST carry the App_Version + App_Build + Process_Started_At_UTC + Report_Generated_At_UTC label so a stale-binary report (the Build 41 acceptance trap) is detectable at a glance. The contract lives in the new `_r68_build_label.py` helper: `apply_word_footer(doc)` for Word section footers, `append_build_label_records(records, ...)` / `append_build_label_records_4col(records)` / `append_build_label_rows_pairs(rows)` for XLSX `Report_Info` sheets. New report writers MUST call the helper directly; do NOT hand-roll the four-row pattern inline. `Process_Started_At_UTC` is captured ONCE at app startup via `_PROCESS_STARTED_AT_UTC` (eagerly imported in `app_simple.py` so the timestamp reflects true app boot). Pinned by `tests/test_round68_build_label_in_every_report.py` (18 tests).
- Restart-aware health endpoint (Round 68 / Build 42 / A2): `GET /api/version` MUST return `{version, build, process_started_at_utc, code_loaded_at_utc, dmg_install_at_utc, restart_required}` where `restart_required: true` when `Path(sys.executable).stat().st_mtime > process_started_at_utc` (frozen build only — dev-mode is always `False`). `static/js/r68_restart_banner.js` polls this endpoint ONCE on first analyze-page load and renders the non-dismissable yellow banner in `#r68-restart-required-banner` when restart is needed. The banner is the user-facing escape hatch from the stale-binary trap. New code paths that reload binaries should set `_PROCESS_STARTED_AT_UTC` accordingly. Pinned by `tests/test_round68_version_endpoint.py` (10 tests).
- Portfolio drift retry-then-escalate (Round 68 / Build 42 / A4): when the R25B/R25C numeric/risk-band drift validator catches a discrepancy in the LLM portfolio narrative, the new flow is bounded retries (`_R68_MAX_PORTFOLIO_DRIFT_RETRIES = 2`) with a tightened correction prompt naming the drifted field + canonical value, then escalates to a deterministic narrative built from `canonical_metrics` via `_r68_build_deterministic_portfolio_narrative` on exhaustion. Pre-R68 the validator raised `ValueError` and collapsed to the bland `"Portfolio-level AI summary withheld"` paragraph 12 times in one Comprehensive run; post-R68 the operator sees an honest factual paragraph naming the canonical totals. `analysis_status['portfolio_llm_diag']` carries `drift_attempts` + `unique_failures` + `escalated_to_deterministic` for visibility. New portfolio-narrative LLM call sites SHOULD wrap with the same retry-then-escalate pattern so transient drift never blocks a build. Pinned by `tests/test_round27_ai_storyboard_validation.py` (updated for R68 behavior) + the deterministic-narrative tests in the R68 suite.
- Per-customer LLM fallback diagnostics (Round 68 / Build 42 / A5): when a per-customer narrative LLM call falls back to the `"AI analysis temporarily unavailable"` placeholder, the failure cause MUST be recorded via `_r68_record_per_customer_llm_outcome(status, customer_name, error_kind, attempts)` so `analysis_status['per_customer_llm_diag']` rolls up to `{by_customer: {<digest>: {first_error_kind, attempts}}, total: N, fallback: M, fallback_rate: M/N}`. Default acceptance is `fallback_rate < 0.05` (5%). Customer names are routed through `_id_digest` so the persisted analysis_status carries no PII. New per-customer LLM wrappers MUST call this helper on both success and failure paths so the rollup stays honest.
- OneDrive autodiscovery zero-byte skip (Round 68 / Build 42 / B1): `get_latest_csone_from_folder_diag` returns a 3-tuple `(path, sync_status, real_file_count)` and skips `st_size == 0` files (OneDrive Files-On-Demand placeholders that haven't been materialized to local disk). Legacy `get_latest_csone_from_folder` is now a thin back-compat wrapper that drops the diag fields. Callers that need to record a `partial_data_warnings` entry on autodiscovery miss MUST use the diag-aware helper directly (the leader endpoint already does). Pinned by `tests/test_round68_csone_autodiscovery_skip_zero_byte.py` (10 tests).
- Daily refresh worker sentinel gate (Round 68 / Build 42 / B5): the daily refresh worker's `transition_unblocked` logic MUST conjunct `_r68_onedrive_sentinel_present()` before triggering `corpus_bootstrap.request_refresh` so we don't burn refresh attempts when the OneDrive sync is healthy but the canonical sentinel hasn't landed yet. The worker continues with an accelerated 30s tick cadence (capped at 240 seconds total via `_DAILY_REFRESH_BLOCKED_TICK_CAP`) instead of attempting a refresh. Pinned by `tests/test_round68_daily_worker_sentinel_gate.py`.
- Ask AI single rank pass (Round 68 / Build 42 / C1): `ask_ai_grounded.build_evidence_context_with_ranking` returns the 4-tuple `(context_text, allowed_ids, used_records, ranked)` so `compute_retrieval_diag` can reuse the ranking via the new `precomputed_ranked` kwarg instead of re-ranking. The original `build_evidence_context` is now a thin back-compat wrapper that drops the `ranked` list. New Ask AI flows that need both the context and the diag MUST call `build_evidence_context_with_ranking` to avoid the 1 embedding+RRF round-trip per question hit.
- Ask AI per-query diagnostics persisted (Round 68 / Build 42 / C2): in addition to the in-memory FIFO ring buffer (Round 66.2 / Pass 5), `_record_ask_ai_query_diag` MUST also call `_r68_persist_ask_ai_diag` so the diag survives a process restart. Storage is the new `ask_ai_diag` table inside `admin_monitoring_v2.db` with the same 100-entry FIFO contract enforced via `INSERT OR REPLACE` + a row-count purge. `_get_ask_ai_query_diag` falls through to the SQLite sidecar when the in-memory buffer doesn't carry the requested query_id. New Ask AI endpoints SHOULD persist diagnostics via the same helper so the operator can share a `query_id` across a crash.
- Ask AI risk profile streaming mode (Round 68 / Build 42 / C4): `ask_ai_grounded.py` raises the per-customer risk-profile cap from 200 to `_RISK_PROFILE_CAP = 500` (configurable via `ADOPTIQ_ASK_AI_RISK_PROFILE_CAP`). For portfolios `> 500` customers, the per-customer scoring loop is entirely SKIPPED to keep the request bounded; the `canonical_headline` block carries an explicit `STREAMING` coverage disclosure naming the universe size + cap so the LLM treats all risk-derived metrics as unavailable for that run. Four-state coverage disclosure: `FULL` (≤500 with profiles), `PARTIAL` (≤500 with some profile failures), `STREAMING` (>500, deliberate skip), `NONE` (no profiles available). New Ask AI flows that consume `risk_profiles` MUST honor the streaming disclosure so a portfolio with 1000+ customers never silently fabricates per-customer risk metrics.
- Ask AI clickable evidence index (Round 68 / Build 42 / C7): the `/api/ask-ai-portfolio` response payload MUST carry `evidence_index` — a list of `{source_id, source_type, customer, timestamp, snippet}` records built from the SAME ranked records actually used to build the LLM context (capped at 200 entries, snippet bounded to 280 chars). The client renders `[Source: ID]` markers as clickable badges via `static/js/ask_ai.js::_r68BuildCitationBadge` with a popover showing the snippet + source_type + customer + timestamp. Source IDs are XSS-safe (rendered via `textContent` only). Multi-ID citations like `[Source: ID1, ID2]` are split into individual badges. New Ask AI endpoints that emit citations MUST also build an `evidence_index` so the UX stays consistent.
- Citation injection in bullet/multi-KPI/paren layouts (Round 76 / R76-A — Builds 50 + 51): `report_source_injector._rewrite_paragraph_with_inline_citations` MUST classify boundary tokens through THREE classifiers in a strict precedence order: (1) `_BOUNDARY_HAS_BULLET_RE` (`•`/`*`/` -`) checked BEFORE the unit-token branch so `"999• Total Action Plans"` doesn't get the bullet glyph misclassified as a unit-deferral target — citation lands flush with the value, bullet stays attached to the next label; (2) `_line_is_paren_kpi_cluster(line, line_matches)` for whole-line clusters like `"(APs: 16, ABs: 3, CPs: 1, TAC: 4)"` — emits ONE citation immediately after the closing `)`; (3) `_embedded_paren_clusters(line, line_matches)` for embedded clusters like `"Bookings stalled (APs: 16, ABs: 3, CPs: 1, TAC: 4) and other context."` — returns `{cluster_end_match_idx: close_paren_position}`, the multi-match loop skips mid-cluster matches and emits ONE citation flush with the closing `)`. Cluster qualification rules (apply to BOTH whole-line and embedded variants): 2+ matches inside the same `(...)` group, no nested `(`, no bullet glyphs, no joiner words (`then`/`and`/`or`/`but`/`also`/`with`) in the between segments, every comma-separated body segment matches a strict `KPI: value` regex. New formatters that emit single-paragraph multi-KPI lines MUST keep punctuation between segments OR use the bullet/paren shape so the boundary classification stays predictable. Pinned by `tests/test_round76_citation_bullet_and_paren_layouts.py` (17 tests) + `tests/test_round76_build51_embedded_paren_cluster.py` (10 tests). Live audit confirmed 0 mid-string injections in all 4 DOCX (down from Build 49's 4 in leader + 1 each in comp/compact/renewal).
- Snowflake global-config error classification (Round 76 / R76-B — Builds 50 + 51 + 52): `EnhancedSnowflakeInsights._is_globally_unavailable_error(err_str)` is the SSoT for distinguishing global-config errors (will hit on every customer in the run) from transient errors (will not). The matcher recognises 8 substring patterns (case-insensitive): `"does not exist"`, `"not authorized"`, `"invalid identifier"`, `"column_missing"`, `"missing required column"`, `"not in allowlist policy"` (raised by `snowflake_table_policy.guard_sql` for unknown tables), `"refused by table policy"` (raised by `_PolicyEnforcingCursor.execute` for off-allowlist tables), and `"blocked by policy"` (raised by `guard_sql` for explicitly-blocked legacy tables). The matcher is wired into THREE call sites: per-section exception handlers in `_get_booking_insights` + `_get_risk_insights` (defense layer 1) AND the central `section_errors` aggregation loop in `get_comprehensive_customer_insights` (defense layer 2 — catches sections that don't tag themselves with `globally_unavailable_section=True` directly but emit a recognized error string). The aggregator routes recognised errors into `self._globally_unavailable_sections: set[str]` (instance-level, accumulates across customers) which is exposed via `get_globally_unavailable_sections()` accessor and surfaced as a single banner near the validation summary in `LeaderReportGenerator._add_validation_section`. **Future Snowflake sections that hit a global-config error MUST funnel exception strings through `_is_globally_unavailable_error` rather than rolling their own substring match — this is the SSoT contract.** Live audit confirmed unavailable token count dropped from 423 → 1 (the legitimate single suppression banner; 99.7% reduction). Pinned by `tests/test_round76_globally_unavailable_section_suppression.py` (17 tests) + `tests/test_round76_build51_global_unavailable_central_filter.py` (17 tests) + `tests/test_round76_build52_table_policy_violation_matcher.py` (10 tests).
- Live acceptance loop is the new ground truth (Round 76 lesson learned): synthetic-fixture pytest passing is necessary but not sufficient for any round that touches Snowflake error handling, narrative emission, or formatter chrome. Round 76 demonstrated this empirically: Build 50's R76-B fix had ALL 17 fixture tests passing AND `make verify` GREEN, but the LIVE audit revealed two production-only error strings the fixtures didn't model (`"refused by table policy"` and `"column_missing:"`); the fix required two more bake-and-audit iterations (Build 51, Build 52) before the live token count dropped below threshold. Future rounds touching this class of bug MUST include the full bake-install-regen-audit loop before claiming closure — the synthetic-fixture-only loop is documented to be insufficient.
- Comprehensive narrative stub-bullet filter (Round 78 / B1 — Build 54): `adoptiq_backend._R78_STUB_RE` is the SSoT regex for detecting LLM-generated empty acknowledgement bullets like `"Industry Benchmarking: Data unavailable."`. The Build 53 audit caught the per-customer storyboard LLM emitting ~170 such stubs per Comprehensive run because `PROMPT_CUSTOMER_TEMPLATE` asks the model to fill 8 sub-categories (Industry Benchmarking, Technical Competency, Adoption Velocity, Competitive Positioning, Integration Complexity, Operational Disruption, Sentiment Indicators, Communication Patterns) and the briefing routinely lacks data for many of them. The filter is wired into the bullet branch of `append_to_word_report` (~line 8348-8359) — when `_R78_STUB_RE.match(bullet_text)` is True, the function emits no paragraph and the loop counter `i` is incremented before the `continue` statement (mirrors the empty-line-skip convention). **Critical contract: the regex MUST match ONLY when the entire bullet is the stub.** Bullets like `"Operational Disruption: Data unavailable. No active incidents."` (substantive narrative after the marker) and `"Strategic Headwinds: ... (SP-ID: data unavailable) ..."` (parenthetical, not the whole bullet) MUST be preserved. The regex uses anchored `^...$` matching with optional `**` bold markers around the category and around the "Data unavailable" phrase, an optional terminal period, and trailing whitespace tolerance — see the regex source-shape pin in `tests/test_round78_b1_comprehensive_stub_bullet_filter.py` for the exhaustive boundary cases. New report formatters that route LLM output through `append_to_word_report` automatically inherit the filter — no per-formatter wiring needed. Pinned by `tests/test_round78_b1_comprehensive_stub_bullet_filter.py` (28 tests).
- Leader Action_Plans cross-CSSM dedup (Round 78 / B2 — Build 54): the Leader XLSX writer in `app_simple.py` (~line 26412) MUST dedup the `sheets['Action_Plans']` DataFrame by `ID` with `keep='first'` BEFORE the Excel write. Build 53 audit caught `Action_Plans` shipping 366 rows / 344 unique IDs (22 cross-CSSM duplicates) for Brian Frazier 90d. Same root cause as the R75/B2 fix on the AB sheet, but on the AP sheet: when a single Action Plan is attributed to multiple CSSMs via the R72 `_ATTRIBUTED_BY_ACCOUNT` shared-account pathway in `_slice_by_owner_or_account`, the per-CSSM AP frames each carry the same `ID`, and the raw `pd.concat` emits the row N times (one per attributed CSSM). The fix splits the combined DataFrame into `with_id` (drop_duplicates by `ID`, `keep='first'`) and `no_id` (passthrough) slices, re-concats them, and emits a `logger.info` line `"Round 78 / B2: leader Action_Plans sheet deduped by ID: {before} raw rows -> {after} unique (removed {delta} cross-CSSM duplicates)"` when the row count changes. **`keep='first'` is intentional** — it preserves the first attribution from the order in which `_slice_by_owner_or_account` walked the team_data, matching the convention already established by R75/B2 for the AB sheet. New per-CSSM XLSX sheets that aggregate via `pd.concat(all_<sheet>, ignore_index=True)` MUST follow the same pattern: dedup by `ID` with `keep='first'`, log the delta, mirror this block. Pinned by `tests/test_round78_b2_leader_action_plans_dedup.py` (8 tests).
- Leader vs Comp Action_Plans scope-filter divergence (Round 78 / F3 — Build 54): the Comprehensive and Leader reports scope `Action_Plans` differently **by design** and small-margin row count divergences are NOT regressions. **Leader scopes by CSSM ownership** (`Owner.Email IN team_emails OR Assignee.Email IN team_emails`) so an action plan with a blank Account Manager and assignees outside the team's CSSM list is correctly excluded from the Leader view. **Comprehensive scopes by Account ID** (`Account.Id IN team_subscription_accounts`) so the same action plan IS included if its Account is in the team's subscription set. Build 53 audit measured a 1.7% delta (6 of 350 rows) — Cisco-on-Cisco internal accounts whose APs have blank `Account Manager` and assignees outside the recognised team. Future scope-edge anomalies under this category MUST be documented in the same section, not "fixed". The two reports answer different questions: Leader = "what does this team's CSSMs own?", Comprehensive = "what does this team's account portfolio carry?" — both are correct.
- Team roster + default-fallback parity (Round 80 / Build 56): `team_config.json` is the SSoT for managers + direct reports; `adoptiq_backend._get_default_team_config()` MUST stay in byte-for-byte parity with the JSON (same managers list with identical ordering, same `(manager, name, email)` tuples). Pre-R80 the fallback drifted (referenced a non-existent `Josh Horowitz` manager and placed `Asad Sarfaraz` under Dee Kindrick); R80 brought them back into parity with one entry per JSON row. Future roster changes MUST update BOTH files together; the parity test `tests/test_round80_team_roster_includes_new_managers.py::test_default_fallback_matches_json_roster_byte_for_byte` will fail loud if they drift again. The R80 manager set is `Dee Kindrick / Brian Frazier / Paresh Jadhav / Mithun Sakthivel Subramanian / Shams / All Managers` (42 direct reports total). `(name, email)` pairs that move between managers MUST be removed from the old block AND added to the new block — a roster row appearing twice (under both old and new manager) breaks `_slice_by_owner_or_account` and double-counts the report's metrics.
- Admin "Back to AdoptIQ" link routes through live URL helper (Round 80 / Build 56): the `enhanced_admin_dashboard_v2.py::render_template_string(ENHANCED_ADMIN_TEMPLATE_V2, ..., main_app_url=_live_main_url(), ...)` call site MUST pass `_live_main_url()` (re-reads `ADOPTIQ_MAIN_URL` from `os.environ` per call), NOT the module-level `MAIN_APP_URL` constant captured at import time. The R44/Phase 8 helper already covers HTTP fetches; R80 extends the same defense to the rendered anchor href. Pre-R80 the .app running on a non-default port rendered the link pointing at `http://localhost:5151` (the import-time default) instead of the live port — this was Brian Frazier's reported bug. New admin template-context bindings that reference the main app URL MUST go through `_live_main_url()` for the same reason. Pinned by `tests/test_round80_admin_back_link_uses_live_url.py` (3 tests).
- OneDrive shared-folder-only discovery (Round 80 / Build 56): `config._csone_onedrive_candidates()` MUST return ONLY paths that terminate in the canonical SharePoint shared-folder shortcut leaf `Jeffrey Story (jestory) - AdoptIQ_CSOne_Reports`. Pre-R80 the function probed four owner-only paths under `OneDrive-Cisco/AI Projects/AdoptIQ_CSOne_Reports` — a tree only the corpus owner could sync, so non-owner users (everyone except Jeffrey) couldn't connect. R80 narrows discovery to two candidates (modern macOS `Library/CloudStorage/OneDrive-Cisco/<leaf>` + pre-Big-Sur `~/OneDrive - Cisco/<leaf>`) so every install — including Jeffrey's, who now dogfoods the same "Add shortcut to OneDrive" workflow — exercises the same code path. The two source-of-truth constants `_R80_CORPUS_OWNER_DISPLAY` and `_R80_CORPUS_FOLDER_NAME` define the leaf; if owner identity ever changes (org transfer, AD display-name change), patch the constants and ship a new build. **AdoptIQ never walks the broader `OneDrive-Cisco/` tree** — users don't have to grant filesystem permission to their personal OneDrive contents, only to the synced shortcut. The `CSONE_ONEDRIVE_FOLDER` env override still wins for power users who pin a non-canonical local path. Panel messaging in `static/js/intel_status.js` instructs users to "Open the AdoptIQ corpus folder in SharePoint and click 'Add shortcut to OneDrive'" — that wording MUST be present in the `baked_not_synced` / `fresh_not_synced` / `blocked_no_onedrive` panel detail messages so users know how to remediate. Pinned by `tests/test_round80_onedrive_shared_folder_only.py` (6 tests).
- Local outputs corpus source + Downloads retirement (Round 102 / Build 70): `corpus_bootstrap._resolve_index_sources()` MUST register `local_outputs` after `onedrive` and MUST NOT register `user_downloads` under any configuration. `Config.CSONE_INCLUDE_USER_DOWNLOADS` is now a hard `False` compatibility constant: even stale env / bundled settings with `CSONE_INCLUDE_USER_DOWNLOADS=true` cannot re-enable `~/Downloads` indexing, because macOS surfaces that as an alarming Files-and-Folders prompt. Generated reports enter the corpus through the sidecar-gated `local_outputs` source rooted at the shared report-output resolver, and user-supplied files belong in the explicit `intel_uploads` drop folder (`Config.CSONE_INTEL_UPLOADS_FOLDER`, populated by `/api/intel/upload`). New corpus source code MUST NOT touch `Path.home() / "Downloads"` or `CSONE_USER_DOWNLOADS_DIR`; pinned by `tests/test_round80_local_outputs_corpus_source.py::test_user_downloads_env_true_is_ignored_in_round_102`.
- Outputs reorganized by `<manager>/<report_type>/` (Round 81 / Build 57): all five report writers (`run_compact_analysis`, `run_customer_renewal_analysis`, `run_comprehensive_analysis`, `run_leader_report_generation`, `run_subscription_analysis`) MUST resolve their target directory via `_r81_resolve_report_output_dir(manager, report_type, customer=None)` rather than dropping files directly into `_APP_SUPPORT/outputs/`. The resolver returns `<outputs>/<manager>/<report_type>/` for multi-customer reports (Compact/Renewal/Comprehensive/Leader) and `<outputs>/<manager>/Customer/<customer>/` for per-customer reports (subscription analysis + per-customer renewal). Path segments are sanitised through `_r81_sanitize_path_segment` (strict `[A-Za-z0-9._-]` allow-list, spaces folded to `_`, capped at 80 chars, defaults to `_Unknown`) so user-supplied manager / customer / tech names cannot escape the outputs root. The `LeaderReportGenerator.generate_leader_report` instance method AND the module-level `generate_leader_report` function in `leader_report_generator.py` accept an `output_dir: Optional[Path] = None` kwarg — when provided the writer uses it directly; when `None` it falls through to the legacy `_ensure_outputs()` flat path so existing internal call sites (the test harness, the standalone CLI runner) keep working unchanged. The download endpoints (`/download-file/<filename>` and the `_resolve_safe_path` helper behind `/download/<id>/<type>`), `scan_historical_reports`, and `_get_intel_chips` MUST use `Path.rglob` instead of `Path.glob` so files in nested subdirs are still findable. A one-shot, idempotent migration helper (`_r81_migrate_flat_outputs_if_needed`) runs at startup, parses legacy filenames against the canonical `AdoptIQ_Report_<Type>_<Manager>_<Tech>_<Days>d_<Timestamp>.<ext>` pattern (longest-prefix match against the team roster handles multi-token managers like `Mithun_Sakthivel_Subramanian`), and writes a `.r81_migrated` sentinel so subsequent launches no-op. Parse failures route to `<outputs>/_Unknown/_Unknown/<filename>` rather than silently re-categorise. **New report writers MUST go through the resolver — never hardcode `os.path.join(OUTPUT_FOLDER, filename)` again** (the layout invariant is what makes the per-manager grouping useful + what keeps the corpus indexer's recursive walk reachable). Pinned by `tests/test_round81_outputs_per_manager_layout.py` (~19 tests).
- Recursive corpus indexing for `local_outputs` only (Round 81 / Build 57, narrowed Round 102): `corpus_indexer.enumerate_user_report_files(folder, *, recursive=False)` accepts a `recursive` keyword argument. `corpus_bootstrap._resolve_index_sources()` passes `recursive=True` ONLY for the `local_outputs` source (the AdoptIQ-generated reports under `_APP_SUPPORT/outputs/<manager>/<type>/` after the R81 layout change) so reports in nested subdirs are still ingested. User-controlled sources such as `intel_uploads` MUST stay non-recursive unless a future round adds a separate consent and validation model.
- SharePoint share-token refresh (Round 81 / Build 57): `Config.ADOPTIQ_CORPUS_SHARE_URL` rotated from `e=O3a4Ij` to `e=kpHMgs` (the share-token Brian and the team now use). The path segment `personal/jestory_cisco_com/Documents/AI%20Projects/AdoptIQ_CSOne_Reports` is unchanged; only the trailing query-string token rotates. `Config.ADOPTIQ_SHAREPOINT_FOLDER_URL` continues to be a back-compat alias resolving to the same string per the Round 35 contract. **Future share-token rotations MUST update `tests/test_round81_sharepoint_url_refresh.py` AND `tests/test_round35_corpus_url_hardcoded.py` together** — the R35 test pins the canonical default and the R81 test guards against accidental rollback to the pre-R81 token. Pinned by `tests/test_round81_sharepoint_url_refresh.py` (3 tests).
- Account-assignment correctness — secondary-CSSM attribution (Round 82 / Build 58 / Phase A): `adoptiq_backend.get_subscriptions_for_team(ctx, emails)` MUST consult BOTH the primary email columns (`_R82_PRIMARY_DSM_EMAIL_COLUMNS = ("PRIMARY_DSM_EMAIL", "CSSM_EMAIL", "ASSIGNEE_EMAIL", "OWNER_EMAIL")`) AND the conservative secondary candidate set (`_R82_SECONDARY_DSM_EMAIL_CANDIDATES = ("SECONDARY_DSM_EMAIL", "SECONDARY_CSSM_EMAIL", "BACKUP_DSM_EMAIL", "BACKUP_CSSM_EMAIL", "DELEGATE_DSM_EMAIL", "DELEGATE_CSSM_EMAIL", "OWNER_EMAIL_2", "OWNER_EMAIL_BACKUP", "CSSM_EMAIL_2", "DSM_EMAIL_2")`). The function runs ONE chunked `IN`-clause query per matching column (each parameterised with `%s` placeholders, no string-concat), `pd.concat`s the results, and `drop_duplicates(subset=[..., "CSSM_EMAIL"], keep="first")` so the primary-attributed row wins ties (the R82 ordering — primary frames first in the concat — is what makes the dedup deterministic). The DataFrame's `.attrs["_r82_team_subs_diag"]` carries `{primary_email_col, secondary_email_cols, primary_row_count, secondary_row_count, total_after_dedup, duplicates_dropped}` so the operator can see at a glance whether secondary columns actually contributed (pre-R82 a CSSM with all their accounts assigned in `SECONDARY_DSM_EMAIL` would be invisible to the report). **The pre-R82 single-column query is preserved as the fallback path when no primary column resolves** so a future schema rename to `OWNER_EMAIL` doesn't lock the report out — the new `/api/diag/dsm-columns` admin endpoint is the operator escape hatch in that case. New code paths that consume DSM data MUST go through `get_subscriptions_for_team` rather than rolling their own `SELECT ... WHERE PRIMARY_DSM_EMAIL IN (...)` — the secondary-column fan-out is the source of truth for "who owns which subscription," and a parallel raw query would silently re-introduce the dropped-account bug. The diag rollup is persisted on `analysis_status['team_subs_diag']` via `_r82_persist_team_subs_diag(status, team_subs_df)` from THREE call sites (customer renewal path L13637, comprehensive path L15509, leader path L26554) so the supervisor harness sees per-run attribution accuracy. Pinned by `tests/test_round82_secondary_cssm_attribution.py` (22 tests covering the candidate tuples, `introspect_dsm_columns`, the UNION-with-dedup query, the diag attachment, the persistence helper, and the `/api/diag/dsm-columns` endpoint with dual-auth).
- DSM column-discovery diagnostic (Round 82 / Build 58 / Phase A): `adoptiq_backend.introspect_dsm_columns(ctx)` MUST return `{table, columns, primary_email_columns_present, secondary_email_candidates_present, all_email_like_columns, ok, error_kind}` — the canonical truth for "what email columns does the LIVE DSM table actually have." The new admin endpoint `GET /api/diag/dsm-columns` (loopback-only, dual-auth: CSRF token OR `X-AdoptIQ-Internal`) connects via `_connect_with_keeper`, calls the helper, and returns the JSON payload verbatim so an operator who suspects an account-assignment miss can confirm at a glance whether the column they EXPECT (e.g. a freshly-renamed `BACKUP_DSM_EMAIL`) is actually present. The endpoint MUST return 503 when the Snowflake harness fails to connect AND 200 with `ok=False` + `error_kind` populated when the introspection itself fails (so the response shape stays stable for the JS consumer). **Future Snowflake-schema-discovery endpoints SHOULD follow the same pattern** — bail with 503 only on connect failure, otherwise return 200 with a structured `ok=False` so the admin UI can render an honest error tile instead of a generic "diagnostic failed." Pinned by `tests/test_round82_secondary_cssm_attribution.py::test_diag_endpoint_*` (4 endpoint tests).
- Per-source citation taxonomy (Round 82 / Build 58 / Phase B): `report_source_injector._R82_KPI_SOURCE_TAGS` is the SSoT for mapping canonical KPI keys (the SAME alias map `report_iteration_loop.KPI_ALIASES` exposes — re-used via the existing `_gate_canonical_kpi_label` helper) to specific source-system tags. Current taxonomy: `Snowflake CSConsole` for AB / AP / Pulse / Success Priorities; `Snowflake CSOne` for Support Cases / TAC / BEMS / escalations; `Snowflake EDW Sales DSM` + `Snowflake EDW Sales subscriptions` for Team Members / Total Customers; `AdoptIQ risk_scoring` for risk score + risk category; `AdoptIQ canonical_metrics` for high-risk customer counts; `AdoptIQ external_intelligence` for incidents; `AdoptIQ team_config` + `AdoptIQ analysis scope` for manager / technology / window metadata. The injector's three injection sites (paragraph multi-match, paragraph single-match, table cell — both two-column rows AND multi-column-header columns) MUST resolve their citation chrome through `_r82_chrome_for_label(label, fallback)` (single label) or `_r82_chrome_for_paragraph(matches, fallback)` (multi-match paragraph — returns the specific chrome ONLY when all canonical matches share a source tag, otherwise falls back to generic). **Mixed-source paragraphs deliberately fall back to the generic chrome** so we don't render multiple DIFFERENTLY-tagged citations on a single line and re-introduce visual mess (the user's R82 acceptance criterion: "verified cited sources... but not to the point where its messy in the report"). New canonical KPIs added to `KPI_ALIASES` SHOULD also get a taxonomy entry (absence falls back to generic chrome — no harm done, only the authenticity upgrade is missed); future fan-outs into new source systems (e.g. ServiceNow tickets, Salesforce CRM data) can append entries here without touching the placement logic. Pinned by `tests/test_round82_per_source_citation_taxonomy.py` (44 tests covering taxonomy shape + parametrised label resolution + chrome construction + paragraph-level resolver + end-to-end DOCX injection per-label specificity + R66/B1 unit-deferral regression guard + R76 paren-cluster regression guard + idempotency + source-shape markers).
- OneDrive sign-in proxy + one-click corpus bootstrap (Round 83 / Build 59): `corpus_bootstrap._r83_onedrive_signed_in_proxy()` is the canonical "is the OneDrive desktop client signed in to ANY account" probe — distinct from the per-folder `_check_onedrive_sync_status()`. macOS implementation globs `~/Library/CloudStorage/OneDrive-*` AND `~/OneDrive - *` looking for a `OneDrive-Cisco` / `OneDrive - Cisco` substring (any Cisco tenant root → `signed_in_cisco`); other-tenant roots → `signed_in_other`; nothing → `not_signed_in`. Windows implementation probes `HKCU\Software\Microsoft\OneDrive\Accounts` via `winreg` (CISCO substring in `Business1.UserEmail` → cisco; otherwise `signed_in_other`); failure / non-Win32 → `unknown`. The probe MUST be wrapped `try/except OSError` with `unknown` as the defensive fallback so a hostile filesystem permission error never raises into `_run_index_pass`. **A new `signed_in_no_corpus` source label fires from `_run_index_pass` when `signed_in_proxy == "signed_in_cisco"` BUT (`onedrive_status != "synced"` OR sentinel absent)** — distinct from the legacy `blocked_no_onedrive` (which now means "not signed in at all OR signed in to a non-Cisco tenant"). The frontend classifier (`static/js/intel_status.js::classifyCorpusPanel`) checks `signed_in_no_corpus` BEFORE `blocked_no_onedrive` so precedence is deterministic; `corpusPanelLabel` returns `"Add corpus share to OneDrive"`, `corpusPanelPillClass` returns `"bg-warning text-dark"`, `corpusPanelDetail` emits the `"Add corpus share to my OneDrive"` CTA copy. `paintDeepLink` unhides the deep-link button on BOTH blocked states with a state-specific button label (`"Add corpus share to my OneDrive"` for `signed_in_no_corpus`, `"Open AdoptIQ corpus folder in OneDrive"` for `blocked_no_onedrive`) — the rewrite uses `textContent` (XSS-safe), NEVER `innerHTML`. The new `GET /api/corpus/bootstrap-shortcut` endpoint returns the SharePoint share URL after validation through `_r83_safe_share_url()` (allow-list `_R83_SHARE_URL_SCHEMES = ("https://",)` — narrower than R53's deep-link list because the bootstrap UX must be SSL-protected end-to-end; cap at `_R83_SHARE_URL_MAX_BYTES = 2048`). The daily refresh worker's `_next_refresh_tick_s` MUST include `signed_in_no_corpus` in the blocked-state disjunction alongside `blocked_no_onedrive` so the accelerated 30-second tick fires for BOTH states; `_daily_refresh_loop`'s `blocked_now` disjunction matches. **Tests that exercise R53 `blocked_no_onedrive` paths on dev hosts that DO have CloudStorage/OneDrive-Cisco populated MUST monkeypatch `_r83_onedrive_signed_in_proxy` to `"not_signed_in"` in their autouse fixture** — otherwise R83 short-circuits to `signed_in_no_corpus` and the legacy R53 branch never runs. Pinned by `tests/test_round83_onedrive_signed_in_proxy.py` (17 tests), `tests/test_round83_signed_in_no_corpus_panel.py` (19 tests), `tests/test_round83_candidate_list_owner_fallback.py` (11 tests), `tests/test_round83_bake_owner_fallback.py` (10 tests), `tests/test_round83_daily_worker_bootstrap_trigger.py` (7 tests).
- Owner-style OneDrive path tier 3-4 fallback (Round 83 / Build 59): `config._csone_onedrive_candidates()` returns FOUR paths (was 2 in R80) — tiers 1-2 are the canonical R80 shared-folder leaf (`~/Library/CloudStorage/OneDrive-Cisco/Jeffrey Story (jestory) - AdoptIQ_CSOne_Reports` + the pre-Big-Sur variant); tiers 3-4 are the new R83 owner-style fallback (`~/Library/CloudStorage/OneDrive-Cisco/AI Projects/AdoptIQ_CSOne_Reports` + the pre-Big-Sur variant) for the corpus owner's machine where the OneDrive desktop client routes the shortcut back to the personal `AI Projects` tree (the canonical leaf doesn't materialise on the owner's box). **R80's narrowing intent is preserved** — tiers 1-2 still hit first for the 99% non-owner case; tiers 3-4 are purely defense-in-depth. `scripts/bake_corpus.py::_resolve_source_dir` and `_resolve_onedrive_sentinel_root` MUST explicitly walk `_csone_onedrive_candidates()` and return the first existing-directory candidate (CLI flags + env vars still take precedence) so the corpus owner's machine resolves automatically without `ADOPTIQ_BAKE_FIXTURE_DIR` / `ADOPTIQ_BAKE_SENTINEL_ROOT` overrides. `build_mac_dmg.sh` echoes the autodetected path during bake AND emits enhanced error guidance pointing the operator at the four canonical candidate paths if the bake fails. **Future OneDrive-discovery code paths MUST go through `_csone_onedrive_candidates()` rather than hardcoding a path** — the 4-tier ordering is the SSoT and a parallel hardcoded list would silently miss the owner-style fallback. `tests/test_round80_onedrive_shared_folder_only.py` was widened to accept the 4-candidate shape while preserving the R80 narrowing assertions (no `Documents/AI Projects` nesting, R80 tiers terminate in shared-folder leaf). Pinned by `tests/test_round83_candidate_list_owner_fallback.py::test_candidate_list_returns_four_entries` + `tests/test_round83_bake_owner_fallback.py` (5 source-dir + 5 sentinel-root tests).
- **Operator-configurable corpus share URL (Round 84 / Build 60)**: the SharePoint share URL is now a 3-tier resolved value via `corpus_share_url_resolver.get_active_corpus_share_url()` rather than a hardcoded constant. Precedence highest-first: `settings.json` (`corpus_share_url`, allow-listed in `adoptiq_settings._SCHEMA`, validated by `_is_valid_sharepoint_url`) → `os.environ["ADOPTIQ_CORPUS_SHARE_URL"]` → `Config.ADOPTIQ_CORPUS_SHARE_URL` (the hardcoded default). Each tier is independently re-validated; an invalid value at any tier silently falls through to the next so a corrupted settings.json or a typo'd env var never returns a malformed URL. The resolver does NOT cache — a UI flip on the analyze-page card takes effect on the very next call (mirrors the R69 model_resolver no-caching contract). New endpoints: `POST /api/settings/corpus-share-url` (CSRF dual-auth via `_r17_2_authorize_corpus_admin`, validates via `adoptiq_settings.is_valid_sharepoint_url`, persists via `save_settings` with atomic `os.replace` at mode `0o600`, empty-string clears the override) and `GET /api/settings/corpus-share-url` (returns `{share_url, source, persisted_value, env_var, env_value_set}`). The analyze-page `[data-corpus-share-url-card]` lets the operator paste, Test (always reads from the SAVED URL via `/api/corpus/bootstrap-shortcut` — never the raw input, so an unvalidated URL cannot be launched), Save, or Clear override. `static/js/corpus_share_url.js` is wrapped in an IIFE, exports `bindCorpusShareUrlSave` / `paintCorpusShareUrlCard` / `fetchCorpusShareUrlCard` on `window.AdoptIQCorpusShareUrl`, and uses `textContent` (NEVER `.innerHTML`) for the "Current value" rendering so a malicious URL persisted via hand-edit cannot bootstrap stored XSS. **R83's `_r83_safe_share_url` keeps its independent https-only + 2048-byte cap as defense-in-depth**, so even a future resolver bug cannot let a non-https value through. **Future endpoints that accept a SharePoint URL MUST validate via `adoptiq_settings.is_valid_sharepoint_url(...)` AND go through `corpus_share_url_resolver.get_active_corpus_share_url()` for read paths** — silently writing an unvalidated URL would land an honest 400 in production but only a deferred error after the next bootstrap-shortcut click (the R67 stale-binary trap class of bug). Pinned by `tests/test_round84_corpus_share_url_setting.py` (22 tests) + `test_round84_corpus_share_url_endpoint.py` (17 tests) + `test_round84_corpus_share_url_resolver.py` (12 tests) + `test_round84_corpus_share_url_ui_source_shape.py` (21 tests).
- **R106 / Build 75 temporary corpus availability rollback (superseded by R107/R108)**: the R96 OneDrive-auth-as-access-gate was relaxed for acceptance. Build 76 explicitly changed the release model by restoring prebaked corpus artifacts in the DMG, and Build 77 keeps that baseline while smoothing updates. `corpus_bootstrap._run_index_pass` opens the encrypted corpus with `allow_local_sentinel=True`, skips the OneDrive/sentinel hard stop, and indexes generated AdoptIQ reports, Intelligence uploads, and OneDrive data when that folder is present. Missing OneDrive/sentinel now only affects diagnostics and source coverage; it MUST NOT block admin refresh/reset or first-launch Ask AI corpus availability. New corpus UI copy MUST say the prebaked/local corpus is available and that OneDrive is optional refresh context.
- **Round 85 / Build 61 — share URL is a `:f:/p/` guest-pass; legacy render-link shape regression-guarded**: the canonical shipping default `Config.ADOPTIQ_CORPUS_SHARE_URL` is now `https://cisco-my.sharepoint.com/:f:/p/jestory/IgBm46pU_P9aTpkxyEQ13ZgYAT4BhGVKNfcUsN7DA7zkRJI` — a `:f:/p/` guest-pass URL where the path segment IS the share token (no query string, no rotating `e=...` parameter). This is fundamentally different from the R35 → R81 → Build 60 shape (`:/r/personal/jestory_cisco_com/Documents/AI%20Projects/AdoptIQ_CSOne_Reports?csf=1&web=1&e=<token>`) which carried a rotating token in the query string. Guest-pass URLs are bearer-shareable on the URL itself; the render-link shape required recipient SSO via the SharePoint site. **Both shapes resolve to the same Cisco-managed share, so the R83 OneDrive-auth-as-access-gate contract is unchanged** — the URL is still NOT a secret because the encryption sentinel is the actual access gate. The `tests/test_round81_sharepoint_url_refresh.py` regression guard (renamed-in-place to round85_* test functions) rejects EVERY legacy fragment we have ever shipped: `e=O3a4Ij` (R35), `e=kpHMgs` (R81), `/personal/jestory_cisco_com` (legacy path), `csf=1` (legacy query param), AND `:/r/` (legacy route prefix). Future rotations require a new path entirely (the path IS the share token in this URL shape), so a token-only refresh helper does not apply — when the next rotation lands, update `Config.ADOPTIQ_CORPUS_SHARE_URL` AND the three URL pins in `tests/test_round35_corpus_url_hardcoded.py::_EXPECTED_DEFAULT`, `tests/test_round81_sharepoint_url_refresh.py::_EXPECTED_FULL_URL`, AND `tests/test_round85_url_refresh_and_preferences_card.py::_R85_NEW_URL` together (cross-pin tests will fail loudly if any pair drifts). The R84 analyze-page corpus-share-URL card is now also mirrored on the Preferences hub (`templates/preferences.html`); both pages bind to the same `[data-corpus-share-url-card]` data marker via the existing `static/js/corpus_share_url.js` IIFE module — no second JS handler. Pinned by `tests/test_round85_url_refresh_and_preferences_card.py` (~19 tests covering validator allow-list, resolver precedence, `_r83_safe_share_url` integration, Preferences-hub source-shape, analyze-page regression guard, cross-pin against R35/R81 fixtures).
- **Round 86 / Build 62 (P0/F1) — Renewal score parity uses explicit 0-10 + 0-100 keys, no scale-guessing heuristic**: `app_simple.py` `renewal_summary_data` loop MUST read `cust_analysis.get('renewal_risk_score_10')` (already on 0-10 scale, written by `_calculate_simple_renewal_risk` from `profile["risk_score_0_10"]`) into `Overall_Risk_Score` and `cust_analysis.get('renewal_risk_score')` (0-100 scale, passthrough of `profile["risk_score_0_100"]`) into `Risk_Score_0_100`. The pre-R86 R67/B1 normalisation block used a heuristic (`if _r67_orig_f > 10.0`) to guess whether the input was 0-10 or 0-100 -- this MIS-CLASSIFIED a 0-100 input as a 0-10 input when the value happened to be ≤10.0, then multiplied by 10 to publish a saturated `Risk_Score_0_100` (e.g. healthy customer with 0-100 score of 7.1 was rendered as 71.0 in the workbook AND `Overall_Risk_Score=7.1` in the docx). The R67/B1 block was rewritten to honor the explicit fields and only handle (a) legacy single-customer paths where the explicit keys are missing, and (b) `Risk_Level` MEDIUM->MODERATE remapping. **New report formats that publish per-customer Renewal scores MUST follow this template: route through `compute_customer_risk_profile`, emit BOTH `risk_score_0_10` AND `risk_score_0_100` from the analyzer, project the explicit keys into the workbook (NOT a guessed-scale single value), remap `MEDIUM → MODERATE`. The R66/B8 incident-threading + R67/B1 cross-format parity contracts continue to apply.** Pinned by `tests/test_round86_renewal_score_parity.py` (7 tests including a negative-control source-shape pin that asserts the buggy `if _r67_orig_f > 10.0` pattern is absent from `app_simple.py`).
- **Round 86 / Build 62 (P0/F2) — `CSConsole_Action_Plans` curated through the same schema as `Action_Plans`**: `report_export_schema.CURATED_COLUMNS` now maps `"CSConsole_Action_Plans" -> _CURATED_ACTION_PLANS` (parity with `"Action_Plans"` and `"Customer_Action_Plans"`). Pre-R86 the Comprehensive XLSX shipped `Action_Plans` at the canonical 30 cols (curated since R67/B7) but `CSConsole_Action_Plans` was a raw 242-col Snowflake dump leaking `CSDF_SYNC_ID_C`, `GS_C_360_SUCCESS_PRIORITY_C`, `IS_DELETED`, `MAY_EDIT`, `MUTE__C`, `OWNER_ID_C`, and ~210 other internal markers. The two sheets carry the SAME row set (same dedupe, same scope-filter); the difference was purely the column projection. **When adding a new optional sheet to `run_comprehensive_analysis` that ingests Snowflake-sourced rich-text fields, the sheet name MUST be added to `report_export_schema.CURATED_COLUMNS` -- the audit script's "internal markers" P0 tag (matching `IS_DELETED|MAY_EDIT|MUTE__C|CSDF_SYNC|_SYNC_ID|GS_C_|SFDC_|ETL_`) catches violations of this contract.** Pinned by `tests/test_round86_csconsole_action_plans_curate.py` (8 tests).
- **Round 86 / Build 62 (P1/F3) — internal AdoptIQ filenames suppressed in Historical Context citations**: `report_corpus_context._is_internal_adoptiq_filename` matches `^AdoptIQ[_-]<segments>\.<ext>$` (case-insensitive, whitespace-tolerant) for the standard AdoptIQ output filename shapes (`AdoptIQ_Report_*.docx`, `AdoptIQ_Data_*.xlsx`, `AdoptIQ_Portfolio_*.xlsx`). `_safe_source_label` returns `None` for matches, which the two render paths (`render_to_text`, `render_to_word`) MUST honor by suppressing the `[src: <filename>]` tag entirely. Pre-R86 a corpus rebuilt from prior AdoptIQ daily reports surfaced `[src: AdoptIQ_Report_<scope>_<ts>.docx]` tags in customer-facing Word narratives -- the user saw their own internal report filenames quoted in their new report. The Historical Context section header already attributes the data to "the AdoptIQ Knowledge Corpus (prior daily reports)", so the filename adds no information. External / non-AdoptIQ filenames (e.g. `CSOne_export_2026-04-15.csv`, user-uploaded snapshots) continue to render normally so operator-meaningful citations are preserved. The "Source files:" footer line at the bottom of each customer entry ALSO suppresses AdoptIQ filenames because `_safe_source_label` is called at construction time in `build_historical_context` -- they never reach either render path. Pinned by `tests/test_round86_internal_filename_suppression.py` (8 tests).
- **Round 96 / Build 69 — release-time gate forbids baked corpus data**: `build_mac_dmg.sh` carries an opt-in `ADOPTIQ_RELEASE_GATE=1` block after `scripts/bake_corpus.py --no-bake` and before PyInstaller. When active, the build hard-fails if `bake/.bake-skipped` is missing OR if `bake/corpus.db.enc` / `bake/corpus.db.salt` are present. Default `${ADOPTIQ_RELEASE_GATE:-0}` keeps dev iteration permissive, but CI / shipping pipelines that produce DMGs SHOULD export `ADOPTIQ_RELEASE_GATE=1` so a future operator cannot accidentally ship corpus data. The `.bake-skipped` marker proves the skip/scrub path ran; the app waits for OneDrive and builds the runtime corpus after launch. Pinned by `tests/test_round87_release_gate_corpus_bake.py` (rewritten in Round 96 for the inverted gate) and `tests/test_round53_spec_no_sentinel.py`.
- **Round 96.1 / Build 69 — runtime-only corpus state and proxy guardrails**: a successful runtime index MUST clear stale blocked source labels by setting `_STATE.source = "fresh"` in the success branch after `handle.commit_to_disk()`. This is intentionally not applied to crypto/indexer/commit failures. The admin proxy bypass guard MUST gate both `blocked_no_onedrive` and `signed_in_no_corpus`, matching the template-disabled buttons. `/api/corpus/bootstrap-shortcut` is read-only but still operator bootstrap metadata, so it MUST stay in `_SENSITIVE_ENDPOINTS` with the rest of the corpus surface. Pinned by `tests/test_round54_f1_toctou_blocked_no_onedrive.py`, `tests/test_round54_f3_admin_reset_gate.py`, and `tests/test_round71_sensitive_endpoints_complete.py`.
- **Round 97 / Build 69 — TACTrack-style startup splash parity, amended Round 99**: macOS builds MUST preserve the PyInstaller binary as `Contents/MacOS/AdoptIQ.bin` and install a shell launcher at `Contents/MacOS/AdoptIQ` that opens a temp startup splash, polls `GET /ping`, exports `ADOPTIQ_LAUNCHER_SPLASH_SHOWN=1`, starts the binary in the background with `nohup`, and exits the wrapper. Round 99 intentionally replaced the old `exec` path because a browser-only Flask app can keep Finder/Launch Services in a bouncing Dock state when the long-running server remains the foreground app process. The Python side MUST keep `_open_browser_url`, `_launcher_splash_already_shown`, `_startup_splash_html`, `_open_startup_splash`, and the plain-text `/ping` route available. The launcher marker suppresses the delayed Python browser opener so users do not get duplicate tabs. Do not copy TACTrack's `os._exit` shutdown or baked-corpus behavior; AdoptIQ keeps SIGTERM shutdown, stale-instance auto-quit, admin autostart, and runtime-only corpus indexing. Pinned by `tests/test_round97_startup_splash.py`.
- **Round 97.2 / Build 69 — report harness startup preflight and warning-log quieting**: `report_iteration_loop.evaluate_app_health` MUST preflight `/ping`, `/api/version`, `/api/status/all`, `/api/corpus/status`, and `/api/intel/status` before starting live report scenarios; the summary JSON MUST include `app_health` and `app_health_gate` so a failed live run distinguishes app readiness from report-generation defects. `scripts/test_build_smoke.sh` defaults to `dist/AdoptIQ.app` (falling back to `OUTBOX/AdoptIQ.app`) and MUST check `/ping`, `/api/version`, `/api/status/all`, and `/api/corpus/status` before clean shutdown. Warning-level embedder fallback logs in `ask_ai_embeddings.py` MUST route through `_logging_helpers.safe_log_warning` so pytest/app shutdown races do not leak `I/O operation on closed file` tracebacks. Pinned by `tests/test_round51_report_iteration_loop.py`, `tests/test_round63_logging_helpers.py`, and `tests/test_round97_startup_splash.py`.
- **Round 99 / Build 69 — runtime corpus stale-crypto recovery, amended Round 106**: `corpus_bootstrap._run_index_pass` MAY recover from a `CorpusCryptoError` by calling `_preserve_broken_corpus(encrypted_path.parent)` and retrying `open_corpus_for_user(..., create_if_missing=True, allow_local_sentinel=True)`. Round 106 no longer converts missing OneDrive/sentinel into a blocked state; stale artifacts are preserved and the retry uses the local sentinel fallback. Retry failure still stays loud with `last_error_kind="crypto"`. Pinned by `tests/test_round39_self_heal_crypto_failure.py::test_round99_runtime_crypto_failure_preserves_and_retries` and `::test_round99_runtime_crypto_retry_failure_stays_loud`.
- **Round 101 / Build 69 — live report soak supervisor and Compact high-risk scale contract**: `scripts/run_report_soak.py` is the SSoT for unattended live report soak runs. It MUST preflight `/api/status/all`, refuse to stack on active running reports unless explicitly allowed, run one `report_iteration_loop.py --iterations 1 --strict --stop-on-failure` child per scenario until the time budget is exhausted, preserve each child stdout/stderr and summary JSON, and write `soak_summary.json`. Use `--baseline-mode manifest` only with a current manifest; when historical manifests have live-data drift, use `--baseline-mode off` for operational stability soaks while preserving strict generation/download/quality checks. Compact declares `risk_summary["high_risk_scale"] = cm.RISK_SCALE_0_TO_10`, `executive_intelligence_formatter` must preserve that key when rebuilding `portfolio_metrics`, and `report_consistency.validate_report_consistency` must honor the declared scale so the gate validates the same high-risk count rendered into the Compact Word report. Pinned by `tests/test_round101_report_soak.py`.
- **Round 102 / Build 70 — Downloads corpus source retired**: `corpus_bootstrap._resolve_index_sources()` MUST NOT add a `user_downloads` source, and `Config.CSONE_INCLUDE_USER_DOWNLOADS` MUST remain `False` even if the environment says `true`. This prevents installed macOS apps from prompting end users for Downloads-folder access during corpus refresh. Generated reports are indexed through sidecar-gated `local_outputs`; explicit user-uploaded corpus files use `intel_uploads`. User-facing copy in Customer 360, Historical Context, and the corpus panel MUST NOT describe Downloads as a fallback. Pinned by `tests/test_round80_local_outputs_corpus_source.py::test_user_downloads_env_true_is_ignored_in_round_102` and `::test_round102_user_facing_copy_no_longer_mentions_downloads_fallback`.
- **Round 103 / Build 71 — launch/report UX cleanup**: `adoptiq_settings.migrate_round103_model_defaults()` is the one-time migration for stale saved `gpt-5-nano` Ask AI/report model preferences. It rewrites exact stale values to `gemini-3.1-flash-lite` and persists `r103_model_default_migrated=True`; after that marker exists, a deliberate UI back-toggle to `gpt-5-nano` MUST continue to resolve normally. `model_resolver` and `Config.CIRCUIT_CONFIG` also map exact stale env/bundled `CIRCUIT_MODEL_NAME*="gpt-5-nano"` defaults to Gemini when no settings override exists. Launch pages MUST keep `[data-report-jobs-panel][data-report-jobs-current-only]` near the top for active jobs only, and completed jobs belong in the lower `[data-report-history-panel]` card with links to `/history` and `/previous-reports`. `templates/leader_report_form.html` MUST NOT reintroduce blocking `alert(...)` on the success path; use the shared jobs panel plus non-blocking notification helper instead. Pinned by `tests/test_round103_ux_cleanup.py`.
- **Round 103.1 / Build 72 — Report Jobs theme cleanup**: `static/js/report_jobs_dashboard.js` MUST NOT use Bootstrap row variants like `table-primary` for active jobs. Active rows use the scoped `adoptiq-report-job-active` class, and `templates/base.html` owns the scoped jobs/history table CSS under `[data-report-jobs-panel]` and `[data-report-history-panel]` using existing theme tokens (`--bg-surface`, `--bg-surface-raised`, `--border-subtle`, `--text-primary`, `--text-muted`, `--accent-primary`). This preserves the Round 103 current-jobs/history layout while keeping the visual scheme dark/orange instead of light gray/blue. Pinned by `tests/test_round103_ux_cleanup.py::test_round103_jobs_dashboard_uses_theme_tokens_not_bootstrap_gray_rows`.
- **Round 104 / Build 73 — Compact incident-scoring stability from live report audit**: `compact_report_formatter.calculate_renewal_risk_scores` MUST NOT import `app_simple` to filter per-customer incidents. The Round 104 local helper `_r104_filter_customer_tagged_incidents` preserves the R65 behavior (portfolio-wide Webex Status incidents pass through when no customer-tag fields exist; tagged future sources filter by `customer_id` / `customer_name` / `BU_NAME`) without triggering `app_simple` top-level startup side effects. The Compact XLSX `generate_excel` worker in `app_simple.py` captures `_r104_ext_incidents=ext_incidents` in its default args and passes that captured list into both primary and fallback risk-score calls, so Compact/Renewal parity keeps the same incident input even inside the threaded writer. Pinned by `tests/test_round67_compact_renewal_score_parity.py::test_round104_compact_incident_filter_does_not_import_app_simple` and `::test_round104_compact_excel_worker_captures_ext_incidents`.
- **Round 105 / Build 74 — live artifact stability audit closeout**: Compact score calculations MUST resolve incidents through `app_simple._r105_compact_incidents_for_scoring(...)` before both threaded Word and XLSX calls to `calculate_renewal_risk_scores`; a missing captured incident list is recovered through the same `fetch_status_incidents(days_back=...)` path instead of silently scoring incidents as zero. Compact `Report_Info` MUST merge `partial_data_warnings` into `Partial_Data_Warning` rows so ACC scope exclusions visible in status/Word are also visible in the workbook. `be_priority_pipeline.build_be_priority_outputs` MUST call `_r105_prepare_ab_for_be_priority` before scoring/classification so raw Leader AB exports (`NAME`, `Comments`, `Age (Days)`, `Customer Name`) populate `title`, `description`, `open_age_days`, and `customer_name`; otherwise Gemini classifies every row as `AMBIGUOUS` due blank prompt fields. Pinned by `tests/test_round67_compact_renewal_score_parity.py::test_round105_compact_incident_recovery_is_wired_into_both_workers`, `tests/test_round67_compact_xlsx_report_info_schema.py::test_round105_compact_report_info_merges_status_partial_warnings`, and `tests/test_round79_b4_be_xlsx_sheets.py::test_round105_leader_raw_ab_columns_feed_be_classifier_prompt`.
- **Round 106 / Build 75 — acceptance blockers**: `data_source_validator._r106_ab_empty_after_technology_scope` treats zero-row scoped AB DataFrames carrying `_apply_scope_filter_ab` tech-filter diagnostics as partial data instead of a missing required AB source, while AB fetch failures and genuinely empty upstream AB data remain fail-loud. `corpus_bootstrap._run_index_pass` must not emit `blocked_no_onedrive` / `signed_in_no_corpus` when OneDrive or the sentinel is absent; it opens with `allow_local_sentinel=True`, preserves stale crypto artifacts, retries with the local sentinel, and lets the daily worker refresh without the R68 sentinel gate. The macOS release DMG must hard-fail if `Unblock AdoptIQ.command` or `READ_ME_FIRST.txt` is missing, and end-user docs must explain the DMG Privacy & Security approval separately from the post-install unblock helper. Pinned by `tests/test_data_source_validator.py`, `tests/test_round53_bootstrap_blocked_no_onedrive.py`, `tests/test_round54_f1_toctou_blocked_no_onedrive.py`, `tests/test_round83_daily_worker_bootstrap_trigger.py`, and `tests/test_round106_mac_gatekeeper_unblock_contract.py`.
- **Round 87 / Build 63 (Phase 2) — corpus security model documented honestly**: `README.md` carries a "Corpus security model (Round 87)" subsection that names BOTH the durable at-rest artifacts (`~/Library/Application Support/AdoptIQ/knowledge/corpus.db.enc` AES-256-GCM ciphertext, mode `0o600`, parent dir `0o700`; `corpus.db.salt` per-install KDF salt) AND the runtime ephemeral plaintext SQLite file (`$TMPDIR/adoptiq_corpus/corpus.<random>.db`, mode `0o600`, parent dir `0o700`). The honest claim is "plaintext is ephemeral, single-user, and scrubbed on clean exit" — NOT "never plaintext on disk" (which is unverifiable because SQLite cannot operate without a real file descriptor). On clean shutdown, `EncryptedCorpusHandle.close` runs a best-effort scrub (zero-write the first 1 MB + `fsync`) and then `unlink`s the temp file; the scrub is registered as an `atexit` handler in `app_simple.py` so it fires on the same code path that saves `analysis_status.json`. Hard crash (`SIGKILL`, power loss) leaves the temp file on disk until `$TMPDIR` clears (macOS clears on every boot; Linux varies). **The OneDrive sentinel + Microsoft tenant ACL are the access gate, NOT the URL** — rotating the share URL (R85, R87 / Phase 4) does NOT weaken the encryption. **Future docs / stakeholder communication MUST use the same honest framing — "ephemeral plaintext, scrubbed on clean exit" — instead of unverifiable claims.** Pinned by `tests/test_round87_corpus_security_doc.py` (4 contract-pin tests).
- **Round 87 / Build 63 (Phase 3) — launcher auto-quits stale older AdoptIQ on upgrade**: `app_simple.py` carries three new module-level helpers (`_query_running_instance_started_at(port)`, `_running_instance_is_stale(port, our_mtime)`, `_force_quit_existing_adoptiq(port, *, pid=None)`) wired into the duplicate-launch branch of the `if __name__ == '__main__':` block. When a frozen build is double-clicked AND the running instance's `process_started_at_utc` (from its own `GET /api/version`) is older than our `Path(sys.executable).stat().st_mtime` (1.0 s tolerance for filesystem mtime resolution + clock drift), the new launcher SIGTERMs the listener pid, polls until the port frees (10 s timeout), and proceeds to boot. Detection is intentionally narrow: frozen-only (`getattr(sys, 'frozen', False)`), any failure (bad port, version probe timeout, malformed JSON, unparseable timestamp) returns False so we fall back to the R38.1 re-route; the kill is `SIGTERM` NOT `SIGKILL` so the running instance's `atexit` handlers fire (`_shutdown_handler` saves `analysis_status.json`, `_r17_corpus_shutdown` scrubs the corpus temp file). The HTTP `/api/shutdown` endpoint is NOT used for the kill because the new launcher process can't share `ADOPTIQ_INTERNAL_TOKEN` cleanly with the running one without a sidecar pidfile + key scheme. **Future launcher logic that detects a stale running instance MUST follow the same SIGTERM-and-poll pattern; the `os._exit(0)` / `SIGKILL` path skips the cleanup contract.** Pinned by `tests/test_round87_launcher_auto_kill_stale.py` (10 source-shape + behavior tests).
- **Round 87 / Build 63 (Phase 4) — corpus share URL card moved to Preferences hub only**: `templates/analyze.html` no longer carries the R84 corpus share URL card or the `static/js/corpus_share_url.js` script tag (the analyze page is for running reports; admin-config belongs on the Preferences hub — mirrors the R73 UX-2 split that moved the report-narrative model picker off this page). The card lives exclusively on `templates/preferences.html` (R85 added it; R87 finishes the migration). The R84 endpoint (`POST /api/settings/corpus-share-url`), JS handler, and JSON contract are unchanged — only the visual surface moved. The R84 UI source-shape test was repurposed in Phase 4 to validate against `preferences.html` instead of `analyze.html`. **Future admin-config surfaces (model picker, share URL, etc.) MUST land on `templates/preferences.html` rather than per-run report tiles like `templates/analyze.html`.** Pinned by `tests/test_round87_card_removed_from_analyze.py` (6 negative-control + destination-side tests) plus the repurposed `tests/test_round84_corpus_share_url_ui_source_shape.py` (21 tests).
- **Round 87 / Build 63 (Phase 5) — corpus indexing UX clarifier**: `templates/analyze.html` carries a small italic clarifier `<p data-intel-indexing-clarifier hidden>` immediately under the AdoptIQ Intelligence panel's `[data-intel-banner-summary]` line. Visible text: "Reports remain safe to run during indexing — only Ask AI grounding waits for the index pass to finish." `intel_status.js::paintBanner` calls `clarifier.removeAttribute('hidden')` when `state === 'running'` (mirrors the SSR `boot.in_progress` conditional that drives the "Indexing CSOne reports..." summary) and `setAttribute('hidden', '')` otherwise so the line never appears outside an active index pass. Build 62 acceptance feedback flagged the operator saw "Indexing CSOne reports..." with no signal that core report generation (Comprehensive, Compact, Renewal, Leader) was unaffected — only Ask AI grounding depends on the corpus index. **Future panel state additions MUST update both the SSR conditional in `analyze.html` AND the JS toggle predicate in `intel_status.js::paintBanner` to keep the user-facing surface consistent across the no-JS, post-load, and steady-state windows.** Pinned by `tests/test_round87_corpus_indexing_ux_subtitle.py` (4 source-shape tests).
- **Round 88 / Build 64 (F1) — markdown asterisk leakage in Word narratives**: `adoptiq_backend.append_to_word_report::clean_and_format_text` MUST process BOTH `**bold**` (Round 16 baseline) AND `*italic*` (Round 88) markdown markers via the new module-level `_r88_italic_re = re.compile(r"(?<![*\w])\*([^*\n]+?)\*(?![*\w])")` regex. The italic pass runs through three branches: (a) when `**bold**` markers are absent, the text is split through `_r88_italic_re.split` so single-star segments emit a `run.italic = True` runs and the literal `*` chrome is stripped; (b) when `**bold**` IS present, italic chrome is stripped from the non-bold segments BEFORE the `**` boundaries are processed so a malformed `**X**` pair followed by `*Y*` doesn't trip the bold path; (c) malformed `**` pairs (odd-count) fall through to a final italic-only sanitisation pass so the user never sees a literal `*` in the docx. The R16 prompt-template scrub also dropped 19 `*...*` instructional wrappers from `PROMPT_COMPREHENSIVE_TEMPLATE`, `PROMPT_CUSTOMER_TEMPLATE`, and `PROMPT_COMPACT_EXECUTIVE_TEMPLATE` so the LLM stops mirroring our own emphasis markup back into the narrative. **New LLM prompts that include emphasis instructions MUST use plain-text instructions (e.g. "Be concise") rather than meta-markup (`*Be concise*`) — the model treats meta-markup as a stylistic cue and mirrors it in the output, which then has to be sanitized.** Pinned by `tests/test_round88_comprehensive_no_markdown_asterisks.py`.
- **Round 88 / Build 64 (F2) — `Risk_Score_0_10` is a first-class column on Compact + Renewal XLSX**: BOTH `Risk_Summary` (Compact) AND `Renewal_Summary` (Renewal) MUST publish a dedicated `Risk_Score_0_10` column adjacent to `Overall_Risk_Score` so an operator parsing the workbook downstream has an unambiguous 0-10-scale column name. The Compact path sources from `_r67_b6_score` (already 0-10); the Renewal portfolio loop sources from `_r86_score_10` (the R86/F1-explicit `renewal_risk_score_10` field on `cust_analysis`); the Renewal single-customer path sources from `overall_risk_score`. The Renewal `_r70_renewal_canonical_cols` tuple inserts `Risk_Score_0_10` between `Overall_Risk_Score` and `Risk_Score_0_100` so the column-order contract from R67/B6 + R70 stays canonical. The legacy `Overall_Risk_Score` (0-10) and back-compat `Risk_Score` (0-10 alias) on Compact, plus `Risk_Score_0_100` (0-100) on Renewal, are ALL preserved so existing downstream consumers don't break. **New report formats that publish per-customer scores MUST follow the R67/B1 + R88/F2 template — route through `compute_customer_risk_profile`, project `risk_score_0_10` as `Overall_Risk_Score`, AND publish `Risk_Score_0_10` (same value, explicit-scale column name) so the workbook's column names match the docx narrative scale.** Pinned by `tests/test_round88_risk_score_0_10_populated.py` and `tests/test_round70_compact_risk_summary_overall_score.py` (R70 source-shape pin updated to reflect new canonical column order).
- **Round 88 / Build 64 (F3) — Renewal docx filename has no redundant `_Renewal_Report` suffix**: `app_simple.py::run_customer_renewal_analysis` (and the portfolio variant) MUST save the Renewal docx as `f"{base_path}.docx"` — NOT the legacy `f"{base_path}_Renewal_Report.docx"` which produced files like `AdoptIQ_Report_Renewal_<scope>_<ts>_Renewal_Report.docx` with the `Renewal` token doubled. The `base_path` already encodes the report type (`AdoptIQ_Report_Renewal_<scope>_<ts>`) so the redundant suffix on the docx-only side made the Renewal output names diverge from Compact / Comprehensive / Leader. **New report formats MUST use plain `f"{base_path}.docx"` for the Word artifact — the report-type token belongs in `base_path`, not the suffix.** Pinned by `tests/test_round88_renewal_filename_no_redundant_suffix.py` and the updated assertion in `tests/test_reports_extensive.py::test_renewal_report_single_customer`.
- **Round 88 / Build 64 (F4) — Leader `_get_subscriptions_for_cssm` delegates to R82 multi-column UNION**: `LeaderReportGenerator._get_subscriptions_for_cssm` MUST delegate to `adoptiq_backend.get_subscriptions_for_team(self.ctx, cssm_emails)` rather than rolling its own per-primary-column SQL loop. The R82 helper is the SSoT for matching a team-email roster against ALL `_C` email columns on the DSM table (primary `RPM_BUYING_CIRCLE_DM_C` + `RPM_BUYING_CIRCLE_DM_OPTY_OWNER_NAME_C` + `RPM_BUYING_CIRCLE_DM_BU_NAME_C` AND secondary `BUYING_CIRCLE_DM2_C` + `RPM_BUYING_CIRCLE_DM_BU_NAME_2_C`) via UNION-style SQL. Pre-R88 the legacy Leader path queried only ONE primary column per pass and silently dropped subscriptions where a CSSM was attributed to a secondary column — Brian Frazier's Build 61 audit caught Greg Dolberry missing 3 of his 5 BU accounts (BU - Cisco Systems INC CA, BU - Wells Fargo, BU - Apple INC US) for exactly this reason. The R82 helper handles primary+secondary in one query AND the `_C` column case-tolerance via `_normalize_columns`. **New code that needs to fetch subscriptions for a roster of CSSM emails MUST call `adoptiq_backend.get_subscriptions_for_team(ctx, emails)` — never rebuild the SQL from scratch, the multi-column primary+secondary UNION is non-trivial and has tripped two separate rounds (R82 + R88) before landing here.** Pinned by `tests/test_round88_cssm_subscriptions_union.py` (delegation source-shape + behavior round-trip) and the existing `tests/test_round82_*.py` regression suite.
- **Round 88 / Build 64 (F5) — operator-configurable CSOne OneDrive folder override**: `adoptiq_settings.py` carries a new schema key `csone_onedrive_folder` (default `""`) plus validator `_is_valid_csone_folder_path` (absolute POSIX `/` or Windows `C:\` or tilde-prefixed `~/`, NUL/control/shell-meta-free, ≤4096 bytes, type=str). `config._resolve_csone_onedrive_folder()` resolves in this precedence (highest first): (1) `settings.json[csone_onedrive_folder]` (this page); (2) `CSONE_ONEDRIVE_FOLDER` env var; (3) `_csone_onedrive_candidates()` auto-discovery; (4) first candidate as final fallback. `app_simple.py` exposes `GET / POST /api/settings/csone-onedrive-folder` (CSRF dual-auth like all R69+ settings endpoints, atomic 0o600 write, in-process `Config.CSONE_ONEDRIVE_FOLDER` mutation so the next index pass uses the new path WITHOUT a restart). The UI lives on `templates/preferences.html` only — modeled on the R85 corpus-share-url card (IIFE-wrapped JS module, textContent rendering, no innerHTML / eval / inline event handlers). Closes Brian's Build 61 acceptance gap where his sharer-prefixed local sync path `/Users/<u>/Library/CloudStorage/OneDrive-Cisco/Jeffrey Story (jestory) - AdoptIQ_CSOne_Reports` did NOT match the canonical `AI Projects/AdoptIQ_CSOne_Reports` shape that auto-discovery looks for. **Threat model: the path is NOT a secret — it points at the operator's own filesystem; OneDrive desktop client gates content access (only files the operator's own Microsoft account is authorised to see land in the local sync mirror). The R83 encryption contract is unchanged — this is purely a filesystem-path override, not a SharePoint URL or auth credential.** Pinned by `tests/test_round88_csone_folder_override.py` (44 tests: schema/validators/resolver/endpoint/marker pins) + `tests/test_round88_csone_folder_override_ui.py` (17 tests: IIFE wrapper, textContent rendering, CSRF headers, no dangerous DOM sinks, no inline event handlers, max-length cap).
- **Round 89 / Build 65 (F3) — macOS `Info.plist` version stamping is SSoT-driven, no hard-coded fallback**: `adoptiq_mac.spec` MUST resolve `CFBundleShortVersionString` and `CFBundleVersion` through the new `_r89_resolve_version_from_config()` helper that reads `ADOPTIQ_VERSION` / `ADOPTIQ_BUILD` directly from `config.py` as a fallback when the env vars are not exported into the PyInstaller subprocess. Pre-R89 the spec carried hard-coded fallbacks of `"1.0.3"` / `"1"` whenever `os.environ.get(...)` returned `None` — the same R67-class footgun the R67/Phase 4 fix landed in `build_mac.sh` / `build_mac_dmg.sh` / `build_pc.bat` but missed in `adoptiq_mac.spec` itself. Build 65 acceptance caught this when the bundled .app's Info.plist showed `1.0.3 build 1` even though every in-app surface (`/api/version`, the R68/A1 build label in every report, the R68/A2 restart-required banner) read the correct `1.0.4 / 65` from `config.py` at runtime — only the macOS Finder/About dialog and the spotlight-indexed bundle metadata showed the stale stamp. Defense in depth: `build_mac.sh` MUST also `export ADOPTIQ_VERSION` and `export ADOPTIQ_BUILD` after the read-back from `config.py` so the PyInstaller subprocess inherits them; without `export`, the shell-local variables stay invisible to the spec's `os.environ.get` calls and we fall through to the `_r89_resolve_version_from_config()` SSoT path (still correct, but the env path should be the primary). **Future PyInstaller spec changes that touch Info.plist / version_info.txt / version-bearing metadata MUST go through the SSoT helper rather than hard-coding fallback strings — a future build script that fails to export the env vars MUST still stamp the right version because the spec falls through to `config.py`. The R67/Phase 4 read-back-from-config pattern in `build_mac.sh` is necessary AND sufficient ONLY when paired with `export`; future build scripts MUST enforce both halves of the contract.** Pinned by `tests/test_round89_f3_mac_spec_version_stamping.py` (10 tests covering: no hard-coded fallback in spec / helper presence + correct regex / env precedence over config / build_mac.sh exports the resolved values / round-trip behavioral verification of the helper / R89 source markers in both spec + build script).
- **Round 89 / Build 65 (F2) — Leader TAC table case Title strips markdown chrome**: `LeaderReportGenerator._add_tac_section` MUST route TAC case Title strings through `_strip_markdown_chrome(...)` before assigning to `row_cells[2].text`. Pre-R89 the writer at line 4578 of `leader_report_generator.py` did `row_cells[2].text = str(title)` raw, bypassing markdown sanitization that the AB / Pulse / Action_Plans / Software_Defects subject paths at lines 4394 / 4445 / 4486 / 5359 / 6333 had ALL already adopted via the same `_strip_markdown_chrome` helper. Build 63 audit caught the residual: a TAC case title `**Classic Calabrio***delete old report - Calabrio WFO# 00179474` rendered with literal asterisks intact in the docx table cell. The R42/Phase 6 helper's docstring (line 195-219 of the same file) literally cites this exact offending string as its motivating example — the helper was authored for this case but never wired into the TAC table path. Fix wires `row_cells[2].text = _strip_markdown_chrome(title) or 'No title'` mirroring the canonical `or 'No subject'` / `or 'N/A'` fallback shape used at the other five strip sites. **Future writers that emit Snowflake-sourced free-text fields into python-docx table cells MUST route through `_strip_markdown_chrome` (or the equivalent `app_simple._strip_markdown_chrome` for app-side paths) — `add_run(...)` / `cell.text = str(...)` will render literal `*` / `_` chrome as visible characters because python-docx does not parse markdown.** Pinned by `tests/test_round89_f2_leader_tac_title_md_strip.py` (8 tests: source-shape pin + R42-helper-marker pin + 5 helper-behavior regression guards + black-box `**` absence smoke).
- **Round 89 / Build 65 (F1) — Compact `Risk_Score` back-compat alias retired**: the R67/B6 hedge column on Compact `Risk_Summary` (`Risk_Score`, kept as a back-compat alias for one build then deprecated per the R67/B6 contract) is GONE. The canonical column names on Compact are `Overall_Risk_Score` (0-10 scale, primary headline) and `Risk_Score_0_10` (R88/F2, explicit-scale column adjacent to the canonical headline). All four reader sites in `app_simple.run_compact_analysis` (the `_canonical_high_risk_names` sort at L10465-71, the `Risk_Band`-based fallback sort at L10477-83, the bare-cutoff filter+sort at L10495-500, and the mean fallback at L10615-27) read `Overall_Risk_Score` directly. The row-build dict at L10404-18 emits `Overall_Risk_Score` + `Risk_Score_0_10` ONLY (no `Risk_Score` slot); the explicit `columns=` tuple at L10429-32 lists `Customer, Overall_Risk_Score, Risk_Score_0_10, Risk_Level, Risk_Band, Adoption_Barriers, Support_Cases` (no `Risk_Score`). **Future Compact `Risk_Summary` consumers MUST read `Overall_Risk_Score` (canonical) or `Risk_Score_0_10` (explicit-scale) — the legacy `Risk_Score` column is gone and any new reader that adds it back will silently re-introduce the R67/B6 ambiguity that R86/F1 spent a round un-saturating.** Pinned by `tests/test_round89_compact_risk_score_alias_removed.py` (writer source-shape + columns tuple + 5 reader-site pins + DataFrame-level absence assertion + sort-on-Overall_Risk_Score regression test) and the updated assertions in `tests/test_round70_compact_risk_summary_overall_score.py` + `tests/test_round88_risk_score_0_10_populated.py`.
- **Round 90 / Build 66 — citation injector paren-balance filter for spurious mid-paren matches**: `report_source_injector._PARAGRAPH_KPI_NUMERIC_RE` matches the regex `\b(?P<label>[A-Za-z][A-Za-z /()\-]{2,80}?)\s*[:\-]\s*(?P<value>...)`. Because `)` is in the label charset (so balanced labels like `Medium Risk (band)` match), it ALSO greedily matches mid-parenthetical fragments like `scale): 9` from text like `Score 4-6 (Watch, 0-10 scale): 9` -- producing a spurious match with `label='scale)' value='9'`. The R66/B1 unit-deferral branch in `_rewrite_paragraph_with_inline_citations` then sees alphabetic tokens (`Score`, `Watch`) in the boundary segment between the third (legitimate) and fourth (spurious) match and lands the citation at the spurious match's `next_label_start` position — which is right BEFORE `scale`, producing the user-facing `0-10 [Source: AdoptIQ Report Data Sources] scale` mid-string injection observed on the Build 65 Compact Risk Summary tile. Fix: a new `_paragraph_match_is_well_formed(match)` helper rejects matches whose label has more `)` than `(` (i.e., labels that start inside an unmatched parenthetical). Real KPI labels never start inside an unmatched `(`, so this is a strict, conservative filter. Applied in TWO sites: (a) the caller's `all_matches` builder so the rewriter is never handed spurious matches from the start; (b) the rewriter's per-line `line_matches` because the rewriter re-extracts matches per-line after splitting on `\n`. **The filter is paren-balance-only (`)` count strictly greater than `(` count); labels with unmatched OPENING parens (`Risk (incremental`) are kept defensively.** Future regex tightening (e.g. removing `)` from the label charset entirely, requiring balanced parens at the regex level) MUST replace the filter rather than weakening it — a regex that no longer matches `scale)` makes the source-shape pin in `tests/test_round90_paren_label_filter.py::test_regex_still_extracts_spurious_scale_paren_match` fail, signalling the contract has shifted. Pinned by `tests/test_round90_paren_label_filter.py` (16 tests covering helper unit semantics + regex source-shape pin + end-to-end pin on the Build-65 failing line + R76 paren-cluster regression guard + R66/B1 single-line regression guard + helper presence + R90 source markers).
- **Round 91 / Build 67 — single-window jobs dashboard and report-model snapshot contract**: new report starts from `templates/analyze.html` and `templates/leader_report_form.html` MUST support in-place submission into `static/js/report_jobs_dashboard.js` rather than forcing a `/progress/<analysis_id>` redirect. Every status payload returned by `/api/status/all` MUST expose the job's `active_report_model` snapshot plus `phase_timings` captured through `_update_progress`; long Comprehensive runs are diagnosed by phase timing, not by guessing. Portfolio-level report LLM calls MUST use the job snapshot where the status was minted so Ask AI and report model preferences never cross wires. The dashboard remains DOM-safe (`createElement` / `textContent`, CSRF headers, `response.ok` checks) and is pinned by `tests/test_round91_jobs_workflow_and_accuracy.py` plus the updated Round 33 progress-watchdog tests.
- **Round 92 / Build 67 — report output path SSoT and generated-report corpus admission gate**: report output roots MUST resolve through `report_output_paths.py` and the Round 81 output helpers, not ad hoc `OUTPUT_FOLDER` joins. Packaged builds default to `~/Documents/AdoptIQ Reports`; settings and `ADOPTIQ_OUTPUTS_DIR` override through the documented precedence chain. Generated reports MUST write a `.adoptiq_corpus.json` sidecar and local corpus ingestion MUST require `corpus_eligible=true` through `_r92_corpus_admission_for_status` / `enumerate_corpus_files(..., require_adoptiq_quality_gate=True)` for generated-report sources. Open-in-place actions MUST resolve only server-side `analysis_status` paths under current or legacy output roots; never accept arbitrary client filesystem paths. Pinned by `tests/test_round92_report_storage.py`.
- **Round 93 / Build 67 — All Contact Center Adoption Barrier scope contract**: `_apply_scope_filter_ab` MUST keep the All Contact Center / Contact Center-family branch strict. Explicit non-Contact-Center product rows are excluded and the function MUST NOT fall through to the older named-technology widening fallback when no ACC rows match. Scope-exclusion diagnostics MUST flow through `_r93_extend_partial_warnings_once` into Compact, Renewal, and Comprehensive status/Word/XLSX surfaces so operators see excluded-row counts instead of silent metric drift. The named non-ACC technology widening contract remains unchanged unless a future round explicitly replaces it. Pinned by `tests/test_round93_all_contact_center_ab_scope.py`.
- **Round 94 / Build 67 — full-sweep acceptance contracts**: Word build labels MUST retain the legacy prefix (`AdoptIQ v... build ... - generated ...`) AND carry the four canonical audit fields (`App_Version`, `App_Build`, `Process_Started_At_UTC`, `Report_Generated_At_UTC`) via `_r68_build_label.get_build_label_text`. Comprehensive XLSX `Report_Info` MUST include `Partial_Data_Warning` rows when `partial_data_warnings` exist, including R93 `tech_filter_scope_excluded` warnings. All generated-report corpus sources (`local_outputs`, OneDrive, intel uploads where enabled) MUST respect the strict sidecar gate when configured with `quality_gate="strict_generated_report"`; tests that exercise this path should pass `require_adoptiq_quality_gate=True`. Pinned by `tests/test_round94_full_sweep_regressions.py`; Build 67 live acceptance regenerated all four canonical reports and audited 8 artifacts clean.
- **Round 95 / Build 68 — Ask AI reranker, canonical self-check, eval expansion, and confidence band**: optional second-stage reranking lives in `ask_ai_reranker.py` and MUST degrade to RRF order at runtime while surfacing `rerank` diagnostics; release/developer bake probes still fail loud when explicitly run. `ask_ai_grounded._r95_cross_check_answer_against_canonical` and `_r95_apply_canonical_corrections` append correction callouts for canonical KPI drift instead of silently presenting wrong totals. The eval framework carries canonical-metric questions/predicates, and `static/js/r95_confidence_band.js` renders the confidence badge with `textContent` only. Pinned by `tests/test_round95_a_cross_encoder_rerank.py`, `tests/test_round95_b_canonical_self_check.py`, `tests/test_round95_c_eval_predicate.py`, and `tests/test_round95_d_confidence_band.py`.
- **Round 107 / Build 76 — prebaked corpus restored**: shipping builds MUST bake and bundle `bake/corpus.db.enc`, `bake/corpus.db.salt`, and `bake/sentinel.json` under `Resources/baked_corpus/`; they MUST NOT bundle `corpus.sentinel.lock.json`. `build_mac_dmg.sh` defaults to `ADOPTIQ_BAKE_CORPUS=1`, and `ADOPTIQ_RELEASE_GATE=1` rejects `.bake-skipped` or missing baked artifacts. `_install_baked_corpus_if_present()` copies the snapshot into App Support on first launch and preserves/replaces broken local corpora with `self_healed_baked`. Runtime refresh remains local-sentinel keyed and OneDrive is optional source context. Pinned by the Round 35/39/53/54/68/87 corpus tests and the baked-data packaging tests.
- **Round 109 / Build 78 — bounded runtime dense-vector upsert + corpus panel + jobs-dashboard model label**: runtime calls to `ask_ai_vector_store.upsert_chunk_vectors(..., strict=False)` are bounded by `_runtime_max_chunks_default` (default 2,000, env-overridable via `ADOPTIQ_RUNTIME_VECTOR_MAX_CHUNKS`) so a 500k+ chunk corpus cannot keep `corpus_bootstrap._run_index_pass` synchronously busy and leave `boot.in_progress=True` for the entire dense catch-up. Bake-time/strict callers MUST keep passing `max_chunks=None` (the default for `strict=True` and for callers that pass an explicit `chunk_rows` sequence) so release DMGs continue to ship with full dense vectors. The `ChunkVectorUpsertResult` dataclass MUST carry `rows_remaining: int = 0`, with `status="partial"` whenever the bounded pass left a backlog; `_run_index_pass` mirrors this onto `_STATE.dense_rows_remaining` AND `_STATE.last_stats["dense_rows_remaining"]` so subsequent `/api/intel/status` polls show the backlog without scraping logs. `_r108_update_retrieval_method_for_vector_status` MUST treat `"partial"` as healthy and flip back to `"hybrid"` (unless the operator pinned `ASK_AI_RETRIEVAL_METHOD=lexical`); the rows we DID write are usable and the missing rows fall through the lexical channel of the RRF fuser. The corpus panel classifier in `static/js/intel_status.js::classifyCorpusPanel` MUST short-circuit `boot.in_progress` to `runtime_synced` when `payload.available && corpus.chunks > 0` — an already-serving lexical corpus is "Active", not "Indexing"; only show `refreshing` when no usable corpus exists yet. The dense-status helper renders `partial` + `dense_rows_remaining > 0` as "Dense retrieval is warming • backfilling N chunks", a quality note, not a blocker. The jobs dashboard model label in `static/js/report_jobs_dashboard.js` MUST resolve from `activeJobs` only and fall back to the server-rendered `data-default-model` attribute when no jobs are running — pre-R109 the panel walked `jobs.map(modelLabel)` and historical "gpt-5-nano" snapshots from before the R108 default flip would pin a stale label. **Future runtime maintenance work that runs inside the corpus boot pass MUST be bounded the same way: a per-call cap, an explicit "remaining" diagnostic, and a status that does NOT keep `boot.in_progress=True`. Future "active model" UI surfaces MUST resolve from currently active jobs OR a server-resolved default — never from completed history.** Pinned by `tests/test_round109_fix_indexing_hang.py` (18 tests covering bounded upsert, partial-status retrieval policy, boot-state diagnostics, panel rendering, and jobs-dashboard model label drift).
- **Round 110 / Build 79 — Leader Report_Info writer dynamic-row schema parity**: the Leader `Report_Info` writer in `app_simple.py` MUST emit EVERY dict in `_info_rows` with the canonical `'Item':` first-column key per the Round 73 / Phase 3 (F6) schema — including the two dynamic-loop branches that append `Partial_Data_Warning_<n>` rows (when `partial_data_warnings` is non-empty) AND `Failed_Sheet` rows (when one or more sheets failed to write). Pre-R110 those two branches alone keyed on `'Field':`; `pd.DataFrame(_info_rows)` then built the union of keys, silently growing a stray fifth `Field` column with NaN on every other row whenever a Leader run produced a partial warning OR a failed sheet — violating R73/F6's promise that `pd.read_excel("Report_Info")[["Item", "Value"]]` projects cleanly across every report format. The defect was latent (Build 78's clean Brian Frazier 90d cohort had `Partial_Data_Warning_Count=0` and `_failed_sheets=[]` so the buggy rows never fired) but would have tripped on the next Leader run with any partial-data warning or sheet-write failure. R110 normalises both dynamic-loop branches onto `'Item':` so the four-column `Item / Value / Detail / Generated_At` schema holds in every Leader run. **Future writer code that appends rows to a Report_Info-style `_info_rows` list MUST use `'Item':` as the first-column key on EVERY append branch — static OR dynamic-loop — and every new dynamic-loop branch MUST be covered by a behavior-pin test that exercises it (the R73 source-shape pins only covered the static rows, which is why R110's defect went undetected for 37 builds).** Pinned by `tests/test_round110_leader_report_info_schema_parity.py` (6 tests covering source-shape negative pins for both legacy `'Field':` literals, source-shape positive pins for the canonical `'Item':` literals, the R110 source marker, an end-to-end behavior pin that round-trips a Leader-shape `_info_rows` list with N>0 partial warnings + N>0 failed sheets through `pd.DataFrame` → in-memory xlsx → `openpyxl` and asserts the canonical 4-column header, plus a negative-control pin that reproduces the pre-R110 5-column NaN-leak so a future "simplification" of pandas' union behavior would also fail loud).
- **Round 111 / Build 80 — Compact <-> Renewal Risk_Score_0_10 parity (R67/B1 root-cause fix)**: `compact_report_formatter._r66_b8_classify_extra_frames` MUST recognise the live production CSConsole pulse column shape. The pulse marker set widens from `{PULSE_RATING__C, PULSE_RATING, SCORE__C, PULSE_SCORE, rating, Customer Pulse}` to ALSO include `CUSTOMER_PULSE__C` (the picklist rating value emitted by `adoptiq_backend.fetch_csconsole_customer_pulse`'s `cp.*` projection over `EDW_SALES_ETL_DB.SS.ESA_C360_CUSTOMER_PULSE__C`) AND `CUSTOMER_PULSE_COLOR_IMAGE__C` (the color-image asset). The disqualifier set in the AP fallback branch widens to the same two markers so a future schema change adding a STATUS column to the pulse table cannot re-route the frame into action_plans_df. Pre-R111 the classifier returned `pulse_df=None` for production data; Compact's per-customer pulse slice was always empty even when the data was available, shifting `_score_engagement` (via `total_activity = AB + SC + pulse + AP`) and producing the systematic Renewal-vs-Compact +1.0/10 drift on 70% of common customers in the Build 79 acceptance audit. The Renewal `_calculate_simple_renewal_risk` path was unaffected because it filters `csconsole_customer_pulse` by `BU_NAME` directly (no classifier). **Defense-in-depth: `calculate_renewal_risk_scores` accepts new keyword-only kwargs `pulse_df` / `action_plans_df` / `subs_df` (default `None`) that, when provided, OVERRIDE the classifier output for the matching slot.** All four `app_simple.py` Compact call sites (Word success / Word fallback / XLSX success / XLSX fallback) MUST pass `pulse_df=_ctx.get('csconsole_customer_pulse')`, `action_plans_df=_ctx.get('csconsole_action_plans')`, `subs_df=_ctx.get('team_subs_df_unfiltered')` so Compact bypasses classification entirely and feeds the canonical CSConsole frames Renewal already feeds — closing the cross-format `Risk_Score_0_10` parity gap (R67/B1 contract). The classifier remains as a fallback for legacy callers outside `app_simple.py`. **Future report formats that publish per-customer scores MUST follow the R67/B1 + R88/F2 + R111 template: route through `compute_customer_risk_profile`, project `risk_score_0_10` as `Overall_Risk_Score`, publish `Risk_Score_0_10` as an explicit-scale column, remap `MEDIUM → MODERATE`, AND when sourcing pulse / AP / subs data from a multi-frame container, prefer explicit canonical-frame kwargs over column-marker classification — a marker-based classifier is a footgun every time the upstream schema rotates.** Pinned by `tests/test_round111_compact_renewal_score_parity_live_data.py` (12 tests covering the widened classifier markers / disqualifier expansion / explicit-kwarg signature / source markers / explicit-kwarg override behavior / per-customer parity with Renewal / pulse-not-lost regression / empty-data control / subs-only edge case / legacy classifier back-compat / app_simple call-site coverage).
- **Round 111 / Build 80 — Renewal Risk_Components is portfolio-level (B3 documentation)**: the Renewal XLSX `Risk_Components` sheet is a PORTFOLIO-LEVEL component-score rollup — aggregated component scores summarised across the team. It is INTENTIONALLY DIFFERENT from the Comprehensive XLSX `Risk_Components` sheet, which carries one row per customer with the per-component breakdown (`Adoption_Barriers_Score`, `Support_Cases_Score`, `Customer_Pulse_Score`, `Action_Plans_Score`, `Incidents_Score`, `Contract_Score`, `Engagement_Score`) plus `Top_Risk_Factor`. The Build 79 acceptance audit flagged the Renewal `Risk_Components` sheet as "missing canonical columns" because it was checking the Comprehensive shape; this is NOT a defect — both shapes are intended. **Future audits that compare `Risk_Components` across formats MUST distinguish "portfolio-level rollup" (Renewal) from "per-customer detail" (Comprehensive). Tests that expect `Adoption_Barriers_Score` per-customer columns on Renewal MUST update the audit, NOT the writer.** No code change in R111 — this is a documentation-only Critical Rule.
- **Round 112 / Build 81 (F1 — SECURITY) — LLM error sanitisation covers CircuIT-specific identifiers AND every LLM-fallback render site MUST route through the sanitiser**: pre-R112 the Compact docx P25 "Non-AI fallback summary" paragraph leaked the upstream CircuIT 429 rate-limit response body containing `appkey=...` + `session_id=...` + a stringified `user` JSON blob. Two-pronged fix: (a) `app_simple.py:5776` now routes the `llm_error` payload through `_r69_sanitize_llm_error(llm_error, max_len=400)` instead of raw concatenation; (b) `_r69_sanitize_llm_error` is extended with three new redaction patterns (case-insensitive): `appkey=<value>` / `"appkey": "..."` / `'appkey': '...'`; `session_id=<value>` / `"session_id": "..."` / `'session_id': '...'`; AND the entire `'user': '{...}'` Python-repr blob (collapsed to `'user': '<redacted>'`). **All future LLM-fallback narrative render sites MUST route through `_r69_sanitize_llm_error` — directly concatenating an `llm_error` string into report-facing text re-introduces the same exposure.** When CircuIT or another LLM provider rotates correlation-ID names, the redaction set MUST be widened in lockstep — a missed name produces a per-customer credential leak across every report run that hits the rate limit. Pinned by `tests/test_round112_build80_acceptance_fixes.py::TestF1LLMErrorSanitizerLeak` (6 tests covering each new pattern + non-credential preservation + max-len cap + the call-site source-shape pin).
- **Round 112 / Build 81 (F2) — Renewal Word writer publishes the canonical 0-10 scale, NOT 0-100, on every display surface**: the Renewal Word writer in `app_simple.py` had two display sites (the score-summary box at L12970 and the per-customer focus-table cell at L13089) that hard-coded the legacy 0-100 scale string `f'{score:.1f}/100'`. R67/B1 + R88/F2 + R111 made the 0-10 scale canonical for the published `Overall_Risk_Score` column AND the executive-summary text body — these two sites violated that contract by mixing scales within the same docx. **Both sites MUST scale the source value `score / 10.0` BEFORE the format string AND emit `f'{score_10:.1f}/10'` as the primary display.** The score-summary box additionally appends the 0-100 reference parenthetically (`f'{score_10:.1f}/10 ({band_label}; {score_100:.1f}/100)'`) for back-compat readability; the focus-table cell is 0-10 only (the table is narrower and the secondary scale would crowd). The R86/F1-explicit `renewal_risk_score_10` field is the source of truth — `renewal_risk_score` (0-100) is preserved on `cust_analysis` for the parenthetical reference but is NEVER the primary display. Pinned by `tests/test_round112_build80_acceptance_fixes.py::TestF2RenewalScoreScale` (2 tests).
- **Round 112 / Build 81 (F3) — wrapper KPI immediately before embedded paren cluster does NOT emit its own citation**: `report_source_injector._rewrite_paragraph_with_inline_citations` (the multi-match path branched on `(i + 1) in in_cluster`) MUST suppress the wrapper KPI's citation when the wrapper-KPI value sits IMMEDIATELY before the embedded paren cluster's open paren — i.e. when `line[value_end:cluster_open_pos].strip() == ""`. The cluster's post-`)` citation already covers BOTH the wrapper and the cluster body as one semantic unit (`"Total Activities: 42 (APs: 18, ABs: 3, CPs: 1, TAC: 20)"` is one assertion, not two). Pre-R112 the wrapper got its own citation and the line rendered `"42 [Source: ...](APs: ...)"` — two adjacent citations interrupting the semantic unit. The cursor advance is `cursor = value_end` (NOT `cursor = m.end()` — the regex match span includes trailing whitespace via `\s*%?` and consuming it would eat the space between `"42"` and `"("`); the cluster-end branch later in the loop picks up from this cursor and flushes `" (cluster_body) [Source: ...]"` in one go. When the boundary contains non-whitespace (e.g. `"42 then breakdown (APs: ...)"`), the pre-R112 behavior is preserved (the `then` joiner-word disqualifies the cluster anyway, so R66/B1 unit-deferral handles it). The R76/Build 51 regression test `test_round76_build51_embedded_paren_cluster.py::test_embedded_paren_cluster_rewrite_collapses_to_single_citation` was updated in lockstep — it had pinned the pre-R112 "wrapper KPI must get its own citation" contract; post-R112 it asserts the new "ONE citation total covers wrapper + cluster" shape. **Future formatters that emit single-paragraph multi-KPI lines with parenthetical breakdowns MUST keep this pattern intact — a wrapper KPI with whitespace-only handoff to its paren cluster is one semantic unit, never two.** Pinned by `tests/test_round112_build80_acceptance_fixes.py::TestF3CitationInjectorWrapperSuppression` (3 tests covering the violation reproducer + the with-text-between negative control + the no-trailing-match negative control).
- **Round 112 / Build 81 (F4) — BE classifier intro narrative is honest about LLM tagging outcome**: `be_priority_word_section._intro_text` MUST read the BE pipeline diag's `classified_count`, `llm_disabled`, and `llm_error` fields (off `diag.get('llm_diag', {})` with defensive type checks) and emit one of three honest narrative variants: (a) success — `"the top {classified_count} of {top_n} barriers were also tagged by an LLM classifier"`; (b) explicitly disabled — `"LLM classification was disabled for this run"`; (c) attempted but produced no tags (rate limit, transient error, content filter) — `"LLM classification was attempted on the top {top_n} barriers but did not return any tags this run"`. Pre-R112 the narrative ALWAYS claimed success when `top_n > 0`, even when `classified_count == 0` and `llm_error` carried a 429 body — the report was honest about which barriers were ranked deterministically but hallucinated about LLM tagging that never happened. **Future LLM-classifier wrappers that emit narrative-facing summaries MUST follow the same three-state contract: success / disabled / attempted-but-failed. Silent-fallback patterns that say "tagged" when no tags landed are the same class of bug as F4 and will fail audit.** Pinned by `tests/test_round112_build80_acceptance_fixes.py::TestF4BEClassifierHonesty` (3 tests).
- **Round 112 / Build 81 (F5) — partial-data warning preamble distinguishes scope-exclusion from data-load failure**: the Compact + Renewal docx partial-data warning paragraphs in `app_simple.py` (~L7566 + L12681) MUST detect when ALL warnings in `partial_data_warnings` are scope-kind (the new `_r112_scope_kinds` set: `tech_filter_scope_excluded`, `manager_filter_scope_excluded`, `time_window_scope_excluded`, `no_onedrive_sync`, `autodiscovered_empty_after_scope`, plus any kind starting with `tech_filter_scope`) and emit the honest preamble `"One or more upstream data sources returned data that was filtered out by the requested scope (manager, technology, or time window) for this run; counts and narratives below reflect only what was in scope."` instead of the legacy `"One or more upstream data sources failed to load for this run"` — which falsely implied an outage when the actual reason was a deliberate scope decision (R93 contract). Mixed-kind warning lists OR genuine load failures continue to use the legacy preamble. **Future code that adds a new R93-style scope-exclusion warning kind MUST add the new kind to `_r112_scope_kinds` (or use a `tech_filter_scope`/`manager_filter_scope`/`time_window_scope` prefix that's already covered by the `startswith` branch) so the preamble selection stays correct.** Pinned by `tests/test_round112_build80_acceptance_fixes.py::TestF5PartialDataWarningKindAware` (1 test pinning both call-site source markers).
- **Round 112 / Build 81 (F6) — `r112_smart_possessive` is the SSoT for manager-name possessive titles**: `report_utils.r112_smart_possessive(name)` returns the bare name for the literal sentinels `"All Managers"` / `"all"` / `"portfolio"` (case-insensitive — `"ALL MANAGERS"` and `"all managers"` both match), apostrophe-only for sibilant-final names (`"Alex" → "Alex'"` ; `"James" → "James'"`), and apostrophe-s for everything else (`"Brian Frazier" → "Brian Frazier's"`). Empty / `None` / whitespace-only inputs return `""`. Pre-R112 report titles for the literal `"All Managers"` sentinel rendered as `"All Managers's Portfolio"` — a double possessive (`All Managers` is already pluralised; appending apostrophe-s is ungrammatical). The helper is wired into 5 `app_simple.py` call sites + 2 `adoptiq_backend.py` call sites that previously hard-coded `f"{manager}'s Portfolio"`. **All future report-title code that emits a possessive form of a manager name MUST go through `r112_smart_possessive` — hard-coded `f"{name}'s ..."` will silently re-introduce the bug for the `All Managers` sentinel and any future sibilant-final manager name.** Pinned by `tests/test_round112_build80_acceptance_fixes.py::TestF6SmartPossessive` (7 tests covering each branch + both source-shape pins).
- **Round 113 / Build 82 (A1) — conversation history MUST be threaded through BOTH the sync and stream Ask AI paths**: `_r74_apply_conversation_history(question, history)` is the SSoT for prepending prior Q/A turns to the question string. Pre-R113 only the streaming `/api/ask-ai-portfolio/stream` route applied it; the sync `/api/ask-ai-portfolio` route never did, so the "Continue conversation" toggle was a silent no-op whenever streaming was disabled or fell back. Both routes now call `_r74_apply_conversation_history(question, data.get('conversation_history'))`. **History augments ONLY the question string — never evidence/citation enforcement** (the prefix is prepended to the question text; allowed-IDs and grounding are unchanged), so multi-turn never loosens grounding. **New Ask AI request paths that accept `conversation_history` MUST route through this helper, never inline-concatenate history into the prompt.** Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestA1ConversationHistory` (incl. the source-shape pin that `_r74_apply_conversation_history(question` appears ≥2 times in `app_simple.py`).
- **Round 113 / Build 82 (B1) — renewal/expiry/ARR grounding in CANONICAL_HEADLINE is multi-currency aware**: `ask_ai_grounded._r113_renewal_headline_fields(bundle)` extracts `contracts_expiring_90d`, `expiring_arr`, and `renewals_at_risk` from the already-prefetched `bundle["enhanced_account_insights"]` (NO new Snowflake fetch). **When the portfolio spans currencies (`is_multi_currency` truthy OR `expiring_arr is None`), the helper MUST emit `expiring_arr_by_currency` (a per-currency breakdown string) instead of a single misleading ARR number.** Booleans are explicitly rejected as numeric inputs (`isinstance(x, bool)` guard) so a `True` flag never renders as `1`. The merge into `canonical_headline` is gap-fill only (`if _r113_k not in canonical_headline`) so SSoT portfolio metrics always win. Malformed bundles return `{}`. **Future headline enrichment that surfaces money MUST be multi-currency aware — never collapse a multi-currency total into one number.** Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestB1RenewalHeadline` (single-currency, multi-currency breakdown, bool-rejection, malformed, merge-wiring).
- **Round 113 / Build 82 (B2) — live suggestion chips read a bounded in-memory per-scope top-risk cache**: `ask_ai_grounded._r113_stamp_top_risk_customers(manager, technology, days, risk_profiles)` stamps the top-`_R113_TOP_RISK_TOP_N` (5) customer names for a scope after `run_portfolio_grounded_ask_ai` computes `_risk_profiles_canon`; `get_top_risk_customers_for_scope(...)` reads them. The cache is an `OrderedDict` capped at `_R113_TOP_RISK_CACHE_MAX` (64) with FIFO eviction, lock-protected, **names-only (no PII to disk, never persisted)**, scope-keyed via `_r113_scope_key` (lowercased + whitespace-stripped manager/tech + int days). Sort is `risk_score_0_100` DESC (fallback `composite_risk`) with name-ASC tiebreak (the SSoT determinism rule). `/api/ask-ai/suggestions` names a real top-risk customer when the cache is warm and **falls back to the template chip when cold so chips never go blank** (the route stays Snowflake-free / sub-100ms). Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestB2TopRiskCache` (roundtrip, cold-cache empty, scope-key normalisation, FIFO bound, composite fallback, warm+cold endpoint behavior).
- **Round 113 / Build 82 (B3) — Ask AI customer drill-through is XSS-safe**: Ask AI customer names (evidence drawer pill + citation popover badge in `static/js/ask_ai.js`) link to the Customer 360 route via `'/customer/' + encodeURIComponent(name)`, and the visible label is set via `.textContent` — NEVER `.innerHTML`. A malicious customer string can therefore neither inject markup nor break out of the URL. **New surfaces that render a customer name as a link MUST use `encodeURIComponent` for the href + `.textContent` for the label.** Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestB3CustomerDrillthrough`.
- **Round 113 / Build 82 (C1) — CSOne folder card reads `folder_path`**: `static/js/r88_csone_folder_override.js` MUST read the active path from `payload.folder_path` (the key `GET /api/settings/csone-onedrive-folder` actually returns), not the pre-R113 `payload.active_path` (which never existed in the response, so the card always showed "(no path resolved)"). The fix reads `payload.folder_path || payload.active_path` for back-compat. Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestC1CsoneFolderCard` (incl. an endpoint contract pin that the GET response carries `folder_path`).
- **Round 113 / Build 82 (C3) — persisted default analysis scope is re-validated against the LIVE roster on read**: `app_simple._r113_resolve_report_defaults()` reads `settings.json` (`default_days` / `default_manager` / `default_technology`) and RE-VALIDATES the manager against `MANAGERS` and the technology against `TECH_CHOICES` on every read, dropping a stale saved value to `""` (graceful degrade) rather than pre-selecting a phantom option. `default_days` is range-gated 1-365 (0 = unset). Schema + syntactic validators live in `adoptiq_settings` (`is_valid_default_days`: 0 or int in [1,365]; `is_valid_default_scope_str`: empty or ≤200-char shell-meta-free string — semantic roster validation stays in `app_simple` to keep `adoptiq_settings` import-cycle-free). `GET|POST /api/settings/report-defaults` mirrors the other settings routes (CSRF dual-path on POST via `_r17_2_authorize_corpus_admin`; atomic `save_settings` write). The resolver is wired into the analyze page (`index`), `ask_ai_page`, and the suggestion-chip fetch so a saved default pre-selects on both pages (resolving the analyze-picks-first-manager vs ask-ai-picks-All asymmetry). **A manager/tech that passes the syntactic gate but is off the live roster is accepted on WRITE (so a later roster change doesn't lock the operator out) but dropped on READ** — the GET response surfaces this as `persisted` != effective. **Future settings that reference roster/tech values MUST re-validate on read, never trust the persisted value.** Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestC3DefaultScope*`.
- **Round 113 / Build 82 (C4) — intel_status.js recurring poll only runs on pages with the banner/panel DOM**: `static/js/intel_status.js::_intelPollSurfacePresent()` checks for `[data-intel-banner]` OR `[data-sharepoint-panel]` and guards both `scheduleNextPoll()` and the initial `pollOnce()` in `init()` so the recurring `/api/intel/status` poll loop NEVER starts on pages without the Intelligence UI (e.g. `/preferences`). The one-shot `pollOnce()` from the Intelligence enable-toggle stays unguarded so the navbar badge still updates immediately after a flip. **New always-loaded JS that polls a backend MUST gate the recurring loop on a page-surface presence check.** Pinned by `tests/test_round113_ask_ai_and_prefs.py::TestC4PollGuard`.
- **Round 114 / Build 83 — multi-column data matrices get ONE source caption below the table, NOT per-cell citations**: `report_source_injector.inject_source_citations_into_docx` MUST NOT inject `[Source: ...]` chrome into individual cells of a multi-column data matrix (a table whose first data row has >=3 columns and whose KPI columns carry numeric values — Team Activity Summary, Individual Team Member Performance, etc.). Instead it inserts exactly ONE compact italic caption paragraph immediately AFTER the table via `_insert_source_caption_after_table(table, caption_text)` (python-docx XML idiom `table._tbl.addnext(...)`), where the caption text is built by `_build_matrix_source_caption(...)` aggregating the matrix's KPI columns through the existing R82 `_R82_KPI_SOURCE_TAGS` / `_r82_chrome_for_label` taxonomy (e.g. `"Sources: Action Plans, Adoption Barriers, Customer Pulse — Snowflake CSConsole; TAC Cases — Snowflake CSOne"`). The caption MUST degrade to the generic `_PARAGRAPH_FALLBACK_CITATION` (`AdoptIQ Report Data Sources`) text when no KPI column resolves through the taxonomy, so no matrix EVER loses its citation entirely. Insertion is idempotent: `_table_has_following_source_caption(table)` short-circuits the re-run so the injector stays safe to call multiple times. The returned `counts` dict carries `table_captions_added`; `paragraphs_injected` and the two-column `table_cells_injected` path are unchanged. **Two-column `label | value` rows KEEP their per-row citation (the R82 two-column contract — not part of the clutter complaint).** Quality-scorer parity: `report_iteration_loop._extract_docx_metric_claims` treats a `_MATRIX_SOURCE_CAPTION_PREFIX`-marked caption paragraph immediately following a matrix as backing for that matrix's multi-column claims (detected via the mirrored `_matrix_has_following_source_caption` helper) so the diagnostic scorer stays green WITHOUT per-cell citations — a contract evolution backed by new tests, not a weakened check. **New report formats / new matrix tables MUST rely on the shared injector caption — never re-introduce per-cell matrix citations.** The `_MATRIX_SOURCE_CAPTION_PREFIX` constant is intentionally duplicated (not imported) between `report_source_injector.py` and `report_iteration_loop.py` to keep the modules decoupled; the two MUST stay byte-identical. Pinned by `tests/test_round114_citation_caption_below_matrix.py` (10 tests) + the updated `tests/test_round57_source_citation_injector.py` count-dict assertions; the read-only `scripts/r114_audit_reports.py` is the Build-82 acceptance-audit artifact (measured 1392 per-cell citations in the pre-fix Leader docx).
- **Round 115 / Build 84 — 2-column `Metric | Value` KPI cards ALSO get ONE source caption below the table, NOT per-row in-cell citations**: this extends the R114 matrix declutter to the 2-column `label | value` KPI cards (the Leader per-CSSM "Metric | Value" tiles — the Build-83 live audit found 125 per-cell `[Source: ...]` citations in the Leader DOCX from the R82 Phase B3 in-cell path). `report_source_injector.inject_source_citations_into_docx` MUST NOT inject `[Source: ...]` chrome into 2-column KPI cells; instead it collects each cited row's R82 source tag (via the existing `_r82_chrome_for_label` taxonomy on the column-0 label) and inserts exactly ONE aggregated caption paragraph below the table via `_insert_source_caption_after_table(table, caption_text)`, where the caption text is built by `_build_two_column_source_caption(rows_cells_text, cited_row_indices)` grouping the cited rows' labels by source system (e.g. `"Sources: Adoption Barriers, Action Plans, Customer Pulse — Snowflake CSConsole; Support Cases — Snowflake CSOne"`). The R82 per-source taxonomy is **preserved** — only the PLACEMENT inverts from in-cell to below-table; the cells stay clean. The caption degrades to the generic `AdoptIQ Report Data Sources` text when no row label resolves through the taxonomy, so a 2-column card NEVER loses its provenance. Idempotency is shared with the R114 path via `_table_has_following_source_caption(table)`. Rows that ALREADY carry a legacy in-cell `[Source: ...]` (e.g. an older pre-R115 docx fed back through the injector) are excluded from the caption — their original citation is left untouched. The returned `counts` dict increments `table_captions_added` (NOT `table_cells_injected`, which is now `0` for 2-column cards). Quality-scorer parity: `report_iteration_loop` treats a 2-column metric claim as `source_backed` by EITHER an in-cell citation (`_source_backed_cell(row, 1)`) OR the following `Sources:` caption (`_matrix_has_following_source_caption(table)`) so strict mode stays green WITHOUT per-cell clutter. **New report formats / new 2-column KPI cards MUST rely on the shared injector caption — never re-introduce per-row in-cell matrix citations.** Pinned by `tests/test_round115_two_column_caption.py` (9 tests) + the updated `tests/test_round82_per_source_citation_taxonomy.py` (caption-based provenance for 2-column cards) + the updated `tests/test_round114_citation_caption_below_matrix.py` (the 2-column path now gets a caption, not per-row citations) + the updated `tests/test_round57_source_citation_injector.py` count-dict (`table_cells_injected == 0`, `table_captions_added == 2`). `scripts/r114_audit_reports.py` gained `--auto` (latest-of-each-type discovery) + `--target NAME=BASE` so the acceptance audit always targets the newest artifact.
- **Round 115 / Build 84 — stale `gpt-5-nano` report-model override re-migrates to `gemini-3.1-flash-lite` once**: an upgraded install can carry a stale `report_model_name: gpt-5-nano` in App-Support `settings.json` (the highest-precedence layer in `model_resolver`). Because the R103 + R108 stale-nano migrations had already stamped their markers, they early-return and treat the value as a deliberate selection — so the report narratives kept running on nano even though the shipped default flipped to gemini in Round 77. `adoptiq_settings.migrate_round115_model_defaults()` adds a NEW `r115_model_default_migrated` schema key and re-treats the EXACT stale `gpt-5-nano` value as stale once more (via the existing `_r103_model_value_needs_migration`), flipping `ask_ai_model_name` / `report_model_name` back to `gemini-3.1-flash-lite` and stamping the marker. It is wired into `ensure_model_defaults_migrated()` (called from `model_resolver._read_settings_value`) so it auto-corrects on the next app start without a manual settings edit. **The re-stomp is intentional and one-time (same precedent as R108)** — a deliberate post-R115 nano selection survives because its `r115` marker is already set (so the migration early-returns). Tests that persist `gpt-5-nano` and assert resolver passthrough MUST set `r115_model_default_migrated: True` in their saved settings to represent a deliberate post-R115 selection (the R77 / R103 / R108 tests were updated this way). Pinned by `tests/test_round115_model_remigration.py` (8 tests) + the updated `tests/test_round77_default_model_flip.py` + `tests/test_round103_ux_cleanup.py`. **Superseded by Round 116 / A (below) — coercion is no longer marker-gated; deliberate nano is now keyed on the `*_user_set` flag, so a wedged install with all three markers `True` but no `user_set` flag now heals to Gemini at resolve time. The R115 re-migration helper stays for back-compat but is no longer the heal mechanism.**
- **Round 116 / Build 85 — A (robust model resolution; deliberate nano keyed on `*_user_set`, NOT migration markers)**: the R103/R108/R115 one-time marker migrations could never heal a `settings.json` wedged at `report_model_name: gpt-5-nano` with all three markers already `True` — every migration early-returns on its marker, and `model_resolver` reads the highest-precedence settings value verbatim. R116 makes coercion **runtime + state-gated, not marker-gated**: `adoptiq_settings._SCHEMA` gains two `bool` keys `report_model_user_set` / `ask_ai_model_user_set` (default `False`) plus a `is_user_set(model_key)` helper and a `_MODEL_USER_SET_KEYS` map. `app_simple._r69_handle_model_setting` (the `POST /api/settings/model` handler) persists the matching `*_user_set=True` ONLY on a successful test-before-save (the model passed `POST /api/llm/ping`), and sets it `False` when the override is cleared to empty. `model_resolver.get_active_report_model()` / `get_active_ask_ai_model()` route the settings-layer value through `_r116_coerce_settings_value(raw, model_key)`, which heals a stale `gpt-5-nano` to `_HARDCODED_DEFAULT` (Gemini) UNLESS `is_user_set(model_key) is True`. So: wedged nano + no `user_set` → resolves Gemini; operator-picked nano (`user_set=True`) → stays nano; cleared override → Gemini. **Future model-default flips MUST rely on this runtime user-set gate, NOT a new per-round migration marker** — markers are kept for back-compat only; stop adding new `rNNN_model_default_migrated` keys for coercion. Tests that persist `gpt-5-nano` and assert resolver passthrough MUST set `ask_ai_model_user_set: True` / `report_model_user_set: True` (not just the markers) to represent a deliberate selection (the R77 / R103 / R115 suites were updated this way). Pinned by `tests/test_round116_model_heal_robust.py` (13 tests) + updated `tests/test_round77_default_model_flip.py` + `tests/test_round103_ux_cleanup.py` + `tests/test_round115_model_remigration.py`.
- **Round 116 / Build 85 — B (Comprehensive "All Contact Center" headline customer-count anchored on the CC subscription roster)**: R93 strict ACC scoping (`adoptiq_backend._apply_scope_filter_ab`) drops AB rows for customers whose AB carries no Contact-Center tech evidence (the `unknown_total` + `nonmatching_total` exclusions). Those customers then vanished from the narrow `AB ∪ CSOne ∪ Pulse` headline universe `cm.count_customers(ab=_ab, csone, pulse)` even though they ARE in the manager's Contact-Center subscription roster — the Brian Frazier / All Contact Center 90d run surfaced "24"/"15" where the team carries ~37-49 CC customers. **For "All Contact Center" runs ONLY** (`status.get('tech') == 'All Contact Center'`), the headline universe is now widened by threading the team CC subscription frame `_r116_acc_subs_df` (= `team_subs_for_customer_counting`) into the SAME four count sites so they agree by construction: the narrow `cm.count_customers(..., subs_df=_r116_acc_subs_df)` headline, the R64 narrow `cm.list_customers(..., subs_df=...)` risk-band bucket universe, the R47 consistency-validator narrow count, AND the Excel `Summary` `count_customers(..., subscriptions_df=...)` cell. **The `AB_Detail_All` sheet stays strictly scoped (R93 contract preserved)** and R93 exclusions still surface as `tech_filter_scope_excluded` partial-data warnings so nothing is silently lost. Named-tech / Compact / Renewal / Leader runs keep the pre-R116 activity-narrow universe (the gate is exact-string `'All Contact Center'`) so the R47/R49/R50/R64 parity + title-page-coherence contracts are untouched. A `[[CUSTOMER_COUNT]] Round 116 / B` provenance log records `tech` / `acc_gate` / `wide_universe` / `subs_rows_for_count` per run. Pinned by `tests/test_round116_acc_customer_count_floor.py` (11 tests — a CC-subscription customer with only unknown-tech AB rows + no scoped CSOne/Pulse is still counted, AND the AB sheet stays strict). The exact live floor (~37-49) is confirmed by a VPN-gated regen (documented in `CURSOR_MAC_BUILD_INSTRUCTIONS.md`); `scripts/r114_audit_reports.py --auto` surfaces `customers_in_portfolio` and flags `< 30` on a `contact_center` report as a suspected under-count.
- **Round 116 / Build 85 — C (Admin Console link is port-aware; Quit control has a visible label + divider)**: the navbar `#adoptiq-quit-btn` was icon-only (`⏹`) and sat flush against the nav links, so a mis-click read as "the Admin Console tries to shut down." `templates/base.html` now gives the Quit button a visible `<span class="quit-btn-label">Quit</span>` and a `<span class="adoptiq-quit-divider">` separating it from the nav links. The Admin Console link is now port-aware: `app_simple.inject_version` resolves `admin_console_url` / `admin_console_port` via `enhanced_admin_dashboard_v2._resolve_admin_port()` (mirrors the Round 80 `_live_main_url` pattern) instead of the hardcoded `:5152`, defaulting to `5152` on any resolve error. **The shutdown JS (`static/js/quit_adoptiq.js`) binds ONLY to `#adoptiq-quit-btn` — never `.nav-link` or the admin anchor** (the Admin Console link is a plain `target="_blank"` anchor that never POSTs to `/api/shutdown` or `/admin_quit`). New navbar controls that stop the server MUST use a dedicated id + visible label and MUST NOT bind shutdown to a generic selector. Pinned by `tests/test_round60_quit_button_template.py` (extended: visible label, divider, port-aware non-hardcoded admin href, admin href != shutdown endpoints, JS binds only the quit id, `_resolve_admin_port` returns an int).
- **Round 116 / Build 85 — E (settings test-isolation; no test may touch the real `settings.json`)**: an autouse `tests/conftest.py` fixture monkeypatches `adoptiq_settings._app_support_dir` to a per-test `tmp_path` so NO test (or CLI run that imports the suite) can read/write the real `~/Library/Application Support/AdoptIQ/settings.json` — the corruption channel that wedged the live file at nano. Tests that need their own settings dir MAY still `monkeypatch.setattr(adoptiq_settings, "_app_support_dir", ...)` themselves; that later override wins because it runs after the autouse fixture. **New settings-writing tests MUST NOT assume or hardcode the real App-Support path** — rely on the autouse isolation. Pinned by `tests/test_round116_model_heal_robust.py` (the heal tests round-trip `save_settings` against `tmp_path`).
- **Round 117 / Build 86 — A (full form reset after a job successfully starts; R91 single-window flow preserved)**: both report forms reset to fresh-page-load state immediately after a successful submit — preserving the Round 91 contract (still calls `recordStartedJob`, still NO redirect to `/progress/`). `templates/analyze.html` adds `resetAnalysisFormAfterSubmit()` (called in the `handleFormSubmission` success branch) which does `elements.form.reset()` + `hideFilePreview()` + clears the custom subscription filter (`subscription-search` / hidden `subscription_id` / `#selected-subscription` UI) + clears `customer_name` + re-checks `#comprehensive` (the report-type radios live OUTSIDE `#analysisForm` so `form.reset()` can't touch them) + calls `syncReportTypeRequirements()` + `checkSubscriptionFieldStates()` + strips `is-valid`/`is-invalid` validation classes. `templates/leader_report_form.html` adds `form.reset()` + clears the `#csone_file` value + strips validation classes in its success branch. **New report forms that adopt the R91 in-place submission flow MUST reset in the success branch (preserving `recordStartedJob` + no-redirect) so the next job queues from a clean state.** Pinned by `tests/test_round117_form_reset_and_csone_instructions.py`.
- **Round 117 / Build 86 — B (CSOne file-input binding pinned to `#csone_file`)**: `templates/analyze.html` resolves `elements.fileInput` via `document.getElementById('csone_file')` with a fallback to the form's generic `input[type="file"]` — NEVER a bare `document.querySelector('input[type="file"]')`. When the intel-upload feature is enabled, its `<input type="file">` can render first in the DOM, so a generic selector would wire the file preview / validation / reset to the wrong input and silently skip the actual CSOne field. **Any new handler that touches the CSOne upload (preview, validation, reset) MUST target `#csone_file` by id.**
- **Round 117 / Build 86 — C (config-driven CSOne report link + export instructions)**: `Config.CSONE_REPORT_URL` (env-overridable, baked default = the shared CSOne report `00OfX000001Nnh2UAC`) is wired through `app.config['CSONE_REPORT_URL']` and exposed by the `inject_version` context processor as `csone_report_url`. Both report forms (`analyze.html`, `leader_report_form.html`) and the Help page (`help.html` `data-sources` section) render a CSP-safe Bootstrap-collapse 5-step "How do I generate this report?" workflow (open report → top-right dropdown → Export → Standard → upload), linking to `csone_report_url` only when set (mirrors the `{% if csone_shared_folder_url %}` guard). The collapse uses `data-bs-toggle="collapse"` (no inline `onclick`, CSP-clean) with per-form unique target ids (`#csoneHowToCollapse` / `#csoneHowToCollapseLeader`). Pinned by `tests/test_round117_form_reset_and_csone_instructions.py`.
- **Round 117 / Build 86 — D (Compact executive stub-bullet parity with Comprehensive R78/B1)**: `executive_intelligence_formatter.py` (the Compact executive Word path — the lone holdout) now drops pure `"<Category>: Data unavailable."` acknowledgement bullets via `_r117_is_stub_bullet(bullet_text)` in `_parse_and_add_content`'s bullet branch. The helper lazy-imports the SSoT regex `adoptiq_backend._R78_STUB_RE` with a local `_R117_STUB_RE_FALLBACK` so a broken import edge can never block report generation — the canonical regex still wins. Substantive bullets with content after the marker (`"Operational Disruption: Data unavailable. No active incidents."`) are PRESERVED. **New narrative formatters MUST route bullet emission through the R78 stub filter (or `adoptiq_backend.append_to_word_report`, which already does) so no report ships an empty acknowledgement bullet.** Pinned by `tests/test_round117_form_reset_and_csone_instructions.py`.
- **Round 118 / Build 87 — ACC customer-count secondary-attribution fix (the live root cause R116/B only half-fixed)**: R116/B anchored the "All Contact Center" headline universe on the team CC SUBSCRIPTION roster (`_r116_acc_subs_df`), but the roster was itself short because `adoptiq_backend.get_subscriptions_for_team` matched ONLY the primary DSM owner column. The LIVE DSM table `CX_DB.CX_SWSSBST_BR.dsm_assignment_data` attributes secondary owners via FIVE numbered slots `DSM_EMAIL1`..`DSM_EMAIL5` (paired with `DSM_ASSIGNED_1..5` / `DSM_ID1..5`), NONE of which were in `_R82_SECONDARY_DSM_EMAIL_CANDIDATES` — so a Brian Frazier CSSM who owned an account through slot 2-5 (but was not the denormalized `PRIMARY_DSM_EMAIL`) was silently dropped, leaving the live ACC Comprehensive count stuck at 24 (Build 86) with `team_subs_diag.secondary_rows == 0`. **The R82 SSoT contract — `get_subscriptions_for_team` consults the primary column + EVERY present secondary candidate, UNIONs, and de-dupes on `(SUBSCRIPTION_ID, ACCOUNT_ID_C, BU_NAME, CSSM_EMAIL)` — is unchanged; R118 only adds the five live slot names to the secondary candidate tuple** so the fan-out actually reaches them. `NEXT_ACTION_OWNER_EMAIL` is deliberately EXCLUDED (it is the next-action owner, not an ownership attribution; matching it would over-attribute). `DSM_EMAIL1` IS included (it dedups against `PRIMARY_DSM_EMAIL` on the shared key so it cannot double-count, but it catches accounts whose denormalized primary points elsewhere while a roster member sits in slot 1). The fix is centralized in the single helper, so Comprehensive / Leader / Renewal / Compact ALL inherit the corrected fetch automatically. Secondary slots were live-discovered via `scripts/r118_dump_dsm_columns.py` (calls `introspect_dsm_columns` directly, bypassing the CSRF-gated `/api/diag/dsm-columns` endpoint; read-only, no PII, never writes). The diagnostic's `all_email_like_columns` escape-hatch filter was ALSO widened from a `*_EMAIL` suffix match (which MISSED the digit-suffixed slots — exactly why this gap survived R82) to any column containing `EMAIL`; this is visibility-only and does NOT widen the WHERE clause (still gated by the conservative allow-list). **The live-verification lesson from R116 is the explicit ship gate: synthetic-fixture pytest passing is necessary but NOT sufficient for DSM-attribution fixes — the operator MUST regenerate the Brian ACC Comprehensive on VPN with Build 87 and confirm `Customers in portfolio > 24` + `secondary_rows > 0` BEFORE the push to main.** Pinned by `tests/test_round118_acc_secondary_dsm_slots.py` (9 tests).
- **Round 118 / Build 87 — Compact prose fold-in (post-audit)**: a read-only DOCX audit of the Build 86 reports surfaced three model-independent Compact regressions, fixed here. **(F1)** `executive_intelligence_formatter.add_executive_dashboard` Risk Summary MUST render `"Moderate Risk (band)"` (canonical MODERATE vocabulary, R67/B1) — the lone pre-R118 `"Medium Risk (band)"` line disagreed with the rest of the Compact + Renewal narratives. The canonical key stays `medium_risk_customers`; only the user-facing word changed. **(F2)** TAC cases MUST route through `executive_intelligence_formatter._r118_dedup_tac_cases(df)` (collapses on the first present id col `Case #`/`SR Number`/`CaseNumber`/`case_id`/`CASE_NUMBER` with `keep='first'`, mirrors the R78/B2 Leader Action_Plans dedup-by-ID) at BOTH `csone_norm` ingestion points — the R40 per-customer TAC→subscription join fans a single case to one row per matched subscription, so without dedup a case rendered N× in the Lifecycle Snapshot AND `canonical_metrics.count_total_tac` (a `_safe_len`) over-counted the KPI. **(F3)** `app_simple._r69_sanitize_llm_error` MUST humanize a residual provider JSON/dict envelope (`Error code: 429 - {'error': {'message': ...}}`) down to its inner `message` when a `{` survives the R112 secret-redaction pass — gated on the `{` so plain-prose 429s pass through untouched (preserves the R112 `test_sanitizer_preserves_non_credential_text` + `<redacted>`-blob contracts; redaction runs FIRST so secrets never survive the collapse). Pinned by `tests/test_round118_compact_prose_fixes.py` (14 tests). The Comprehensive 24-vs-12 headline contradiction the same audit flagged is the documented R47-COMP-CUSTCOUNT-PARITY narrow-vs-wide split confounded by the run's 429 blackout — deliberately NOT changed (see QUALITY_AUDIT.md R118 handoff).

- **Round 128 / Build 97 — Relaunch-to-update CTA + apply lock**: when `GET /api/update/status` reports `update_available` and `mode !== 'off'`, the top banner (`#r119-update-available-banner` via `static/js/r68_restart_banner.js`) MUST show **Relaunch to update** in **both** `auto` and `notify` — never hide the only manual escape hatch in auto mode. Status exposes `can_apply_now` and `apply_in_progress`; the CTA disables when busy. Manual and worker apply paths MUST route through `_r128_invoke_apply_update()` so concurrent apply cannot spawn two swappers (`reason: already_in_progress`). Pinned by `tests/test_round128_relaunch_update_banner.py` + updated `tests/test_round119_auto_update_build_and_ui.py`.
- **Round 119 / Build 88 — cross-platform auto-update (Tier C silent self-replace)**: the app self-updates from a machine-readable `latest.json` manifest at the synced OneDrive Releases root `AI Projects/OUTBOX` (per-platform artifacts in `AdoptIQ/` for the Mac DMG and `AdoptIQ_PC/` for the Windows EXE). The engine lives in the decoupled `auto_updater.py` module (stdlib + `config` only — NO `app_simple` import, fully offline-testable). **Safety contract that MUST hold (any violation re-introduces the broken-install risk this design exists to avoid):** (1) **never swap an unverified artifact** — `stage_and_verify` enforces sha256 mandatory AND macOS `codesign --verify --deep --strict` mandatory (Windows Authenticode is a soft-check while the EXE is unsigned; flip `_PC_REQUIRE_SIGNATURE` when it gets signed); (2) **never auto-update mid-analysis** — `apply_update` is idle-gated through the SAME `analysis_status` running-check as `/api/shutdown` and a busy/unprovable-idle state defers to notify (a broken `is_busy` check is treated as busy, never as idle); (3) **any failure degrades to the notify banner** — every path returns `{state: 'notify', error_kind: ...}` instead of raising, so the operator never ends up with a half-installed app; (4) **the swapper runs detached from the bundle** (written to `$TMPDIR`/`%TEMP%`, NOT inside the bundle) and **keeps `<name>.old` for one boot** for manual rollback. The manifest writer `scripts/write_release_manifest.py` is **merge-aware** — each build host (Mac DMG on macOS, Windows EXE on Windows) writes ONLY its own platform slot and recomputes the informational top-level `build` as the max, so a Mac-only bump never makes a Windows install think a newer EXE exists (per-slot `build` is the authoritative value `is_update_available` compares against `ADOPTIQ_BUILD`). `auto_updater.apply_update` carries a TESTING short-circuit (`testing=True` returns `would_update` without staging/spawning) so pytest can NEVER trigger a real swap; `start_update_check_worker` is a no-op under pytest. The per-machine kill switch is `adoptiq_settings.auto_update_mode` (allow-listed `off`/`notify`/`auto`, default `auto`, validated by `is_valid_auto_update_mode`), flippable on the Preferences hub via `POST /api/settings/auto-update-mode` (CSRF dual-auth) with no rebuild. Endpoints: `GET /api/update/status` (read-only, loopback-safe, no secrets/PII), `POST /api/update/apply` (CSRF dual-auth, 409 `needs_force` when busy, TESTING short-circuit). **Future changes MUST preserve all four safety-contract invariants AND keep the manifest writer per-platform-slot-merge-aware; any new LLM/network call MUST NOT be added to the swap path (the swap touches only the local OneDrive mirror + the verified local artifact).** `auto_updater` is pinned as a hidden import in both `adoptiq_mac.spec` and `adoptiq_pc.spec`. Pinned by `tests/test_round119_auto_update_manifest.py` + `_engine.py` + `_endpoints.py` + `_build_and_ui.py` (65 tests).

- **Round 120 / Build 89 — report-accuracy sweep (F1-F7, presentation-only)**: a read-only audit of the four Build 88 reports surfaced seven model-independent accuracy/grammar issues, all fixed here WITHOUT touching any SSoT count/score logic (`canonical_metrics` / `risk_scoring` unchanged; the R47-COMP-CUSTCOUNT-PARITY narrow-vs-wide split is preserved — F1 only disambiguates the label). **(F1)** the Comprehensive Portfolio Overview narrow customer count (`total_customers_canonical_narrow`, the AB∪CSOne∪Pulse engaged subset) MUST render under a distinct label (`"Customers with adoption barriers, support cases, or pulse activity"` in the overview paragraph; `"Customers (AB/Cases/Pulse)"` as the dashboard tile header) so it never reads `Total Customers: 13` next to the title page's `Total Customers: 33` (the wide scored roster). **(F2)** `report_source_injector._rewrite_paragraph_with_inline_citations` pure-punctuation boundary branch MUST advance `cursor = value_end` (NOT `m.end()`) so the original whitespace before a `|` separator survives — pre-R120 the citation jammed against the pipe (`…1.2 [Source: …]| High…`). Preserves R57 / R66/B1 / R76 / R90 contracts. **(F3)** the Compact non-AI fallback (`_generate_comprehensive_fallback_insights`) MUST route the LLM error through `_r120_humanize_fallback_reason(llm_error)` which calls `_r69_sanitize_llm_error` FIRST (R112 redaction contract preserved) then maps a leading `ERROR: <kind>` to a customer-readable `"Reason: …"` sentence — the raw `[LLM error] ERROR: …` marker MUST NOT reach customer prose. **(F4)** Renewal TAC + AB detail brackets MUST be built via `_r120_kpi_detail_bracket(pairs, age_str=…)` which omits any field whose value normalizes into `_R120_UNKNOWN_TOKENS` (`""`/`n/a`/`unknown`/`none`/`nan`/`null`/`--`/`-`) — kills the 232× `Status: Unknown, Type: unknown` flood while keeping Severity/known fields. **(F5)** `leader_report_generator._r120_pluralize(n, singular, plural=None)` MUST drive the portfolio-overview count nouns (`customer account`, `adoption barrier`, `action plan`, `TAC case`) so a count of 1 reads singular. **(F6)** the Leader team-totals row MUST set the Sentiment column (`totals_cells[6]`) to an em-dash `"—"` — NOT `"Team Avg"` (col 6 is Sentiment, not an activities column; the true average lives in Key Insights). **(F7)** `_compute_customer_health` MUST drop rows whose customer name is a sentinel via `_r120_is_customer_sentinel` (`_R120_CUSTOMER_SENTINELS`) so a bare `Unknown | 482.6 | …` row never reaches the Customer-Health table. **Future report-prose changes that surface a possibly-Unknown KPI field MUST route through `_r120_kpi_detail_bracket` (brackets) or `_r120_is_customer_sentinel` (row suppression) rather than rendering the normalized sentinel; count-noun prose MUST use `_r120_pluralize`.** Pinned by `tests/test_round120_report_accuracy_sweep.py` (31 tests) + updated `tests/test_round112_build80_acceptance_fixes.py` (F3 wiring).
- **Round 121 / Build 90 — report-accuracy residual sweep (G1-G4, presentation/normalization-display only)**: the Build 89 live-re-acceptance audit confirmed F1-F7 mostly held but found three fixes that landed on one render path and missed a sibling, plus one real status-display bug. No SSoT count/score logic changed. **(G1)** the Renewal BEMS-escalation list (`app_simple.py` ~L13549) MUST guard the `(Type: <case_type>)` run with `if str(case_type).strip().lower() not in _R120_UNKNOWN_TOKENS:` — the F4 TAC-bracket de-sentinel contract, applied to the sibling BEMS bracket that pre-R121 emitted `(Type: unknown)` 77×. **(G2)** the Leader portfolio-overview follow-on sentences in `leader_report_generator.py` (`requires immediate attention`, `elevated activity`, mixed-health, and the `high-priority adoption barriers` line, ~L3146-3183) MUST route count nouns through `_r120_pluralize(...)` (Build 89 only pluralized the opening "Portfolio shows N…" line) so a count of 1 reads singular. **(G3)** BE Focus Areas / BE Priority Barriers MUST relabel a bare sentinel theme/technology to `"Other / Unclassified"` at the DISPLAY layer via `be_priority_scorer.relabel_unclassified(value)` (the SSoT helper; maps only exact sentinel tokens — `""`/`unknown`/`other/unknown`/`nan`/`none`/`n/a`/`na`/`unclassified` — so genuine labels like `"Unknown Protocol"` survive). Applied at `compute_be_focus_areas` row construction (`Technology`/`Theme`) AND `be_priority_pipeline` barriers rows (`Technology`/`Sub_Technology`); grouping/scoring keys are UNTOUCHED so row counts + ordering are preserved. Word headings + XLSX sheets inherit the relabel. **(G4a)** `data_normalization.add_case_lifecycle_fields` MUST, for the `closed_date present AND case_status_norm == "Unknown"` mask, ALSO set `case_status_norm = "Closed"` (not just flip `is_closed`/`is_open`) so a case carrying Opened+Closed timestamps renders an accurate Status; this is the source-of-truth normalizer so all consumers inherit it, and counts are unaffected (the boolean was already correct). **(G4b)** the support-case `Type` table cell (`executive_intelligence_formatter` lifecycle table cell 6) MUST render via `_r121_display_case_type(value)` which maps only the bare `unknown`/blank/`nan` sentinel to `"Unclassified"` — the underlying `case_type_class` value is NOT mutated. **Future report-prose / table changes that surface a possibly-Unknown technology/theme/type label MUST route through `relabel_unclassified` (BE) or `_r121_display_case_type` (support-case Type) rather than rendering the bare sentinel; count-noun prose MUST use `_r120_pluralize`; any `(Type: …)` / bracketed-KPI run MUST be guarded by `_R120_UNKNOWN_TOKENS`.** Pinned by `tests/test_round121_report_residual_sweep.py` (14 tests) + updated `tests/test_round8_data_normalization_behaviour.py` (G4a) + `tests/test_round79_b3_be_focus_areas_rollup.py` (G3 display relabel).

- **Round 122 / Build 91 — XLSX sentinel display-relabel + corpus-context residual + Ask AI recall (H1/H2 presentation-only; reranker recall lever)**: the Build 90 live-re-acceptance audit confirmed the R121/G3 BE display-relabel held but found two residual surfaces still leaking bare normalizer sentinels, plus a bounded Ask AI recall lever. No SSoT count/score logic changed; all relabels are display-only and count-safe. **(H1)** `report_corpus_context._relabel_corpus_sentinel(value)` relabels a bare `Other/Unknown`-style sentinel in a Historical-Context theme/technology label to `"Other / Unclassified"` (delegates to `be_priority_scorer.relabel_unclassified` via lazy import so the label/token set cannot drift) BUT — unlike the BE helper — leaves a BLANK value blank so an absent technology still renders no `"(...)"` bracket / no header bit. The relabel runs at `build_historical_context` construction time (`HistoricalTheme.theme`/`.technology` and `HistoricalEntry.technology`) so BOTH render paths (`render_to_text` + `render_to_word`) inherit it. **(H2)** `data_normalization.relabel_display_sentinels(df, column_label_map, *, tokens=None)` returns a shallow `df.copy()` with bare sentinels in NAMED columns rewritten to a display label via `Series.map` — count-safe (never drops/reorders rows), canonical-safe (caller's frame never mutated), conservative (only exact case/whitespace-insensitive matches against `_DISPLAY_SENTINEL_TOKENS` = `unknown`/`other/unknown`/`other / unknown`/`nan`/`none`/`n/a`/`na`; blank/NaN left untouched so `"Unknown Protocol"` survives). The XLSX export SSoT `report_export_schema.apply_export_schema` wires it via `_R122_DISPLAY_RELABEL_MAP` (`case_type_class` → `"Unclassified"`; `sub_technology`/`ab_category_final`/`Barrier Category (Final)` → `"Other / Unclassified"`) as the LAST step (post friendly-header rename, on the row-preserving projection copy) so every workbook writer — Compact/Renewal/Leader/subscription one-shots in `app_simple.py` + Comprehensive in `adoptiq_backend.write_excel_workbook` — inherits it with zero per-writer wiring. **(Ask AI)** `Config.ASK_AI_RERANK_CANDIDATE_K` widened 30 → 40 (env-overridable, no rebuild) so a truly-relevant chunk that RRF ranked at position 31-40 now reaches the cross-encoder reranker; the in-code fallback in `ask_ai_grounded` was kept at 40 in parity. The synthetic Ask AI eval runs lexical (reranker not exercised) so it stays a regression guard at 75/75; the recall uplift is validated on the live VPN smoke. **Future report-prose / table / corpus changes that surface a possibly-Unknown technology/theme/type label MUST route through the matching SSoT helper — `_relabel_corpus_sentinel` (corpus context, blank-preserving), `relabel_unclassified` (BE), `_r121_display_case_type` (support-case Type cell), or `relabel_display_sentinels` (XLSX export column) — rather than rendering the bare sentinel; new XLSX columns that can carry a sentinel MUST be added to `_R122_DISPLAY_RELABEL_MAP`, never relabeled inline in a writer.** Pinned by `tests/test_round122_residual_and_askai.py` (25 tests: H1 helper + construction + both render paths + blank-preservation + BE label parity; H2 column-scoped relabel + no-row-drift + canonical-not-mutated + blank/NaN preservation + apply_export_schema end-to-end incl. the renamed-category column; Ask AI candidate-pool default + in-code fallback parity + composer-seam smoke).

- **Round 123 / Build 92 — Customer Health Grade canonical-grounding (hybrid: ground briefing + deterministic stamp)**: the Build-91 deep dive found everything clean EXCEPT one genuine accuracy defect — the Comprehensive per-customer `Customer Health Score: <letter>` (and the `Portfolio Health Score`) were fully LLM-discretion and routinely CONTRADICTED the canonical `Risk_Components` band on the SAME report (ZONES INC = HEALTHY/5.6 graded `F`; WINTRUST = HIGH/62 graded `C`; ~15/31 customers drifted). The per-customer briefing omitted `risk_profiles`, so the model graded from qualitative case text; Round 27 only stripped the bracket leak, never corrected the VALUE. The hybrid fix has three layers, each its own SSoT contract: **(1) Canonical band→letter SSoT** — `risk_scoring.band_to_health_grade(band, score=None)` maps `HEALTHY→A, LOW→B, MEDIUM/MODERATE→C, HIGH→D, CRITICAL→F` (case/whitespace-insensitive; score-fallback via `RISK_BAND_THRESHOLDS` through `_risk_band` when band missing/unrecognised; defaults `A` only with no signal). `health_grade_for_profile(profile)` reads `risk_band`→`risk_score_0_100`; `portfolio_health_grade(summary)` grades from `average_risk_score_0_100` (average, not worst-case, intentional). **(2) Ground the briefing** — `app_simple.py` threads a single-entry `risk_profiles={customer_name: profile}` into the per-customer `_create_briefing_book(...)` call so the briefing emits the "Canonical Risk Bands" block for THIS customer; the briefing block in `adoptiq_backend.py` was widened to read BOTH the legacy `risk_score`/`risk_level` keys AND the canonical `compute_customer_risk_profile` shape (`risk_score_0_100`/`risk_band`) — pre-R123 it only read the legacy keys so a canonical profile silently read 0.0/HEALTHY. The prompts (`PROMPT_CUSTOMER_TEMPLATE` + `PROMPT_PORTFOLIO_TEMPLATE`) are pinned to require the grade equal the canonical band mapping. **(3) Deterministic stamp (the guarantee)** — `adoptiq_backend.stamp_customer_health_grade(narrative, canonical_letter)` / `stamp_portfolio_health_grade(...)` overwrite the LLM letter with the canonical mapping after generation: pure, idempotent, tolerant of optional `**`/`[ ]`/whitespace/case chrome, refuses to half-rewrite a stray "Critical" word (negative lookahead `(?![A-Za-z])`), never stamps an out-of-range letter, and preserves heading/bold chrome + non-grade brackets. The customer stamp runs in the per-customer loop BEFORE the R27 grounding gate so the corrected narrative passes validation; the portfolio stamp runs before `parse_ai_output_and_add`. Drift is recorded PII-safely via `app_simple._r123_record_health_grade_outcome(status, customer_name=…, llm_letter=…, canonical_letter=…, scope=…)` into `status['health_grade_diag']` (`{total, stamped, drifted, by_customer:{<_id_digest>}}`; only drifted cases populate `by_customer`; the literal `"portfolio"` key for the portfolio grade). **Renewal's deterministic numeric `Adoption Health Score: X/100` (`advanced_renewal_analyzer.py`) is already grounded and was NOT touched. Future report formats that publish a letter health grade MUST derive it via `risk_scoring.band_to_health_grade` / `health_grade_for_profile` / `portfolio_health_grade` and deterministically stamp it post-generation via the `adoptiq_backend.stamp_*_health_grade` helpers — never let the LLM author the grade value from qualitative text.** Pinned by `tests/test_round123_health_grade_grounding.py` (35 tests: exhaustive band→letter mapping + MODERATE alias + score fallback + threshold consistency; extract/stamp shapes incl. idempotency, out-of-range refusal, word-corruption guard, non-grade-bracket preservation; drift-diag rollup + PII digest + portfolio key + no-LLM-letter path; end-to-end band→stamped-letter; source-shape pins for briefing threading + prompt grounding + both-key-shape briefing read).

- **Round 124 / Build 93 — Build 92 report accuracy + stability sweep (11 fixes + 2 investigations)**: a four-audit pass over the Build 92 Comprehensive / Compact / Renewal / Leader pair surfaced 11 confirmed accuracy/presentation defects and two suspected issues. **F1 (en-dash AP fold)** — `canonical_metrics._normalize_ap_status_for_open_check` now translates Unicode dashes (en U+2013, em U+2014, figure U+2012, minus U+2212, non-breaking hyphen U+2011) to ASCII `-` BEFORE whitespace-collapse so `"Closed – Cancelled"` (en-dash) counts as closed across Comprehensive/Compact/Leader in one SSoT place (was over-counting open APs by 3). **F2 (Leader AP/CP over-count)** — `leader_report_generator._create_summary_table` + `Team_Summary` + `_create_adoptiq_summaries_per_person` now dedup per-CSSM `action_plans` / `customer_pulse` by `ID` (mirrors the R53.2 AB block) so shared-account rows attributed to multiple CSSMs via the R72 `_ATTRIBUTED_BY_ACCOUNT` pathway count once; TAC stays a sum (one CSSM per row by construction). **F3 (Compact portfolio grade grounding)** — the Compact path now builds `risk_profiles` + `portfolio_risk_summary`, threads a `### Canonical Risk Bands` block into `_create_executive_briefing_book_with_csone` (via `app_simple._r124_render_canonical_risk_bands`), adds a `Portfolio Health:` label alias to `extract/stamp_portfolio_health_grade`, and deterministically stamps the portfolio grade after the R27 gate with drift recorded via `_r123_record_health_grade_outcome` (was grading `[F]/IN CRISIS` while canonical was 1.1/10 grade A). **F4 (Compact per-customer band)** — `adoptiq_backend.stamp_compact_customer_bands` rewrites each `**N. [Customer] – Risk: [BAND]**` line to `risk_scores[customer]['risk_band']` so narrated CRITICAL bands can't contradict the canonical HIGH band. **F5 (Compact BEMS/case briefing consistency)** — `_create_executive_briefing_book_with_csone` applies `_r118_dedup_tac_cases` to `csone_df` BEFORE computing portfolio + per-customer BEMS/TAC totals so the briefing (was raw `len(csone_df)`) matches the Word dashboard (95/343). **F6 (Comprehensive grade-vs-prose drift)** — `adoptiq_backend.reconcile_portfolio_prose_band(narrative, canonical_letter)` (via `_R124_GRADE_TO_BAND_WORD`: A→HEALTHY, B→LOW, C→MEDIUM, D→HIGH, F→CRITICAL) rewrites the `<BAND> risk state/posture` prose word to match the stamped grade, and `strip_rate_not_available` drops the stray `"(Rate not available)."` LLM artifact; both wired into the Comprehensive AND Compact portfolio stamp paths in `app_simple.py`. **F7 (BE Focus Areas Open ABs)** — `be_priority_word_section` reads `row.get("Open_Barriers")` with an `Open_Count` back-compat fallback (the scorer writes `Open_Barriers`; the cell was rendering a hard 0). **F8 (uncategorized relabel)** — `be_priority_scorer._R121_UNCLASSIFIED_TOKENS` gained `"uncategorized"` so the Theme column relabels to "Other / Unclassified". **F9 (Comprehensive banner scope-vs-load)** — `executive_report_builder.add_partial_data_warning_banner` ports the R112/F5 `_r124_scope_kinds` / all-scope branch so a pure `tech_filter_scope_excluded` warning says "filtered out by the requested scope" instead of "failed to load". **F10 (XLSX chrome strip)** — `app_simple.py` strips a trailing `[Source: …]` citation from `risk_factors[0]` before assigning the `Risk_Components.Top_Risk_Factor` XLSX cell (Word narratives keep their citations). **F11 (stub bullets)** — `adoptiq_backend._R78_STUB_RE` widened to tolerate a trailing `[Source: …]` citation and the `Pattern N:` category shape so `"Pattern 3: Data Unavailable [Source: …]"` empty bullets drop before write (negative-controlled so substantive `Pattern N:` bullets survive). **I1 (missing C-grade customers)** — `app_simple._r124_deterministic_grade_line(profile)` re-emits a `Customer Health Score: <letter> (<band>; canonical risk X.X/10)` line on all per-customer narrative withhold/failure/exception fallback paths so a profiled customer (the Build-92 T-MOBILE C-grade / UPMC) whose LLM body was suppressed by the R27 gate still keeps a grounded grade in the doc. **I2 (Leader catch-all attribution)** — the per-CSSM `account_ids` set in `leader_report_generator._collect_team_data` now strips + drops blank / whitespace-only / `nan` / `none` / `null` sentinels (was only `.dropna()`), so an empty-string account id no longer acts as a wildcard in `_slice_by_owner_or_account`'s `isin()` check matching every blank-account task row (the suspected Ron Estillore 96-customer over-attribution); mirrors the `if a` guard already used for the all-team `primary_account_set`. **Future report formats MUST derive letter grades + band prose via the canonical risk helpers and reconcile/stamp post-generation; per-CSSM account-id sets MUST filter blank/sentinel ids before any `isin()` attribution.** Pinned by `tests/test_round124_*.py` (68 tests: en-dash fold, Leader AP/CP dedup parity, Compact grade+band+label, Compact briefing dedup, portfolio prose-band reconcile + rate-strip, BE Open_Barriers cell, uncategorized relabel, scope-vs-load banner, XLSX chrome strip, Pattern-N stub pos/neg, deterministic grade line, account-id catch-all). Floor 5998 → 6068.

- **Round 125 / Build 94 — Build 93 report accuracy + stability sweep + auto-update hardening**: a follow-up audit of the Build 93 Comprehensive / Compact / Renewal / Leader pair plus a trace of the in-the-wild auto-update path. **A1 (Comprehensive missing narratives)** — the per-customer loop iterates `all_customers` (a narrower set than `risk_profiles`), so 71/249 + 2/33 profiled customers (UPMC, ENDURANCE) never got an Executive Analysis or stamped grade; the loop universe is now the union of `all_customers` with `risk_profiles.keys()` so every profiled customer is narrated (or gets a deterministic stamped-grade mini-section via R124/I1's `_r124_deterministic_grade_line`). **A2 (grade↔prose drift)** — `reconcile_portfolio_prose_band`'s band-word phrase set was extended to catch `"MEDIUM risk state"` (and hyphenated `"medium-risk"`) so a `Portfolio Health Score: B` paired with "in a MEDIUM risk state" collapses the prose to LOW; confirmed wired on the comprehensive portfolio narrative path. **A3+B3 (scope banner)** — `tech_filter_empty_after_scope` is added to the scope-kind set in `executive_report_builder.add_partial_data_warning_banner` AND the Compact banner path so an AB frame emptied BY the requested scope says "filtered out by the requested scope" instead of "failed to load" (parity with `tech_filter_scope_excluded`). **A4 (plain-paragraph stubs)** — the R78/B1 `_R78_STUB_RE` filter only ran inside the bullet branch of `append_to_word_report`; the Build 93 audit found `"Pattern N: Data Unavailable [Source:...]"` stubs leaking as plain paragraphs (Brian 5, All-Managers 43). A second filter call site normalises an optional leading bullet marker then applies the same regex BEFORE the heading branch (headings can never match the `^\*{0,2}[A-Z]` anchor). Every `_R78_STUB_RE.match(` site keeps the `i += 1` + `continue` infinite-loop guard. **A5+A6 (All-Managers portfolio grade + BEMS)** — Portfolio Health Score stamping is wired on the All-Managers comprehensive path; the BEMS count is reconciled to the canonical distinct-ID. **B1+B4 (ELEVANCE name join)** — the compact high-risk surface routes customer names through `data_normalization.normalize_composite_customer_key` so the corrupted composite `ELEVANCE_ELEVANCE HEALTH_US` renders as `ELEVANCE HEALTH` and the Executive_Dashboard high-risk count joins to the canonical 7 (matching Risk_Summary/High_Risk_Customers). **B2+B5+B6 (compact prose grounding)** — the ungrounded portfolio-average claim (26.4) is deterministically reconciled to the canonical mean via `reconcile_avg_risk_claim` (handles `/10` and `/100` suffixes); the P2 active-case prose is reconciled via `reconcile_p2_active_claim`; the `Score 4-6 (Watch)` tile uses literal `low=4.0, high=6.0` bounds via `canonical_metrics.count_score_range` (was a band-derived cutoff). **C1 (renewal tech scope)** — renewal CSConsole Action Plans + Customer Pulse frames route through `_filter_csconsole_data_by_technology` for non-"All" tech scopes (ACC parity with AB/SC). **C2+C3+C4 (renewal labels)** — `Key_Metrics.Risk_Score` renamed to the explicit `Risk_Score_0_100`; the Renewal `Report_Info` baseline rows renamed to canonical `Export type` / `Generated at (UTC)`; "medium-risk" recommendations prose aligned to `MODERATE`. **D1+D2 (Leader per-CSSM dedup)** — the per-CSSM `Team_Summary` rows + DOCX activity body rows dedup-by-ID so per-person and headline AP/CP/AB reconcile (extends R124/F2 from headline-only to per-person); synthetic fixtures cover the Dee headline-drift + Shams shared/blank-account catch-all shapes. **E1 (auto-update retry)** — a build is added to `_r119_update_state['attempted_builds']` ONLY on a non-transient (terminal) outcome; `_R125_TRANSIENT_UPDATE_KINDS` (`artifact_missing`, `sha256_mismatch`, etc.) are retried on the next poll so a not-yet-synced DMG doesn't permanently block update until app restart. **E2+E3+E4 (releases_folder + manifest)** — `adoptiq_settings._SCHEMA` + `_VALIDATORS` gain `releases_folder` (validated via `_is_valid_csone_folder_path`, public alias `is_valid_releases_folder`) so the previously-dead `config._resolve_releases_folder` override is now functional; `scripts/write_release_manifest.merge_manifest` is confirmed to preserve an existing `pc` slot when publishing a mac-only manifest (a mac publish never clobbers/invents a pc slot). **Idempotency fix** — the shared `_build_health_grade_value_re` / `_RE_PORTFOLIO_HEALTH_GRADE_VALUE` post-letter whitespace was tightened to `[^\S\n]*` (non-newline) so re-stamping a grade line followed by a blank line is byte-idempotent (was silently collapsing the trailing blank). **Future report formats MUST normalize composite customer keys at the high-risk surface, reconcile ungrounded portfolio-average/active-case prose against canonical metrics, and route per-scope CSConsole frames through `_filter_csconsole_data_by_technology`; auto-update workers MUST only mark a build attempted on a terminal failure so transient not-yet-synced artifacts stay retriable.** Pinned by `tests/test_round125_*.py` (44 tests: reconcilers + idempotency, composite-name normalization, releases_folder schema/validator/round-trip + manifest pc-slot preservation, scope-banner wording + transient-update-kind backoff, Leader dedup + watch-tile bounds). Floor 6068 → 6112.

- **Round 132 / Build 102 — customer alias normalization (NYU/NYULH hotfix + SSoT)**: when CSOne / Snowflake / AB frames carry **different strings for the same organization** (e.g. `NYU MEDICAL CENTER` vs `NYU LANGONE HEALTH SYSTEMS`), joins and scope filters MUST NOT treat them as separate customers. **`data_normalization.load_customer_alias_registry` / `canonical_customer_name` / `customer_names_match` / `alias_join_keys_for_name` / `collapse_customer_name_set`** are the SSoT; bundled defaults live in **`customer_aliases.defaults.json`** (PyInstaller `datas`); operator overrides in App Support **`customer_aliases.json`**. **Canonical display name** = longest matching scoped **`BU_NAME`** from `team_subs_df` among aliases in the group (tie-break alphabetical); if no DSM anchor, first alias alphabetically + `partial_data_warnings` `kind='customer_alias_no_dsm_anchor'`. Wired paths: **`adoptiq_backend._apply_scope_filter_csone`** (expand roster join keys), **`app_simple._slice_customer`** (alias-aware match), **`canonical_metrics._collect_customer_names`** (collapse universe), **`leader_report_generator` TAC tier-3** (register all alias keys on `cust_key_to_cssm`), **`compact_report_formatter.calculate_renewal_risk_scores`** (pass `subs_df` to `list_customers` + alias-aware slices). **Future customer-name joins/slices/counts MUST route through these helpers — never reintroduce literal `normalize_customer_name(a) == normalize_customer_name(b)` for cross-source equivalence.** Preferences UI for alias editing is a deliberate follow-on. Pinned by `tests/test_round132_customer_aliases.py` (9 tests). Floor 6233 → 6242.

**Frontend:**
- All `target="_blank"` links need `rel="noopener noreferrer"`
- Always check `response.ok` before using fetch results
- Null-check DOM elements before accessing `.value` or `.innerHTML`

**Admin security:** Admin dashboard binds to loopback (`127.0.0.1`) by default. Don't change this default without the `ADOPTIQ_ADMIN_BIND_PUBLIC=1` escape hatch.

**Quit / shutdown (Round 60):** the user can stop the local AdoptIQ process from either navbar (main app `#adoptiq-quit-btn` → `POST /api/shutdown`; admin dashboard `#adoptiq-quit-btn` → `POST /admin_quit`, which proxies to the same main-app endpoint with `X-AdoptIQ-Internal`). The endpoint enforces the same dual-auth as `/api/corpus/refresh` (CSRF token OR `X-AdoptIQ-Internal`), snapshots `analysis_status` under the lock, and returns 409 + `needs_force=True` when one or more entries are `status='running'` so the browser can prompt the user before killing in-flight work. Shutdown is `signal.SIGTERM` (NOT `os._exit(0)` / `SIGKILL`) scheduled via a 0.5s `threading.Timer` so the HTTP response flushes BEFORE the process dies AND the existing `atexit` handlers fire — `_shutdown_handler` saves `analysis_status.json` and `_r17_corpus_shutdown` scrubs the in-memory plaintext corpus temp file. `os._exit(0)` would skip both. In TESTING mode (Flask config OR `ADOPTIQ_TESTING=1` env) the endpoint returns `would_shutdown=True` and skips the kill so pytest does not terminate itself. Source-shape pinned by `tests/test_round60_shutdown_endpoint.py` (19 endpoint tests) and `tests/test_round60_quit_button_template.py` (11 template tests).

**Operator-flippable LLM model selection (Round 69 / Build 43, default flipped Round 77 / Build 53):** the active LLM model for both Ask AI (`ask_ai_model_name`) AND report narratives (`report_model_name`) is independently flippable from the analyze-page card, the ask-ai-page card, AND the admin console. Both surfaces use the **same** dual-auth as `/api/corpus/refresh` (CSRF token OR `X-AdoptIQ-Internal`) and a **shared test-before-save UX** implemented in `static/js/r69_model_preferences.js` — the Save button stays disabled until the operator clicks Test and `POST /api/llm/ping` returns `ok=true` for the proposed model name (empty string is also accepted to clear the override and fall back through the precedence chain). Settings precedence (highest first): `settings.json` (`ask_ai_model_name` / `report_model_name`) → per-feature env (`CIRCUIT_MODEL_NAME_ASK_AI` / `CIRCUIT_MODEL_NAME_REPORT`) → general env (`CIRCUIT_MODEL_NAME`) → hardcoded default (`gemini-3.1-flash-lite` as of Round 77 / Build 53; was `gpt-5-nano` from R69 → R76). All resolution goes through `model_resolver.get_active_ask_ai_model()` / `get_active_report_model()` — these helpers do NOT cache so a UI flip takes effect on the very next call (audit-pinned). `generate_llm_response` and `generate_llm_json_response` accept a keyword-only `model_name=...` that overrides the fallback for one call only; threading is asymmetric by design (Ask AI sites use the Ask AI resolver, report sites use the Report resolver) so the two surfaces stay risk-isolated. Validation is allow-list `^[A-Za-z0-9._-]{1,128}$` (`adoptiq_settings._is_valid_model_name`); upstream CircuIT errors flowing through `/api/llm/ping` are sanitised by `_r69_sanitize_llm_error` before echo (Bearer/JWT/api_key/Authorization redacted, message capped at 200 chars). The active model name is stamped onto every Ask AI diagnostic record AND surfaced in `/api/ask-ai-portfolio` responses + the R68 debug chip footer so the operator can prove which model produced a given answer. **Future endpoints that accept a model name MUST validate via `adoptiq_settings.is_valid_model_name(...)` AND require a successful `/api/llm/ping` BEFORE persisting** — this is the contract the test-before-save UX depends on, and silently writing an unprovisioned model name will land an honest 400/500 in production but only a deferred error after the next LLM call (the Round 67 stale-binary trap class of bug). Source-shape pinned by `tests/test_round69_model_preferences.py` (43 tests covering settings/validation/precedence/threading/endpoints/admin proxy/UI source-shape/diag surface/build metadata + the wires-don't-cross regression guard).

**Default LLM model flipped to gemini-3.1-flash-lite (Round 77 / Build 53):** the hardcoded fallback in both `model_resolver._HARDCODED_DEFAULT` AND `Config.CIRCUIT_CONFIG`'s three env-fallback strings (`model_name`, `model_name_ask_ai`, `model_name_report`) flipped from `gpt-5-nano` to `gemini-3.1-flash-lite`. Both options are CircuIT free-tier (15 RPM, 120K peak tokens/min, 50M monthly input, 5M completion, $0 quarterly). Operator testing showed flash-lite delivered materially lower per-customer LLM latency on the comprehensive report's per-customer storyboard loop while preserving the R66/B11 + R67/B8 grounding-pass rate. **The R69 (Build 43) operator-flippable seam and R73 (UX-3) two-option dropdown contracts are unchanged — `gpt-5-nano` remains a one-click toggle in every preferences surface.** Both `<select data-r69-model-input>` blocks on `/preferences` (and the admin console mirror) list `gemini-3.1-flash-lite` as the FIRST `<option>` with `selected` so the implicit default agrees with the resolver layer for fresh installs. The `Config.CIRCUIT_CONFIG` fallback strings MUST track `model_resolver._HARDCODED_DEFAULT` byte-for-byte; a config/resolver split would let `CircuitChatClient` instantiate with one model while `model_resolver.get_active_*_model()` returned another, producing silent inconsistencies in per-call diagnostic records — pinned by `tests/test_round77_default_model_flip.py::test_round77_config_circuit_config_default_matches_resolver`. Pinned by `tests/test_round77_default_model_flip.py` (16 tests covering resolver default / per-resolver fallthrough / settings-empty fallthrough / gpt-5-nano back-toggle / config-resolver drift guard / dropdown first-option order on both /preferences and admin / `selected` attribute / descriptive copy positive + negative controls / docstring narrative / build-number floor).

**Comprehensive narrative stub-bullet filter (Round 78 / B1 — Build 54):** `adoptiq_backend.append_to_word_report` MUST drop bullet lines whose entire text matches `_R78_STUB_RE` — a module-level regex that fires ONLY on pure `<Category>: Data unavailable.` stubs (the Build 53 audit found 170 such bullets per Comprehensive run, all generated by the `PROMPT_CUSTOMER_TEMPLATE`'s 8 sub-categories when the briefing has no data for them). The regex is anchored `^...$` and includes optional `**` bold-wrap permutations (4 combinations covered: `**Cat:** stub`, `**Cat**: stub`, `Cat: **stub.**`, `**Cat: stub.**`); the prefix is constrained to start with an uppercase letter so generic lowercase user text never matches. Bullets with substantive narrative AFTER the marker (e.g. `"Operational Disruption: Data unavailable. No active incidents."`), parenthetical mid-sentence usage (e.g. `"... (SP-ID: data unavailable) ..."`), and ARR/financial commentary that uses the marker as preamble are ALL preserved. The skip path mirrors the empty-line-skip convention at the top of the writer's while loop (`i += 1` BEFORE `continue`) so the loop never infinite-loops on the unincremented index. **Future LLM prompts that emit bullet lists MUST either (a) emit only substantive bullets, OR (b) extend `_R78_STUB_RE` if a new "data unavailable" sentinel pattern emerges. Do NOT inline-rewrite the prompt to remove the "state data unavailable if empty" instruction — the LLM honesty contract benefits the audit trail upstream of the post-filter.** Pinned by `tests/test_round78_b1_comprehensive_stub_bullet_filter.py` (28 tests covering source-shape, regex match/no-match cases, bold-wrap permutations, substantive-bullet preservation, end-to-end paragraph-count, no-hang regression).

**Leader Action_Plans XLSX dedup (Round 78 / B2 — Build 54):** the leader XLSX writer in `app_simple.py` MUST run `pd.concat(all_action_plans, ignore_index=True)` through a `drop_duplicates(subset=['ID'], keep='first')` pass before assigning `sheets['Action_Plans']`. Build 53 audit caught 366 sheet rows / 344 unique IDs (22 duplicate AP rows) for Brian Frazier 90d — same root cause as R75/B2 (Adoption_Barriers) but on a different sheet: when a single Action Plan is attributed to multiple CSSMs via the R72 `_ATTRIBUTED_BY_ACCOUNT` shared-account pathway in `_slice_by_owner_or_account`, per-CSSM AP frames each carry the SAME ID and the raw `pd.concat` emits the row N times. Rows without an ID column (rare malformed-export edge case) are passed through unchanged via the explicit `notna() / isna()` split. The dedup MUST emit a structured `logger.info` line with `"%d raw rows -> %d unique"` formatting so the operator can correlate the XLSX sheet's row count against `canonical_metrics.count_open_action_plans`. **The R75/B2 (AB) and R78/B2 (AP) blocks are co-located and ordered AP-then-AB to match the existing writer flow** — a future refactor that consolidates them into a shared helper MUST pick up both sheets together. Pinned by `tests/test_round78_b2_leader_action_plans_dedup.py` (8 tests covering source-shape / log-format / R75-R78 mirror / 2-CSSM shared-AP collapse / canonical-count parity / no-ID frame / partial-ID frame / empty-list guard).

**Leader vs Comprehensive scope-filter divergence is BY DESIGN (Round 78 / F3):** Leader and Comprehensive reports scope-filter Action_Plans (and Adoption_Barriers, Customer_Pulse, Success_Priorities, TAC_Cases) through DIFFERENT predicates — Leader uses per-CSSM email matching (`Owner.Email IN team_emails OR Assignee.Email IN team_emails`) while Comprehensive uses per-Account-ID matching (`Account.Id IN team_subscription_accounts`). For accounts whose APs have a blank `Account Manager` field AND assignees outside the team's CSSM list (e.g. Cisco-on-Cisco internal accounts where the AP is owned by a Cisco IT subaccount, not a customer-facing CSSM), the two reports legitimately diverge. Build 53 audit observed a 1.7% delta (6/350 rows) — Comprehensive carried 6 APs that Leader did not, all on internal Cisco accounts with blank Account Manager. **This is NOT a regression and MUST NOT be "fixed" by collapsing the predicates.** The two reports answer different questions: Leader = "what work is my team doing?" (email-driven), Comp = "what work touches my accounts?" (subscription-driven). Future scope-edge anomalies under this category should be documented in this section (or QUALITY_AUDIT.md as a deferral note) — not coerced into single-predicate parity. No code change. No test (this is a design choice, not a regression-risk).

**BE-engineering priority barrier analysis (Round 79 / Build 55):** the Comprehensive and Leader reports MUST publish an independent priority signal for adoption barriers that is decoupled from CSConsole's user-set `Priority` field and grounded in a deterministic risk + customer-sentiment formula. The architecture is **hybrid**: `be_priority_scorer.compute_be_priority_score` deterministically ranks ALL ABs by a weighted formula combining capped severity (P1=1.0..P4=0.4), customer composite risk (0..1 from `risk_profiles[customer]['composite_risk']`), pulse negativity (0..1 derived from per-customer pulse score with `PULSE_NEGATIVE_THRESHOLD_0_TO_10=5.0`), open age (linearly scaled, 90d cap), content signal (regex hits in title + description for blocker / outage / cannot / urgent / impacting / etc.), and the explicit `AB_ESCALATE_C` flag — output is a 0..100 priority score plus a `be_top_signal` dominant-signal field. **Only the top N (operator-tunable via `Config.BE_PRIORITY_LLM_TOP_N`, default 50) are then tagged by a second-pass LLM** into one of `TRUE_BLOCKER` / `TRAINING_GAP` / `FEATURE_REQUEST` / `USER_BEHAVIOR` / `DUPLICATE` / `OBSERVATION_ONLY`. **The LLM tag MUST inform the narrative but MUST NEVER change the deterministic score** (the scorer's output is the canonical priority; the classifier is descriptive context). `be_priority_pipeline.build_be_priority_outputs` orchestrates scorer→top-N selection→LLM classifier→merge and returns `(barriers_df, focus_df, diag)` where `barriers_df` carries the per-AB ranking and `focus_df` is the per-`(sub_technology, ab_category_final)` rollup. Both DataFrames are written to the Comprehensive XLSX as `BE_Priority_Barriers` and `BE_Focus_Areas` sheets AND surfaced in BOTH the Comprehensive and Leader DOCX via `be_priority_word_section.add_be_priority_focus_areas_section` (banded per-technology tables). The Compact and Renewal reports are explicitly OUT of scope for this round. **Hoist contract for the per-customer briefing (B6):** `app_simple.run_comprehensive_analysis` MUST call `build_be_priority_outputs` BEFORE the per-customer narrative loop AND merge `be_priority_score` + `be_llm_class` back into `ab_norm` so per-customer slices (`cust_ab`) inherit the columns when the briefing book reads them. `adoptiq_backend._create_briefing_book` then emits four R79 fields on each AB row (`CSConsole_Severity`, `Independent_BE_Priority`, `Independent_BE_Class`, `Days_Open`) when present and non-empty — `UNCLASSIFIED` and blank `be_llm_class` values are filtered out so noisy sentinels never reach the LLM. `Config.BE_PRIORITY_BRIEFING_ENABLED=False` is the operator kill-switch (defensive: re-resolves through `sys.modules['config'].Config` per call so `importlib.reload(config)` pollution from sibling tests doesn't strand a stale `Config` reference and silently no-op the kill-switch). `Config.BE_PRIORITY_LLM_ENABLED=False` is a separate kill-switch that disables the second-pass LLM classifier (rows ship with `be_llm_class=UNCLASSIFIED` and the deterministic score still ranks them). **Future report formats that publish per-customer priority barriers MUST follow this same hybrid template: deterministic scorer FIRST, then LLM classifier on top-N only, never reverse the order — letting the LLM rank the full set re-introduces the hallucination risk this architecture exists to avoid.** Pinned by `tests/test_round79_b1_be_priority_scorer.py` (41 tests — formula determinism, severity capping, age scaling, content signal, dominant-signal identification, vectorized frame helper, weights dataclass), `tests/test_round79_b2_be_priority_llm_classifier.py` (38 tests — kill-switch, JSON parsing, ID validation, class normalization, top-N cap, PII redaction), `tests/test_round79_b3_be_focus_areas_rollup.py` (15 tests — groupby determinism, true_blocker boost, max_per_tech cap, sample-issue formatting), `tests/test_round79_b4_be_xlsx_sheets.py` (27 tests — pipeline integration, provenance rows, LLM kill-switch, XLSX round-trip, HTML strip allowlist, app_simple wiring), `tests/test_round79_b5_be_word_section.py` (23 tests — heading sort, table structure, intro text, end-to-end DOCX round-trip), `tests/test_round79_b6_briefing_enrichment.py` (17 tests — per-field emission, NaN/blank/UNCLASSIFIED filtering, kill-switch, hoist source-shape, merge source-shape).

## Development Workflow

Branch model: develop on machine-specific branches (`pc-sync-YYYY-MM-DD`, `mac-sync-YYYY-MM-DD`), integrate to `master` only after `make verify` passes and platform build + smoke test complete.

Quality gate (from `.cursor/rules/quality-gate.mdc`): Never hide or skip tests. When fixing bugs, add a regression test. Run the narrowest relevant check first (`pytest tests/test_<module>.py`) before the full suite. Changes must report: files changed, tests added/updated, commands run, pass/fail status.

Audit log convention: when adding a Round-N audit, use `# Round N` markers in the affected source so `git diff <file> | grep 'Round N'` gives a per-file footprint; document phase-by-phase findings, residual risks, and follow-ups in `QUALITY_AUDIT.md`. See Round 14/15/16 sections for the established format.

## Loop conventions (Cursor ↔ Claude Code)

This repo runs a two-tool loop: **Cursor generates code, Claude Code audits and writes a review back**. The loop is bootstrapped in Round 0 (see `QUALITY_AUDIT.md`).

**Where things live:**
- LLM-bible (this file): `CLAUDE.md` — invariants, SSoT modules, critical rules.
- Audit journal: `QUALITY_AUDIT.md` — per-round handoff + review log.
- Verify gate: `make verify` — lint + security + audit + test. **Local contract**; CI (`.github/workflows/build.yml`) runs only `pytest -q`.
- Cursor session-close handoff: appended to `QUALITY_AUDIT.md` under `## Round N — handoff <date>`. Format pinned in `.cursor/rules/session-handoff.mdc`.
- Claude session review: appended under the same Round, in a `## Round N — Claude review` subsection.
- Review standards: `.cursor/rules/quality-gate.mdc` (in-session gate) and `.cursor/BUGBOT.md` (PR-style review checklist) — both apply to Claude as well.

**Commit trailers** (existing convention — keep):
- Cursor commits: `Made-with: Cursor`
- Claude commits: `Made-with: Claude Opus 4.7 (1M context)` (or current model)

**Round-N source markers** (existing convention — keep): when changing code as part of Round N, drop a `# Round N` comment so `git diff <file> | grep 'Round N'` shows the per-file footprint.

**Floor:** every round must hold the Round 0 floor — `make verify` clean with at least the test count recorded in Round 0. A round that lowers the floor is a regression and must explain why in the handoff under **Known deferrals**.
