Complete the live Cisco truth gate for AdoptIQ Round 167 on the approved Cisco
Mac with VPN and its authorized Keeper, Snowflake, CSConsole, CSOne, CircuIT, and
OneDrive configuration. Use extra-high reasoning. Before mutation, fully read
`CLAUDE.md`, `NEXT_MACHINE_PROMPT.md`, and `QUALITY_AUDIT.md` Round 167/167.2/167.3.

Known truth: candidate source is
`6a956e203b88a3586251d7f63eae49b319a653c0` on `rupret007/AdoptIQ`. The
stage-only arm64 v1.0.4 Build 114 DMG is 1,708,039,038 bytes with SHA-256
`4cc485daf1bc695ec1376dd6161ce47eb10ca3dae0c67be11c8f91a4a17d6235`.
It is not published/approved. Preflight, DMG/signatures, and frozen smoke passed.
Its corpus is 358/358 files and 488,079/488,079 lexical/dense rows, hybrid-ready.
Local simulation passed 36/36 A-G, 24/24 multi-manager, four decision scopes twice,
23 degraded states, 75/75 Ask AI plus 25/25 canonical checks. It correctly remains
`live_validation_performed=false`, `production_accuracy_claimed=false`, and
`release_ready=false`.

1. Preserve local work and the internal Cisco remote/config. Fetch
`rupret007/main`; require a clean integration branch and prove `6a956e2` is its
ancestor. Never force/reset/clean, merge unrelated history, copy credentials, or
whole-file replace Cisco connection code. Preserve parameter binding, allow-lists,
redaction, fail-closed manager/member/customer/subscription scope, CSOne discovery,
and localhost/CSRF controls. Stop on history/semantic ambiguity.

2. Run `make verify` and `make production-simulation` with approved CSOne exports;
any failure/lower inventory stops. Execute the metadata-only Snowflake inventory in
`NEXT_MACHINE_PROMPT.md`. If unreachable, require its
sanitized `connection_unavailable` summary, zero row queries, and no raw stderr.
Never widen allow-lists. For accessible fields record meaning, join/cardinality,
scope authorization, source clock, decision value, canonical target, and evidence.
Missing permission is `blocked`, never zero.

3. Verify the DMG hash/size, then run the normal app with existing Cisco config.
Confirm `/ping`, `/api/version`, connectivity, corpus/model, DSM, and source-state
diagnostics. Run the documented Round 146 `work-machine` command with an authorized
manager, member, unambiguous customer, subscription, All Contact Center, frozen
90-day as-of, and current CSOne file. Also test All Managers and a second team.
Exercise Leader Team/Member/Customer twice; Comprehensive portfolio/customer;
Compact; Renewal portfolio/customer; Subscription; A-G; Ask AI sync/stream and
report-bound scopes; evidence drill-down; and degraded/live provider states.
Out-of-roster, ambiguous, and mixed-scope requests must fail before data access.

4. Accuracy—not generation—is the release criterion. Reconcile every Word KPI and
chart to `Chart_Data`, claim to `Metric_Lineage`, and evidence to `Evidence_Links`
plus the exact live record. Reconcile AP lifecycle/age to raw ID/status/dates/owner;
TAC/BEMS to distinct IDs; renewal exposure/outlook separately from AdoptIQ risk;
and ownership/shared attribution without inflation. Open at least one Action Plan,
Barrier, Pulse, and Success Priority CSConsole link; verify object, record, account,
and claim. Inspect every DOCX page/workbook sheet for missing charts, clipping,
orphan pages, raw `undefined`/`null`, unexplained `Unknown`, fake links, or misleading
zeroes. Unavailable/partial must be disclosed, never guessed.

5. Never weaken tests/validators or let LLM text author canonical facts. Keep live
artifacts outside Git; commit only sanitized evidence. If a mismatch needs code,
stop, add source fix/regression, invalidate Build 114, choose a new build number,
and repeat all gates—never patch/reuse the app. If all passes, present exact diff,
live and manual reconciliation, visual review, risks, and GO/NO-GO before push,
merge, tag, manifest/OneDrive change, or deploy. Promotion needs separate approval
and must preserve the existing PC manifest slot byte-for-byte.
