# macOS
.DS_Store

# Cursor local planning artifacts
.cursor/plans/

# Build artifacts
dist/
build/
bake/
*.exe
*.zip
_bundled_secrets.py
secrets.env
secrets.env.bak-*
version_info.txt
*.spec.bak

# Python
__pycache__/
*.py[cod]
venv/
.venv/
.env

# Installer output
OUTBOX/
developer_candidates/
native_candidate_stage/
*.dmg
v1.0.*/

# Runtime artifacts
*.log
*.db
analysis_status.json
diff_full.txt
build_info.txt
# Release-candidate provenance is intentionally tracked.  Keep the broad
# runtime/build ignore above, but allow immutable candidate sidecars to survive
# a clean clone and CI checkout.
!release_candidates/**/build_info.txt
build_output.txt
uploads/
.tmp/
# Round 52: dev-run report outputs (Word/Excel) accumulate here
# during local pytest + harness iteration; they are NOT release
# artifacts (those land in OUTBOX/).  baselines/ IS committed --
# it carries the manifest-pinned baseline floor under
# baselines/round<N>/baseline_manifest.json.
outputs/
# Round 143: supported decision-report acceptance output.  The runner also
# refuses an output directory inside Git metadata, but ignore the conventional
# local folder as defense in depth against customer artifacts entering a commit.
.adoptiq-acceptance/
decision-report-acceptance-*/

# Round 89 / Build 65 + later: ephemeral helpers and per-build audit
# findings produced by the live acceptance loop.  Source-of-truth
# audit results land in QUALITY_AUDIT.md; these files are scratch.
_r72_dev_launcher.py
_audit_*.py
_audit_*.md
.venv311/

# Round 161: dev fastembed ONNX cache (downloaded on first hybrid retrieval warmup)
embeddings/fastembed_cache/
embeddings/release_fastembed_cache/
