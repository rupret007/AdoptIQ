# AdoptIQ Executive Analyzer - Production Dependencies
# Core data processing and analysis
pandas>=2.0.0
numpy>=1.24.0

# Database and data warehouse connectivity
snowflake-connector-python>=3.0.0
snowflake-sqlalchemy>=1.4.0
sqlalchemy>=2.0.0

# Excel file processing
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# Word document generation
python-docx>=0.8.11

# Web framework
Flask>=2.3.0
Flask-WTF>=1.1.0
WTForms>=3.0.0

# Authentication and security
hvac>=1.0.0
cryptography>=41.0.0

# Round 36 / onedrive-sync-auth: msal + keyring were removed when the
# MSAL/Graph runtime path retired in favor of the OneDrive desktop
# client's local mirror at Config.CSONE_ONEDRIVE_FOLDER.  No runtime
# code path imports either package any longer.

# TLS trust store integration (lets urllib3/requests/hvac/snowflake use the
# OS keychain so Cisco corporate TLS inspection no longer breaks keeper.cisco.com)
truststore>=0.9.0

# AI and machine learning
openai>=1.0.0

# Round 66 / Pass 5 - Hybrid retrieval. fastembed ships ONNX Runtime so
# we get small-footprint dense embeddings (BAAI/bge-small-en-v1.5,
# 384-dim, INT8 quantized ~33MB) without the PyTorch tax. The model
# binary is downloaded ONCE on first use (or pre-baked into the .app
# under Resources/embeddings/ by build_mac_dmg.sh). At runtime we
# lazy-import fastembed so a missing install or model file gracefully
# downgrades the Ask AI path to lexical-only retrieval rather than
# raising. Pin: >=0.8.0 (current stable; default model is bge-small).
fastembed>=0.8.0

# HTTP requests and web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
feedparser>=6.0.0

# Data visualization and reporting
rich>=13.0.0

# Command line interface
typer>=0.9.0

# Testing
pytest>=7.0.0
pytest-flask>=1.3.0

# Development and packaging
pyinstaller>=5.13.0

# Environment variables (.env)
python-dotenv>=1.0.0

# Additional utilities
python-dateutil>=2.8.0
pytz>=2023.3
psutil>=5.9.0
