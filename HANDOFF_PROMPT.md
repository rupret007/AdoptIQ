# AdoptIQ — Round 169 handoff (pending Build 116)

The authoritative runbook is `NEXT_MACHINE_PROMPT.md`; the bounded copy-ready prompt
is `WORK_MACHINE_BUILD116_PROMPT.md`. Exact sanitized evidence is in the latest Round
169 section of `QUALITY_AUDIT.md`.

## Release truth

- Product source identity is v1.0.4 Build 116.
- Build 115 is immutable historical evidence with status **invalidated / NO-GO** in
  `release_candidates/macos-build115/candidate.json`. It must not be accepted,
  installed, promoted, published, overwritten, or recreated.
- No Build 116 candidate exists. Do not invent a candidate SHA, artifact digest, byte
  size, build time, smoke result, live result, manual approval, or release approval.
- Local verification and real-shape pseudonymous simulation are offline evidence only:
  `live_validation_performed=false`, `production_accuracy_claimed=false`, and
  `release_ready=false` remain mandatory.
- Packaging, live acceptance, manual review, and promotion are separate fail-closed
  stages requiring separate authorization.

## Round 169 source contract

- Stable source IDs are reconciled deterministically. Exact and complementary query
  observations coalesce; substantive same-ID disagreement is quarantined from public
  counts/claims; missing IDs remain explicit data-quality limitations.
- Compact, Comprehensive, Leader, and Renewal parity covers the exact ordered 17-sheet
  Source Data inventory and ten canonical count/identity/semantic/attribution/state
  signatures, including `Evidence_Links` and `Defect_Correlations`. Family-specific
  presentation facts require explicit typed markers.
- Public source sheets restore curated, type-stable evidence fields and normalize only
  route aliases. Canonical metrics/risk/source/freshness remain deterministic.
- The CSOne corpus is profiled metadata-first and prepared once into a bounded,
  canonical, pseudonymous replay. Both guarded runtimes must deserialize the same
  digest-bound payload with zero source-loader calls and matching pre/post identity
  probes. Raw rows/values never enter the summary or Git.
- Acceptance processes, stdout/stderr, JSON, OOXML expansion, artifact roots, and
  scratch permissions are bounded. Timeouts and process trees fail closed and are
  reaped. R114 evidence retains only counts/status/digests.
- The eight-check metamorphic gate covers artifact invariance, duplicate invariance,
  conflicting-duplicate quarantine, invalid-ID publication blocking, identity
  quarantine, freshness truth, scope isolation, and Ask AI origin transport.
- Manager Team/Member/Customer UX, exact lower-bound disclosures, canonical links,
  report-bound facts, and sync/SSE trust parity remain preserved.

## Required next action

On the authorized Cisco work machine, start from a clean exact `rupret007/main` tip and
rerun Python 3.12 `make verify`, the 18-gate external-corpus production simulation,
metadata-only Snowflake capability discovery, and authorized live source reconciliation.
Exercise all report families/selections, two named managers plus All Managers, Leader
Team/Member/Customer, customer/subscription scopes, exact source links/rows, charts,
lineage, attribution, freshness, degraded states, and Ask AI sync/SSE.

Any defect returns to source with a focused regression and complete gate rerun. Only
after explicit source and packaging approval may a new Build 116 DMG and create-only
candidate directory be produced from the exact clean commit. Frozen smoke, live
acceptance, manual visual/source reconciliation, promotion, publication, installation,
and deployment remain NO-GO until separately completed and approved.

## Security

Never commit reports, CSOne data, runtime logs/databases, credentials, or candidate
binaries. Preserve Cisco connection code and remotes. A credential previously pasted
into conversation context must remain revoked/rotated and absent from all evidence.
